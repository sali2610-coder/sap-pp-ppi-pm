# Research queue · functions catalog (S/4HANA enrichment)

Kept by the single writer for `data/verification/functions.ts`. One line per id that was
refuted or deferred at the adversarial-verification gate, with the evidence still missing.
Written 2026-09-02 during the functions data commit (12 drafts audited: 12 written with
their auditors' downgrades applied; 0 refuted).

## refuted / needs new evidence

- (none in this batch — all 12 audited drafts survived verification and were written with downgrades applied.)

## conflicts

- `fm:ARCHIVE_DELETE_FROM_DB` — `data/function-intel.ts#ARCHIVE_DELETE_FROM_DB` (inferred) describes the FM as performing the ADK delete phase, while the official ADK Interface page (fully read) lists no module of that name and states ADK interface modules cannot delete from the database (the archiving object's delete program does, scheduled via SARA/RSARCHD). Classified `verification_required`, not `conflicting_sources`, because the repo record itself declares `inferred: true`.
- `fm:CARO_TASKLIST_READ` — internal repository inconsistency: `data/transactions.ts` (IA05), `data/troubleshooting-ext2.ts` (tasklist-not-in-plan) and `data/domains.ts` present the FM as usable, while `lib/verification.ts` classifies the `^CARO_` pattern as suspicious and `data/function-intel.ts` flags the entry `inferred: true`; additionally `data/academy/lessons/pm-generated.ts:142` labels it trust `verified-docs` ("SAP Help Portal — Plant Maintenance (S/4HANA)"), which this session's official searches could not reproduce — either add the missing official citation or fix the trust label.
- `fm:CM_FV_PROD_VERS_READ` — (1) `data/academy/lessons/pp-generated.ts:265` labels a refs block naming this FM trust `verified-docs` ("SAP Help Portal — Production Planning (S/4HANA)"), a label the session's official searches could not reproduce for the FM name. (2) Fiori id drift for "Manage Production Versions": F2568 in `data/library/fiori-apps.json` vs F2703 in `data/sapData.pppi.ts:7549`; neither id SAP-verified this session, so the app stayed out of the record's xrefs.
- `fm:CR_COST_CENTER_READ` — `data/academy/lessons/pp-generated.ts:205` and `:235` label CR_COST_CENTER_READ trust `verified-docs` sourced to "SAP Help Portal — Production Planning (S/4HANA)", but no public Help page names this FM (lines 208/214 also name it at trust `curated`, which is consistent); `data/function-intel.ts:308` marks the same FM `inferred: true`. The `verified-docs` label overstates the evidence.
- `fm:CVI_VENDOR_TO_BP_CONVERT` — repo files (`data/function-intel.ts:239`, `data/sapData.pppi.ts:5233` and `:6069`, `data/sapData.pm.ts:4219`, `data/ecc-s4.ts:112`, `data/troubleshooting-ext2.ts:52`) present the FM as existing with inputs/outputs, while the official Business Partner Conversion Activities document (read in full: zero occurrences) and help.sap.com name only MDS_LOAD_COCKPIT, CVI_UPGRADE_CHECK_RESOLVE (SAP Note 2344034) and PRECHECK_UPGRADATION_REPORT (SAP Note 2211312). Recommend repointing repo content to the officially documented tools instead of the unverified FM name.
- `fm:BAPI_ALM_ORDER_MAINTAIN` — naming nuance, not a repo conflict: api.sap.com titles API_MAINTENANCEORDER "Maintenance Order - Read", while help.sap.com 2025.001 documents write operations under the same service path (including `;v=2`); the write-capable on-premise Hub listing is OP_API_MAINTENANCEORDER_0001. Record both names so the read-only title is not taken to cover the full modern-API scope.
- `fm:BAPI_PROCORD_CREATE` — (1) `data/function-intel.ts:26` names the modern OData alternative "API_PROCESSORDER_2"; the name given by official help.sap.com snippets is API_PROCESS_ORDER_2_SRV ("Process Order (Version 2)") — repo spelling drift worth normalizing. (2) `data/bapi-enrichment.pppi.ts:51` marks releasedStatus "Released · RFC" sourced to "SAP Help Portal + SE37 metadata", but no public official page naming the BAPI was found this session; the released state is repository-verified only. (3) The draft's OP_API_PROCESS_ORDER_2_SRV_0001 on-premise Hub variant was removed at audit — api.sap.com content is not verifiable without a Hub API key (bogus ids also return HTTP 200); re-add only with a citable source.

---

# Functions batch 2 (2026-09-14): 14 drafts audited, 14 written, 0 refuted

Written by the single writer from the adversarial verdicts: `verdict.fixedRecord` taken verbatim where present (13 records); `fm:CO_ZF_ORDER_ITEM_READ` = researcher draft + its single downgrade (status.he now points production-order relevance of AFPO to the `table:AFPO` record). Writer-level normalisations: `reviewer` dropped on all 14 (no overlay file carries the field); `fm:BAPI_ALM_NOTIF_GET_DETAIL` Hub evidence sourceTitle trimmed to the verbatim search-result title "Overview | Maintenance Notification" (the parenthesised technical name was auditor-constructed; the domain-restricted search on 2026-09-14 returned exactly that title at `/api/OP_API_MAINTNOTIFICATION/overview`); `fm:BAPI_GOODSMVT_CREATE` status.source shares the `GOODSMVT_ODATA_API` const with its evidence[2]. The one em dash in the batch is inside the verbatim SAP deliverable title "Logistics — General (LO)" (returned as-is by the Help search service) and was left untouched.

## refuted

- (none in this batch — all 14 audited drafts survived verification and were written with their auditors' downgrades applied.)

## conflicts

- `fm:BAPI_ALM_NOTIF_CREATE` — `data/fiori/apps.ts` F1511 (line 37) and `data/function-intel.ts:66` name the OData service API_MAINTENANCENOTIFICATION; every official 2025.001 record names API_MAINTNOTIFICATION. F4604 is NOT part of this gap (it carries API_MaintenanceOrder). The module is absent from `lib/bapi-registry.ts`; its BOR object BUS2038, parameters and Released state come only from `data/bapi-enrichment.pm.ts` (repository tier).
- `fm:BAPI_ALM_CONF_CREATE` — parameter name disagreement inside the repository: `data/function-intel.ts` names input table CONFIRMATIONS (ORDERID, OPERATION, WORK, FIN_CONF), `data/bapi-enrichment.pm.ts` names TIMETICKETS (BAPI_ALM_TIMECONFIRMATION); neither is officially verified. The function-intel alternative "Fiori Confirm Maintenance Order" is not an F-ID in the catalog. The "verified-docs / s4OnPremSupport yes / Released · RFC · created 4.6 (110)" values in bapi-enrichment.pm.ts are g() template defaults (lines 44-48), not record-specific data.
- `fm:BAPI_ALM_NOTIF_GET_DETAIL` — `data/bapi-enrichment.pm.ts` marks BAPI_ALM_NOTIF_LIST_FILTER `invalid-name` although the id resolves in the universe; dropped from xrefs. `fm:NOTIF_ITEM_READ` is kept as an xref but its own sweep record flags the name as unverified. function-intel spells the OData alternative API_MAINTENANCENOTIFICATION (see NOTIF_CREATE).
- `fm:BAPI_ALM_NOTIF_CLOSE` — the literal Function Import name ("Complete Maintenance Notification") is not visible in the 2023.latest snippet (only NOCO + reference date/time and the NOPR operation); api.sap.com existence rests on a search-result title at `/api/API_MAINTNOTIFICATION/resource` (the page shell is byte-identical to that of a nonexistent API path, so a resolving URL proves nothing). A help.sap.com-restricted web search for the exact BAPI name returns the Data Migration page c03f981dd76f4fc7a241f17adc80758b as a hit, but no official snippet shows the name.
- `fm:BAPI_ALM_NOTIF_CHANGEUSRSTAT` — `data/bapi-enrichment.sweep.ts` names parameters USER_STATUS / USER_ST_CODE while `data/bapi-enrichment.pm.ts` names USR_STATUS / SET_INACTIVE (sweep is a dead patch per baseline-inventories.json); KBA 1923267 exists only in the repo record and was not verified; the record's verified-docs source carries no URL.
- `fm:BAPI_ALM_NOTIF_DATA_ADD` — `data/function-intel.ts` describes adding data "to a notification not yet saved" while `data/bapi-enrichment.pm.ts` says "existing notification"; both usages plausible, unverified officially. BAdI IWON_NOTIFICATION (the only official BAPI-level pointer, Maintenance Notification 2025.001) is absent from `data/exits.ts` / `data/enhancements.ts`, so it cannot be an xref. Create Notification Item Cause surfaces only at 2023.latest (loio 589fe8cb9f6a4a3fa52e5a07ed10bf5d); cause coverage in OData stays unconfirmed for 2025.001.
- `fm:BAPI_GOODSMVT_CREATE` — presentation drift, not a source conflict: without an authored status the function page derived "unchanged" (verified-docs + s4OnPremSupport yes) while BASELINE.md:56 records a "changed" tone from the MSEG attachment; the authored released_api_available now carries the official picture. BAPI_GOODSMVT_CANCEL, MIGO_DIALOG, MB_CREATE_GOODS_MOVEMENT (named in Simplification items 27.6/27.9) and Fiori F1807 are not ids in the dataset.
- `fm:CKMVFM_MATERIAL_PRICE_READ` — the name may be a composite of the CKMVFM transaction prefix and a functional description (no official page, unrestricted web returns only the Value Flow Monitor transaction); only SE37 can settle it. The blueprint note "ACDOCA/ACDOCC" (data/sapData.pppi.ts MBEW) is unconfirmed officially. I_MaterialPrice / I_ProductValuationBasic do not exist in the universe; I_ProductValuation does and is officially documented (VDM 2023.latest, Released), but its price fields are Tier-2 only (data/cds-enrichment.ts).
- `fm:CO_ZF_ORDER_HEADER_READ` — `data/academy/lessons/pp-generated.ts:443` labels the FM trust `verified-docs` ("SAP Help Portal — Production Planning (S/4HANA)"); the file header says the lesson was generated from domain-detail + function-finder, and no help.sap.com search reproduces the name. Label overstates the evidence.
- `fm:CO_ZF_ORDER_ITEM_READ` — official archiving page (2025.001) states AFPO exists in PM/CS only for refurbishment orders, so the catalog's "maintenance orders" assignment of this FM needs that caveat; Fiori "Find Maintenance Order (F2393)" named in the AFPO blueprint is not in `data/fiori/apps.ts`; the module is absent from `lib/bapi-registry.ts` and `data/domain-detail.ts`.
- `fm:CP_DI_OPERATION_READ` — the repository assigns the same module to PP-PI master-recipe operations/phases (`data/sapData.pppi.ts` PLPO) and to PM task-list operations (`data/sapData.pm.ts` PLPO, IA05/IA06); API_MASTER_RECIPE covers only the former, so no released_api_available was authored. `data/academy/lessons/pm-generated.ts:142` labels it `verified-docs`, not reproducible.
- `fm:CR_WORKCENTER_READ` — `data/academy/lessons/pm-generated.ts:171` labels it `verified-docs` ("SAP Help Portal - Plant Maintenance (S/4HANA)"), not reproducible; `data/function-intel.ts` says "זמין ב-S/4HANA" with no inferred flag and no source; `data/domains.ts:92` (pm-work-centers) lists it under `bapis` although it is not a BAPI; the PP-PI blueprint names the sibling CR_WORK_CENTER_READ for the same table (both ids in the universe, neither verified). Out of scope but noted: the `table:CRHD` record's note says tx:IR02 / tx:IR03 have no page; both are in ROUTE_MANIFEST.tcodes and build pages, so that note is wrong.
- `fm:CRAP_WORKCENTER_GET_DETAIL` — `data/bapi-enrichment.sweep.ts:85` marks CR_WORK_CENTER_READ invalid and points to CR_WORKSTATION_READ / CR_RFC_WORKCENTER_LIST (neither in the universe); `lib/verification.ts` classifies `^CRAP_` as suspicious while domains / transactions present the FM as usable; `data/academy/lessons/pm-generated.ts:171` labels it `verified-docs`. Official Data Migration pages (fbb00ccf ERP, 70a186f1 AFS; 2025.001) name only the sibling CRAP_WORKCENTER_CREATE; `fm:CRAP_WORKCENTER_CREATE` is not in the universe. Manage Work Centers (F6175) is named officially but absent from `data/fiori/apps.ts`.
- `fm:CS_BOM_EXPL_EQU_RC1` — the material sibling CS_BOM_EXPL_MAT_RC1 is already `invalid-name` in the sweep (successor CS_BOM_EXPL_MAT_V2); the equipment variant has no sweep record and no official mention anywhere. Official VDM record names CDS view I_EQUIPMENTBOMLINK (Released, 2023.latest), absent from the project's cds catalog; consider adding it before citing a successor. KBA titles naming CS_BOM_EXPL_MAT_V2 live on userapps.support.sap.com (not allowlisted).

---

# Functions batch 3 (2026-09-21): 8 drafts audited, 7 written, 1 refuted

Written by the single writer from the adversarial verdicts: `verdict.fixedRecord` taken where present (6 records — `fm:BAPI_EQUI_CHANGE`, `fm:BAPI_EQUI_GETDETAIL`, `fm:BAPI_FUNCLOC_CHANGE`, `fm:BAPI_FUNCLOC_GETDETAIL`, `fm:BAPI_ALM_ORDER_GET_DETAIL`, `fm:BAPI_MATERIAL_SAVEDATA`); `fm:BAPI_FUNCLOC_CREATE` = researcher draft with its five record-level downgrades applied (HTTP-200 clause deleted, the re-verified loio list corrected from four to five, the `/opu/odata/.../FunctionalLocation` path attribution moved off loio b6a1e644, IL01 marked as not visible in the snippet and attributed to WN_OP2025_FPS01_EN.pdf §3.1.15 via `data/verification/enhancements.ts`, and the blanket "bodies unreadable" sentence narrowed). All seven carry `released_api_available` on a documented OData alternative; none claims a successor, none carries a SAP Note or KBA number.

Writer-level normalisations applied to all seven:
- The closing "this record carries no `reviewer` field" sentence was dropped from every `notes` (schema narration is not an SAP caveat; the convention is already stated in the file headers). No record carries a `reviewer` field.
- Em dashes removed from authored Hebrew prose. The only two that remain are inside the verbatim repository label `'SAP Help Portal — verified 2026-07-15'` (the literal `SRC_HELP` at `data/bapi-enrichment.sweep.ts:14`).
- Six shared `Evidence` consts hoisted to file level, matching the `ALM_V2_OPS` / `GOODSMVT_ODATA_API` precedent, so a status source and its evidence entry cannot drift: `EQUI_OPS_WRITE`, `EQUI_OPS_READ`, `FLOC_API_CREATE`, `FLOC_API_READ`, `ALM_ORDER_OPS_V2`, `PRODUCT_A2X`.
- `fm:BAPI_EQUI_CHANGE` evidence[1].sourceTitle: the label "Chapter 3: Corporate Services" was replaced by the actual file name `Chapter_03__Release_Notes_Corporate_ServicesE.PDF`, because the auditor established that label comes from the URL and not from the document's cover page. The cover-vs-chapter discrepancy stays disclosed verbatim in `notes`.
- `fm:BAPI_FUNCLOC_CHANGE` evidence[3]: the pointer "ראה conflicts" was repointed to this queue file, since `VerificationRecord` has no `conflicts` field.
- `fm:BAPI_MATERIAL_SAVEDATA` evidence[1]: the doubled ASCII apostrophes around `Scope Limitation` / `Scope Profile` were dropped and the source's single-quote punctuation is described in prose instead, so the quoted string does not misrepresent the snippet.

## refuted

- `fm:BAPI_EQUI_CREATE` — refuted at the adversarial gate and **not written**. Problems as recorded by the auditor: (1) evidence[0] presented `'Create [...] Equipment POST /sap/opu/odata/sap/API_EQUIPMENT/Equipment'` as explicitly visible in the snippets of loio d1e3c797d3f44120b552d0e64680e445 at 2025.001; across four re-runs every snippet truncated immediately after the bare word `Create`, so the text after the ellipsis was author-supplied. (2) The base-entity `PATCH` was likewise not seen on that page; only `Update Equipment Text PATCH` (EquipmentLongText) and `Update Equipment Partner PATCH` (EquipmentPartner) surfaced, plus the separate sentence `'The Etag handling feature is implemented on the A_Equipment entity and field LastChangeDateTime. PATCH requires etag to be specified.'` (3) The pages that do carry the literal rows are at the wrong release: `Create Equipment` (loio fc818bd5718f4d22ac68aae03d7c3465) and `Update Equipment` (loio 4ee7f73c08f84b2d98a8b7cc8fcd672f) surfaced only at 2023.latest, never at 2025.001. (4) The defect propagated into `status.he`, `status.recommendedAction` and the summary, so it could not be repaired inside evidence[0] alone. (5) `status.source` pointed at the weakest of the four evidences; loio 13d40bd35fc74d289e81fc284a928448 literally carries `'Create equipment master data Update equipment master data'` and `'Equipment (A_Equipment) Allows you to create an equipment.'` at 2025.001 and is the correct anchor. (6) Minor: `recommendedAction` named "Migration Cockpit", a name no cited 2025.001 source uses (the deliverable is `Data Migration`, the app is `Migrate Your Data`). (7) Minor: `notes` listed the authorization objects as I_IWERK / I_BEGRP, while `data/bapi-enrichment.pm.ts:203` lists `["I_IWERK", "I_BEGRP", "I_EQUI"]`. (8) Style: `status.source` inlined a second full copy of evidence[0] instead of sharing a const.
  - **Writer's measured counter-finding (2026-09-21, recorded, not acted on):** running `scripts/sap-help-search.mjs "Update Equipment PATCH host sap opu odata sap API_EQUIPMENT Equipment Equipment 217100091" --size 10 --json` returns, for loio d1e3c797d3f44120b552d0e64680e445 at versionId 2025.001, the snippet fragment `Equipment POST /sap/opu/odata/sap/API_EQUIPMENT/Equipment Update Equipment PATCH /sap/opu/odata/sap/API_EQUIPMENT/Equipment (Equipment='217100091', ValidityEndDate=datetime'9999-12-31T00%3A00%3A00`. The base-entity POST and PATCH rows therefore **are** snippet-bounded on that page; the highlight simply needs a query shaped to surface them, which is why four differently-shaped queries missed it. The refusal stands for this pass (the writer does not overturn an adversarial verdict), but the re-research is cheap: re-draft with that query recorded, anchor `status.source` on loio 13d40bd3 as the auditor asked, fix the "Migration Cockpit" naming and add `I_EQUI` to the authorization list.

## conflicts

- `fm:BAPI_EQUI_CHANGE` — three spellings of the same structure-and-flag pattern live in the repo, none SE37-verified: `data/bapi-enrichment.pm.ts` writes `DATA_GENERAL(+X), DATA_SPECIFIC(+X)`, `data/bapi-enrichment.sweep.ts:96` writes `DATA_GENERAL(X), DATA_SPECIFIC(X)` (no plus sign), `data/function-intel.ts:192` writes `DATA_GENERAL + DATA_GENERALX`. `lib/bapi-registry.ts:215` spreads `{ ...PM_ENRICHMENT, ...PPPI_ENRICHMENT, ...SWEEP_ENRICHMENT }`, so the sweep label ("SAP Help Portal — verified 2026-07-15", no URL) is the one the page shows. The record quotes all three and adjudicates none. The `Released · RFC` status and the function group are repository-only.
- `fm:BAPI_EQUI_GETDETAIL` — `data/bapi-enrichment.sweep.ts` says the BAPI returns location data, `data/bapi-enrichment.pm.ts` lists only `DATA_GENERAL_EXP` + `DATA_SPECIFIC_EXP`, and `data/function-intel.ts:193` lists only `DATA_GENERAL_EXP` and says "זמין ב-S/4HANA" without naming the OData alternative, although the same file does name `API_EQUIPMENT` on the `BAPI_EQUI_CHANGE` entry. `W0028` (Display Technical Object), named in the official migration page, is absent from `data/fiori/apps.ts`, so the record's only resolvable Fiori xref is the curated `F2730A`. An unauthenticated `curl` to `https://api.sap.com/api/OP_API_EQUIPMENT/overview` returns HTTP 401, so the Hub listing is title-only.
- `fm:BAPI_FUNCLOC_CREATE` — three parameter spellings again: `data/function-intel.ts` names `FUNCTLOC` in and `FUNCTLOCATION` out, `data/bapi-enrichment.pm.ts` names `IMP EXTERNAL_NUMBER, DATA_GENERAL, DATA_SPECIFIC · TAB RETURN`, `data/bapi-enrichment.sweep.ts` names `IN: FUNCTLOCATION, DATA_GENERAL, DATA_SPECIFIC · OUT: FUNCTLOCATION, RETURN`. No official snippet names any parameter, so the record makes no interface claim. The technical-object Fiori id is still split three ways in the repo (F2079 in the blueprint, F2730A in `data/fiori/apps.ts`, F1827 in `data/tx-intel.ts`) — see `research-queue-fiori.md`. `W0028`, `W0029` and `F3473`, all named in official snippets, are absent from `data/fiori/apps.ts`; `BADI_ASM_MD_FUNCLOC` and `BADI_EAM_ITOB_BAPI_CUST_FIELDS` are absent from `data/exits.ts` / `data/enhancements.ts`. The IFLOT-to-API_FUNCTIONALLOCATION mapping stays repository-only (carried over from `research-queue-tables.md`).
- `fm:BAPI_FUNCLOC_CHANGE` — internal repository split on the BAPI signature: `data/bapi-enrichment.pm.ts:214` and `data/bapi-enrichment.sweep.ts:109` give `FUNCTLOCATION, DATA_GENERAL(+X), DATA_SPECIFIC(+X)`, while `data/function-intel.ts:195` gives `FUNCTLOCATION, DATA_FLOC_CHANGE + X`. Both are Tier-2, no official source decides between them, and the record adopts the bapi-enrichment form while marking the signature unverified. Separately, What's New 2023 `Deprecation of Property for Functional Location` (loio 20b815a95ed74708a82851b999581685) deprecates only the `FunctionalLocationIsActive` property of the OData service, not the service and not the BAPI.
- `fm:BAPI_FUNCLOC_GETDETAIL` — three output-parameter spellings: `DATA_GENERAL_EXP` (`data/bapi-enrichment.pm.ts`), `DATA_GENERAL_EXP` + `DATA_SPECIFIC_EXP` (`data/bapi-enrichment.sweep.ts`), `DATA_FLOC_EXP` (`data/function-intel.ts`). The `verified-docs` / `Released · RFC` / `s4OnPremSupport yes` values are `g()` template defaults shared by the whole PM family, not record-specific data. `table:IFLOTX`, `cds:I_FunctionalLocationText` and `fiori:F3473` do not exist in the id universe and were therefore not linked, although the enrichment record names IFLOTX.
- `fm:BAPI_ALM_ORDER_GET_DETAIL` — `data/bapi-enrichment.pm.ts` lists `ET_COSTS`, `data/bapi-enrichment.sweep.ts` does not. Because `lib/bapi-registry.ts:215` spreads the sweep last, the sweep entry replaces the PM entry wholesale for this id, so the BUS2007 / IW33 / AUFK / AFIH / AFVC detail in the PM file never reaches the registry patch — a data-merge bug worth fixing independently of this record. The PM family's verification source is `SE37 metadata mirror (sapdatasheet.org) + SAP Community` (Tier-3) while the sweep record claims `SAP Help Portal`. `fiori:F2731` is kept only as a navigational reference: the official documentation binds the name "Manage Maintenance Orders" to `F5241`, which is absent from `data/fiori/apps.ts`, and the id binding is already recorded as `conflicting_sources` in `data/verification/fiori.ts`.
- `fm:BAPI_MATERIAL_SAVEDATA` — `data/bapi-enrichment.pppi.ts:81-86` gives `IMP HEADDATA, CLIENTDATA(+X), PLANTDATA(+X) … · TAB MATERIALDESCRIPTION, RETURN`, while `data/bapi-enrichment.sweep.ts:103` overwrites description, verification source and parameter summary with `IN: HEADDATA, CLIENTDATA(X), PLANTDATA(X), VALUATIONDATA(X) · OUT: RETURN`. The `releasedStatus` / `verificationStatus` / `s4OnPremSupport` / `stability` values are `def()` defaults (`data/bapi-enrichment.pppi.ts:38-45`), not per-record data. No official snippet names a parameter, so `table:MARM` and `table:MBEW` (derived from the sweep's `VALUATIONDATA` string) were dropped from the xrefs. `F1602` (Manage Product Master), named in two official snippets, is absent from `data/fiori/apps.ts`. `BAPI_MATERIAL_SAVEREPLICA`, named officially in both the PP/DS page and the Data Migration page, is not an id in the universe, so it is neither an xref nor a successor. The Data Migration record loio b7c452a7d4494ffcb0c5d7c432a708cc returns a title ("FI - Accounts payable open item (tax line) (Customer-specific) (AFS)") that does not match its own snippet content — an index defect in the Help search service, not a repository conflict.
- **Audit-vs-measurement conflict, resolved in the writer's favour and recorded here:** the verdict for `fm:BAPI_MATERIAL_SAVEDATA` called the sentence `'Note SAP recommends that you use Product Master (A2X) API for the following entities: …'` a misattribution, claiming it belongs to loio a8661dda13ef407abc16902e4da68361 and not to loio 74aa2b58a333a107e10000000a441470. Measured on 2026-09-21 with `scripts/sap-help-search.mjs "Product Master A2X Sales Text Basic Text Purchase Text Plant Text all other entities Product Version 2" --size 8 --json`: the sentence is returned verbatim in the snippets of **both** records at versionId 2025.001. The auditor's structural fix (two separate evidence entries, each claim bounded by its own snippet) was kept because it is strictly more precise, but `notes` item (1) was rewritten to state the measured fact instead of the false exclusivity claim.

### catalog-integrity finding (2026-09-21, measured during the batch-3 inventory)

`data/function-intel.ts` carries five keys that are not ABAP function modules, and the
coverage report counts every one of them as a row of the `functions` catalog:

| key | what it actually is | already covered elsewhere |
|---|---|---|
| `MATMAS` | IDoc message type | yes, `idoc:msg:MATMAS` in `data/verification/idocs.ts` |
| `LOIPRO` | IDoc message type | yes, `idoc:msg:LOIPRO` in the same file |
| `BOMMAT` | IDoc message type (marked `inferred: true`) | no |
| `Control Recipe` | a PP-PI process concept, not a callable object (`inferred: true`) | no |
| `PPCC1` | process-message concept (`inferred: true`) | no |

Consequences, all measured: the functions catalog total (145) is inflated by five;
MATMAS and LOIPRO are double-counted across two catalogs under two different id kinds;
and `Control Recipe` cannot receive a canonical `fm:` id at all, because the id syntax
rule in `lib/evidence/validate.ts` rejects a space.

Not fixed inside an enrichment batch. Removing or re-homing rows in `function-intel.ts`
changes what the cockpit renders on the function pages, so it belongs in its own audited
change with its own before/after route check, not as a side effect of writing overlay
records.

---

# Functions batch 4 (2026-09-21): 8 drafts audited, 8 written, 0 refuted

Written by the single writer from the adversarial verdicts. `verdict.fixedRecord` taken where it was complete (7 records: `fm:BAPI_PROCORD_GET_DETAIL`, `fm:BAPI_PROCORD_GET_LIST`, `fm:BAPI_PROCORDCONF_CREATE_TT`, `fm:BAPI_PROCORDCONF_GETLIST`, `fm:BAPI_MEASUREMENTDOCUM_CREATE`, `fm:BAPI_MEASUREMENTPOINT_CREATE`, `fm:BAPI_BATCH_GET_DETAIL`). `fm:BAPI_BATCH_CREATE` was re-derived: its `fixedRecord` carried placeholders ("UNCHANGED FROM DRAFT", "AS DRAFT with three edits") instead of text, so the researcher draft was taken and each listed downgrade applied by hand.

Six records carry `released_api_available` on a documented OData alternative; none claims a successor and none carries a SAP Note or KBA number. Two (`fm:BAPI_MEASUREMENTDOCUM_CREATE`, `fm:BAPI_MEASUREMENTPOINT_CREATE`) carry `verification_required` with `release: null` / `source: null`, because the repository contradicts itself about whether the object exists at all. Measured consequence of that honesty: both drop from depth L3 to L2 and leave the `verified` column, because an authored `verification_required` is below the L3 threshold and `conflicting_sources` is not a verified tier. That is the correct reading, not a regression to fix.

Catalog coverage, measured with `npm run report:coverage -- --catalog functions` before and after:

| | total | L2 | L3 | L4 | L5 | verified | verif.req | conflict | s4-appl |
|---|---|---|---|---|---|---|---|---|---|
| before | 145 | 50 | 79 | 1 | 13 | 114 | 31 | 0 | 77 |
| after | 145 | 52 | 71 | 1 | 19 | 112 | 31 | 2 | 77 |

The `functions` catalog was already graduated out of `FOUNDATION_RECORDS` in `test/evidence-schema.test.ts` (batch 1, 2026-09-02) and is already covered by the graduated `repoRef` test; no guard change was needed in this batch.

## Writer-level normalisations applied

- The closing "this record carries no `reviewer` field" sentence was dropped from every `notes` (it is schema narration, not an SAP caveat; the convention is stated in the file header). `fm:BAPI_PROCORD_GET_LIST` notes item (14), `fm:BAPI_PROCORDCONF_CREATE_TT` item (9) and `fm:BAPI_BATCH_GET_DETAIL` item (11) were whole numbered items; the last was removed and item (12) renumbered to (11). No record carries a `reviewer` field.
- `accessedAt` and `lastVerifiedAt` are emitted as the file constant `DATE21` (`data/verification/functions.ts:29`), not as the literal `"2026-09-21"`.
- Four shared `Evidence` consts hoisted to file level so a status source and its evidence entry cannot drift, matching the `ALM_V2_OPS` / `EQUI_OPS_WRITE` precedent: `PROCORD_OPS_V2`, `PROCONF_TT_OPS`, `BATCH_MASTER_API`, `BATCH_OPS_API`. Where the auditor deliberately wrote a shorter `status.source.claim` than the matching evidence claim (`fm:BAPI_PROCORD_GET_DETAIL`, `fm:BAPI_PROCORDCONF_GETLIST`), the two were left as separate objects.
- Terminology: the overlay file uses `פקודת תהליך` (7 occurrences, 0 of `הזמנת תהליך`), a point the `fm:BAPI_PROCORD_GET_LIST` auditor measured. The same normalisation was applied to `fm:BAPI_PROCORD_GET_DETAIL`, whose `fixedRecord` still said `פרטי הזמנת תהליך` in evidence[2] and `פריטי ההזמנה` in `status.he`. English quotations untouched; the repository's own Hebrew wording is paraphrased, never quoted in the old form.
- Em dashes removed from authored Hebrew prose. `fm:BAPI_PROCORDCONF_GETLIST` evidence[3] carried two (`במשפחת BAPI_PROCORDCONF_ — CREATE_HDR ו-CREATE_TT —`) and now reads with commas. The four em dashes that remain in the batch are all verbatim: the repository label `'SAP Help Portal — verified 2026-07-15'` (`data/bapi-enrichment.sweep.ts:14`, quoted twice) and the SAP deliverable title `Logistics — General` returned by the search service. The four in the new `/** … */` const comments follow the file's existing comment style.
- `fm:BAPI_MEASUREMENTPOINT_CREATE` evidence[0]: the en dash in the unquoted parenthetical `(Document Version 1.0 – 2026-02-25)` became a comma. The file carries zero en dashes elsewhere and the string is not inside quotation marks. The one en dash written in this batch is inside the verbatim English PDF quote `'No influence on the business process – alternatively mentioned transaction codes need to be used'`.

## Writer-level downgrades where two audits disagreed about the same snippet

Three pages in this batch were quoted by two sibling records each, and the search service returns query-dependent snippets. In every case the narrower, explicitly measured reading was written into both records:

- `loio fc8dbf5e46004f1c9069b6ac4301c384` ("Process Order Confirmation"): the `fm:BAPI_PROCORDCONF_CREATE_TT` auditor measured that the trailing words `for process orders` fall outside the retrievable snippet and trimmed the quotation at `confirmations on order level`. The `fm:BAPI_PROCORDCONF_GETLIST` fixedRecord still carried the longer tail; it was trimmed to match. Both records now quote the page identically.
- `loio 3d366e68d53345b4bad055ec8fe85d6e` ("Operations for Batch API"): the `fm:BAPI_BATCH_CREATE` auditor hit that loio with six queries and found the Retrieve row always truncates at `BatchIdentifyingPlant`; the full key tuple `(Material='MYMATERIAL',BatchIdentifyingPlant='',Batch='MYBATCH001')` belongs to the `BatchCharcValue` row on the same page and to the separate "Change Batch" page (`loio 318d52d13b234c9096bfaf30a926fa78`). `fm:BAPI_BATCH_GET_DETAIL` quoted the full tuple `Batch(Material='TG21',BatchIdentifyingPlant='',Batch='0000000003')` on the Retrieve row; it was truncated to `Batch(Material='TG21',BatchIdentifyingPlant` and a disclosure sentence added to both records. Conversely, `fm:BAPI_BATCH_CREATE`'s fixedRecord had silently dropped the single quotes around `'TG21'`; the verbatim form confirmed by the sibling audit was restored.
- `loio b9cc83277e5645d780aba27800777163` (EWM synchronous goods movements): the two audits measured two different snippet renderings of the same page, one showing the fragments in reverse reading order, one running further into the list. Each record keeps its own measured rendering and says in its claim that the snippet is fragmentary; neither reconstructs presumed document order.

Not done, and why: the `fm:BAPI_BATCH_CREATE` auditor offered an optional fifth evidence (`'Create Batch'`, `loio 808cf9d2153d4aefb2fc617c1af5c270`, 2025.001) carrying the key-tuple sentence properly. The `fixedRecord` chose the deletion path and carries four evidences, so four were written. Adding that page would strengthen the key-tuple claim and is worth a follow-up.

`fm:BAPI_BATCH_GET_DETAIL` notes item (11) was re-worded: the draft's isolated coverage arithmetic ("שורת functions עוברת מ-L3=79 ו-L5=13 ל-L3=78 ו-L5=14") describes a single-record run that cannot hold once eight records land in one commit. The per-record statement (derived `unchanged` at repository tier / depth L3 before, `released_api_available` at official tier / depth L5 after) was kept and independently measured through `fromFuncRegistry` + `evidenceBlock`; the catalog-line arithmetic was dropped.

## refuted

- (none in this batch — all 8 audited drafts survived verification and were written with their auditors' downgrades applied.)

## conflicts

- `fm:BAPI_PROCORD_GET_DETAIL` — (1) `lib/bapi-registry.ts` `enrichAll` spreads PM, then PP-PI, then the sweep, so for this id the sweep record replaces the PP-PI record whole: COR3, AFKO/AFPO/AFVC, BOR object BUS2116 and the summary `IMP NUMBER · EXP ORDER_OBJECTS · TAB RETURN` never reach the registry. Same structural failure already queued for `fm:BAPI_ALM_ORDER_GET_DETAIL`. (2) Consequence measured on the merged object: it carries task-list and master-recipe tables and transactions instead of process-order ones (PLAS, PLFL, PLMZ, MAPL, FHMI, PLZU, TC60, TCA01, AFPO, AFVC, AFFH, AFFL; C202, C203, CS08, CA01, CA02, CA03, C201, CFV1, CFV2, CFV3, C298, CC01), because `data/sapData.pppi.ts` uses the pair BAPI_PROCORD_CREATE + BAPI_PROCORD_GET_DETAIL as the default function column on task-list and PRT topic rows. The cockpit BAPI page therefore links routing transactions rather than COR3. (3) ORDER_OBJECTS is an import parameter in one repository file and an export in another; undecided, needs SE37 or BAPI Explorer. (4) `data/function-intel.ts` still names the alternative `API_PROCESSORDER_2` instead of `API_PROCESS_ORDER_2_SRV` — queued in batch 1 under `fm:BAPI_PROCORD_CREATE`, still not fixed.
- `fm:BAPI_PROCORD_GET_LIST` — (1) three different parameter summaries for one object (`IMP PLANT, SELPROD… · TAB PROCESS_ORDERS, RETURN` in pppi, `IN: selection ranges (order/plant/material) · OUT: order list, RETURN.` in the sweep, `Selection → ORDER_OBJECTS` in function-intel); the sweep wins the merge and the PP-PI detail (BUS2116 / COOISPI / AFKO / AUFK) never reaches the registry. (2) Spelling: `data/function-intel.ts` `why` says `שם תקני: GETLIST` while the same line's `name` field and `lib/route-manifest.generated.ts` both carry BAPI_PROCORD_GET_LIST. One prose aside against two structured fields; undecided against a live system, so the manifest spelling was kept. (3) The sweep declares `verificationSource` = SAP Help Portal although no public Help page names the BAPI; the PP-PI source is the Tier-3 SE37 mirror, so the Released state is repository-tier only.
- `fm:BAPI_PROCORDCONF_CREATE_TT` — (1) the sweep overwrites the PP-PI description, `parameterSummary` and verification source, so the app shows `IN: TIMETICKETS (ORDERID, PHASE, YIELD, SCRAP, activities) · OUT: RETURN, CONFIRMATIONS` while pppi says `IMP POST_WRONG_ENTRIES · TAB TIMETICKETS, GOODSMOVEMENTS, LINK_CONF_GOODSMOV, DETAIL_RETURN, RETURN` and function-intel names OPERATION and DETAIL_RETURN. (2) `verificationStatus` / `s4OnPremSupport` / `releasedStatus` / `stability` on that record are `def()` and `verified()` template defaults, not per-record data. (3) Named by the official snippets but absent from the id universe, so not xrefs: `fm:BAPI_PROCORDCONF_CREATE_HDR`, `table:AFFW`, `enh:badi:WORKORDER_CONFIRM_UPDATE`, `table:MATDOC`.
- `fm:BAPI_PROCORDCONF_GETLIST` — (1) `data/bapi-enrichment.pppi.ts:73` assigns the transaction `COConf`, which is not a transaction in the dataset universe and appears in no official record; the officially named process-order confirmation transactions are COR6, COR6N, CORK, CORS and CORT ("Documentary Batches in Production", `loio 36ffb753128eb44ce10000000a174cb4`, 2025.001). (2) `data/fiori/apps.ts#F3364` records `odata: "API_PROCORDCONF"` while the official technical name is API_PROC_ORDER_CONFIRMATION_2_SRV. (3) The two enrichment files declare two different verification sources for the same object (SE37 mirror vs SAP Help Portal). (4) Name-confusion hazard: the exact-name query's top hit ("Troubleshooting in PEO-ERP Integration", `loio a3efc4219e234f38a98ce23064bdbdf9`) names BAPI_PRODORDCONF_GETLIST, a different object (production order). Not recorded as an alias. The alias `BAPI_PROCORDCONF_GET_LIST` that the record does carry is a misspelling-catcher only: it is in no official record, in no repository file and not in the universe. (5) `I_PROCESSORDERCONFIRMATIONTP` is named officially ("Objects Changed for Developer Extensibility in Production Operations", `loio 6af9237e91bb4682bfa8363bdd7a26de`, 2025.001) but is a business-object interface, not a VDM view, and is not in the universe, so no `cds:` xref was written.
- `fm:BAPI_MEASUREMENTDOCUM_CREATE` — the repository contradicts itself about whether the name exists at all: `data/bapi-enrichment.pm.ts` marks it `invalid-name` at confidence `high` ("אומת: אינו אובייקט SAP סטנדרטי (לא נמצא ב-SE37)") while `data/function-intel.ts` carries a full parameter record and `data/sapData.pm.ts#IMRG` lists it among the table's functions, both claiming S/4HANA availability. Written as `conflictingEvidence`, so the page moves from "לא רלוונטי / מאומת מול נתוני הפרויקט" to "נדרש אימות נוסף / מקורות סותרים". `BASELINE.md` records the same pattern across a whole class: 18 ids marked `invalid-name`, 13 of them with a non-inferred function-intel record claiming S/4 availability. Needs SE37. `fm:BAPI_MEASUREMENTDOCUM_CREATEM` carries the identical contradiction and is owed the same treatment.
- `fm:BAPI_MEASUREMENTPOINT_CREATE` — same class of contradiction (`data/bapi-enrichment.pm.ts` `invalid-name` plus the redirect "השתמש ב-BAPI_MPID_CREATE (או FM MP_RFC_SINGLE_CREATE)", against function-intel's POINT / MEASUREMENT_POINT record and `data/sapData.pm.ts#IMPTT` `funcs`). Additionally undecided from the official source: the migration page's snippet shows the column header as `Navigation Function Module`, so it cannot be settled from the snippet whether MP_RFC_SINGLE_CREATE performs the create or only the navigation; sibling migration pages carry the dedicated `CNV_PE_S4_*` module in the same position. MP_RFC_SINGLE_CREATE and Fiori app id W0030 are not in the universe, so neither an xref nor a successor was written, and no alias was added between BAPI_MEASUREMENTPOINT_CREATE and BAPI_MPID_CREATE because their identity is unverified.
- `fm:BAPI_BATCH_CREATE` — (1) classification split inside the repository: `data/bapi-enrichment.pppi.ts` says "יצירת אצווה (סיווג נעשה בנפרד)", the sweep says "כולל סיווג (Class Type 023)" and `data/function-intel.ts` `what` says "כולל סיווג מאפיינים", so the repository reads 2 to 1 for classification inside the call. Nothing officially verified, so the record claims no parameters and no classification-table xrefs. (2) `parameterSummary` differs between pppi (`IMP MATERIAL, PLANT, BATCH, BATCHATTRIBUTES · TAB RETURN · EXP BATCH`, `data/bapi-enrichment.pppi.ts:92`) and the sweep (`IN: MATERIAL, PLANT, BATCH / BATCHATTRIBUTES · OUT: BATCH, RETURN.`). (3) pppi names the business object `BUS1001_BATCH` while the official "Reference Objects | Production Planning and Control" page (`loio 62d3b65334e6b54ce10000000a174cb4`, 2025.001) lists `BUS1001 Material BUS1001002 Batch`. (4) Not in the universe, so not xrefs: `table:MCHB`, `table:AUSP`, `table:INOB`, `fm:BAPI_BATCH_CHANGE`, `tx:COB1`.
- `fm:BAPI_BATCH_GET_DETAIL` — (1) the sweep attributes "תכונות, סיווג, תוקף" to the BAPI while its own `parameterSummary` lists only BATCHATTRIBUTES and RETURN. (2) `data/function-intel.ts` writes "זמין ב-S/4HANA" with no OData alternative, although the sibling BAPI_MATERIAL_GET_DETAIL record in the same file does name API_PRODUCT. (3) Function group VBWB appears only in `data/bapi-enrichment.pppi.ts` and in no official source; its provenance is the Tier-3 SE37 metadata mirror (`sapdatasheet.org`), which is what the `Released · RFC` and `s4OnPremSupport: yes` values ultimately rest on (both are template defaults besides). (4) `fiori:F1576` against the officially named F2462 for "Manage Batches", already documented in `data/verification/fiori.ts`.
- Cross-record, method — the help.sap.com search service returns query-dependent snippets and result counts that are not stable between runs. Every quotation and every count in this batch is bounded to the runs performed on 2026-09-21 and is written as such. Where a count could not be reproduced (`fm:BAPI_PROCORDCONF_CREATE_TT`, `fm:BAPI_MEASUREMENTPOINT_CREATE`, `fm:BAPI_BATCH_GET_DETAIL`), the enumeration was replaced with a run-bounded statement rather than a closed list. HTTP 200 from help.sap.com is not evidence that a loio exists: the page is a JavaScript shell that answers 200 for any loio-shaped path, and the records say so where a status code was measured.

### Resolved 2026-09-21 (design audit round 2 · audit/ux-2026-09/SAP-FIXES.md)
- catalog-integrity finding (five non-FM keys): applied. `FunctionIntel.kind` added; MATMAS/LOIPRO/BOMMAT = `idoc`, Control Recipe/PPCC1 = `concept`. BOMMAT joined `IDOC_RE` (official: message type BOMMAT, basic types BOMMAT01/03/07) and has record `idoc:msg:BOMMAT`; the two concepts keep their pages, are labelled, and are not counted. functions catalog 145 → 142, idocs 2 → 3.
- `fm:BAPI_PROCORD_GET_DETAIL` (1)(2): applied. `lib/bapi-registry.ts` merges the enrichment families per field (`mergePatch`); the BAPI page separates the curated lists from the blueprint associations. Same fix covers `fm:BAPI_ALM_ORDER_GET_DETAIL`.
- `fm:BAPI_PROCORDCONF_GETLIST` (1)(2): applied. `COConf` → COR6/COR6N/CORK/CORS/CORT; F3364 odata → `API_PROC_ORDER_CONFIRMATION_2_SRV`.
- `fm:BAPI_BATCH_CREATE` (3): applied. `BUS1001_BATCH` → `BUS1001002`.
- `fm:BAPI_PROCORD_CREATE` (1): applied repo-wide. `API_PROCESSORDER_2` → `API_PROCESS_ORDER_2_SRV` (16 sites in 11 content files).
- PPCC1: no official record names it (Help search 2026-09-21); enrichment record downgraded from "a transaction code, not an FM" to `requires-verification`.

# Functions batch 5 (access date 2026-09-21, written 2026-09-22): 4 drafts audited, 4 written, 0 refuted

Written by the single writer from the adversarial verdicts. `verdict.fixedRecord` taken verbatim where present (2 records: `fm:BAPI_ALM_NOTIF_SAVE`, `fm:BAPI_ALM_NOTIF_TASK_ADD`). `fm:BAPI_ALM_NOTIF_LIST_FILTER` = researcher draft + its two downgrades (notes item (1) exact-name count sentence made run-bounded across the 2026-09-21 and 2026-09-22 runs, 4 to 5 On-Premise / 15 to 18 SAP ERP; the editorial bracket in `notific[ation]` removed because the loio e994a057 snippet carries the full word). `fm:BAPI_ALM_ORDERHEAD_GET_LIST` = researcher draft + its eight downgrades (Gateway page attribution reduced to what the static HTML carries: title 'Ranges Table - SAP Documentation', 'Mapping the Query Operation' and 'SAP NetWeaver Gateway Generators Cookbook' as More Information links only, parent guide name not in the HTML; WN_OP2025_FPS01_EN.pdf page numbers corrected 36 to 37, 42 to 43, 98 to 99; the SIMPL_OP2025.pdf evidence retyped `simplification_item` like its 39 sibling uses; PDF provenance sentence appended to notes item (2)). Two records carry `released_api_available` on a documented OData alternative and claim no successor; two stay `verification_required` at `conflicting_sources` because the repository contradicts itself about whether the name exists at all (the `fm:BAPI_MEASUREMENTPOINT_CREATE` / `fm:BAPI_MEASUREMENTDOCUM_CREATE` treatment). One KBA number in the batch: 3379615 on `fm:BAPI_ALM_NOTIF_SAVE`, read from the public userapps.support.sap.com preview (title, Symptom, Environment, Product, Keywords) and cited by its me.sap.com/notes url; Cause and Resolution were not read.

Coverage (`npm run report:coverage -- --catalog functions`), before then after: total 142 / 142; L2 52 / 54; L3 68 / 64; L5 19 / 21; verified 109 / 107; verification_required 31 / 31; conflicting 2 / 4; s4-applicable 77 / 77. The two released records moved from derived `unchanged` at repository tier (L3) to `released_api_available` at official tier (L5); the two contested names moved from derived `not_applicable` at repository tier (L3) to `verification_required` at `conflicting_sources` (L2), which is the honest reading of a self-contradicting repository.

## Writer-level normalisations applied
- `reviewer` dropped on `fm:BAPI_ALM_NOTIF_LIST_FILTER` (draft carried "NEO researcher (functions batch 5)"; no overlay record carries the field; both fixedRecords already omit it and say so in their notes).
- Two shared `Evidence` consts hoisted to file level so a status source and its evidence entry cannot drift (`MAINTNOTIF_OPS_CRUD` for SAVE, `ALM_ORDER_READ_ALL_V2` for ORDERHEAD_GET_LIST), matching the `ALM_V2_OPS` / `PROCORD_OPS_V2` precedent. The page-37 correction was therefore applied once, to the const.
- `fm:BAPI_ALM_ORDERHEAD_GET_LIST` evidence[0].claim lead-in: "עמוד ה-SAP Library של SAP NetWeaver Gateway" became "עמוד התיעוד של SAP NetWeaver Gateway". The verdict's problem (1) measured that the string 'SAP Library' does not occur on the static page; its literal downgrade replaced only the parenthetical and would have left the same assertion standing in the lead-in. Re-derived from the verdict text, not added.
- Optional downgrade on `fm:BAPI_ALM_NOTIF_TASK_ADD` (split the mixed On-Premise / SAP_ERP negative-search evidence into two items) not taken: the file's precedent (`fm:BAPI_EQUI_GETDETAIL`, `fm:BAPI_FUNCLOC_GETDETAIL`, `fm:BAPI_BATCH_GET_DETAIL`) keeps one search-bounded item with product "SAP S/4HANA / SAP ERP ..." and edition on-premise.
- Em/en dashes: 0 added in authored Hebrew; the file's counts are unchanged from before the batch (86 em, all inside verbatim SAP titles, repository labels and pre-existing comments; 1 en, inside a verbatim PDF quote). The two new `/** */` const comments use a colon instead of the older comments' em dash.
- `test/evidence-schema.test.ts`: no change. The functions catalog graduated on 2026-09-02; it is already in the graduated repoRef test and not in `FOUNDATION_RECORDS`.
- Gates run after the write: `tsc --noEmit` 0, `tsc --noEmit -p tsconfig.test.json` 0, `npm test` 201 pass / 0 fail, coverage as above.

## refuted
- (none in this batch: all 4 audited drafts survived verification and were written with their auditors' downgrades applied.)

## conflicts
- `fm:BAPI_ALM_NOTIF_LIST_FILTER`: the repository contradicts itself on whether the name exists. `data/bapi-enrichment.pm.ts` marks it `invalid-name` at confidence high ("אין וריאנט FILTER גנרי") and proposes LIST_EQUI / LIST_FUNCLOC / LIST_PARTNER / LIST_PLANGROUP / LIST_SORTFIELD, none of which is in the universe or in any Help record; `data/function-intel.ts` carries a full non-inferred record (Filter input, NOTIFICATIONS output, IW28/IW29, QMEL, "זמין ב-S/4HANA"), and `data/sapData.pm.ts#PM:QMEL` topic 12 funcs, `data/transactions.ts#IW28` and `data/domain-detail.ts#pm-notifications` repeat the name. Needs SE37 / BAPI Explorer on BUS2038. Secondary: function-intel writes the Fiori app name in the plural ("Find Maintenance Notifications"); the official 2025.001 title is singular, 'Find Maintenance Notification (F2071)'; the plural exists officially only for the Defense & Security variant F4633. F2071 is absent from `data/fiori/apps.ts` (no xref). KBA 1923267 appears only in the enrichment VSRC string, unverified, not carried. IWON_NOTIFICATION (named in an EHP8 What's New snippet) is not in `data/exits.ts` or `data/enhancements.ts`.
- `fm:BAPI_ALM_NOTIF_SAVE`: `data/bapi-enrichment.pm.ts` cites "SAP KBA 1923267" for the save/commit contract without a URL; not verified, not carried in a kba field. Released status, function group IWOPM, the TOGETHER_WITH_ORDER parameter and BOR object BUS2038 are repository-only (verified-docs 2026-07-14, no URL) and were not checked live. KBA 2541226 (Phase/Status inconsistency; public Symptom on PUTINPROGRESS, keywords include SAVE) was seen and not cited because its body was not read. Public Cloud availability unverified (0 Help records under SAP_S4HANA_CLOUD; cloudSupport unknown in the repository). How NOTIFHEADER returns the final number rests on the repository and on the KBA Symptom only.
- `fm:BAPI_ALM_NOTIF_TASK_ADD`: same invalid-name class. `data/bapi-enrichment.pm.ts` marks it `invalid-name`, redirects to BAPI_ALM_NOTIF_DATA_ADD + NOTIFTASK and names BAPI_ALM_NOTIF_TASK_COMPLETE / BAPI_ALM_NOTIF_TASK_RELEASE (neither in the universe); `data/function-intel.ts` (NUMBER / NOTIFTASK / RETURN, IW22, QMSM, PM-6, non-inferred), `data/sapData.pm.ts#PM:QMSM` funcs and `data/verification/tables.ts#table:QMSM` present it as an existing object. Neither name appears in any official record in either scope. Whether API_MAINTNOTIFICATION exposes a notification-task entity is undecided: the 2021 FPS01 What's New record (loio 33364ab2) is about task lists, not notification tasks, and no body was read. I_MaintNotificationTaskData (official, Released, 2023.latest) and F3359 are not in the universe. `table:QMSM` and the PM blueprint should be realigned once SE37 decides.
- `fm:BAPI_ALM_ORDERHEAD_GET_LIST`: (1) three parameter summaries for one object (sweep 'IN: selection ranges (plant/status/dates) · OUT: order-header list, RETURN.'; pm 'IMP selection · TAB ET_HEADER, RETURN'; function-intel 'Selection ranges' to ET_HEADER); only a generic ranges table (ItRanges in Gateway spelling, FIELD_NAME / SIGN / OPTION / LOW_VALUE / HIGH_VALUE, OPTIONS_FOR_ENTER_DATE as the documented example) is anchored officially, ET_HEADER is repository-only. (2) `data/bapi-enrichment.sweep.ts:93` declares verificationSource 'SAP Help Portal' (2026-07-15) but no public Help page for an S/4HANA release names the BAPI; the PM family's source is the Tier-3 SE37 mirror, so Released and s4OnPremSupport yes are repository-tier. (3) BOR object: the pm file says BUS2007, the Gateway page names the object 'MaintenanceOrderBAPI'; no read source links the two names. (4) The only official mention is a Business Suite era NetWeaver Gateway page (saphelp_ssb, static HTML, fully read); its parent guide name is not in the HTML and the version string 1.0 comes from the URL path. (5) F2175 / F5241 absent from `data/fiori/apps.ts`; F2731 contested in fiori.ts; none written as xref. (6) The WN_OP2025_FPS01_EN.pdf and CONV_OP2025.pdf download URLs were not recorded in this pass; both served negative text-bounded checks only (the What's New URL is on record in `fm:BAPI_MEASUREMENTPOINT_CREATE`).
- Cross-record, method: as in batch 4, help.sap.com search snippets and counts are query- and run-dependent, so every count in this batch is a day-bounded measurement (LIST_FILTER, SAVE and TASK_ADD carry the auditors' replay values beside the researchers' values). HTTP 200 from help.sap.com/docs is a JavaScript shell, not proof that a loio exists. The sc4sap MCP failed to connect in every session of this batch (researchers, auditors, writer), so no SE37 check confirms or contradicts any name; the four SE37 questions above stay open.

# Functions batch 6 (2026-09-22): 4 drafts audited, 4 written, 0 refuted

Written by the single writer from the adversarial verdicts. `verdict.fixedRecord` taken verbatim where present (2 records: `fm:BAPI_BUPA_CREATE_FROM_DATA`, `fm:BAPI_GOODSMVT_GETDETAIL`). `fm:BAPI_CENTRAL_CHARACT_CREATE` = researcher draft unchanged (verdict: 0 problems, 0 downgrades). `fm:BAPI_EQMT_INSTALL` = researcher draft + its three downgrades (the EQUZ/ILOAN functional-check sentence in recommendedAction rewritten so the only official wording is the quoted 'creates a new entry in the equipment usage list' and the EQUZ/ILOAN reading is attributed to the project's EQUZ and ILOA records; the two repository labels that carry a dash in the source ('SAP Help Portal verified 2026-07-15' on the sweep, 'SAP Help Portal, Plant Maintenance (S/4HANA)' on the academy lesson) no longer rendered inside quotation marks; the EHP5 Release Notes sourceTitle shortened to the file's PDF-title convention). Two records carry `released_api_available` on a documented OData alternative and claim no successor (Business Partner (A2X) / API_BUSINESS_PARTNER for BUPA; Read Material Documents / API_MATERIAL_DOCUMENT_SRV for GOODSMVT_GETDETAIL); two stay `verification_required` at `conflicting_sources` because the repository contradicts itself about whether the name exists at all (the `fm:BAPI_MEASUREMENTPOINT_CREATE` treatment). One SAP Note number in the batch: 2265093 on `fm:BAPI_BUPA_CREATE_FROM_DATA`, read from the Simplification List text (item 5.1.27, Business Impact Note 0002265093) and from both blueprints' BUT000 rows, carried only on the repository claim with a repoRef; the note body was not read. No KBA number carried (2878950 seen in the 'PM - Equipment' migration-object snippet, not cited).

Coverage (`npm run report:coverage -- --catalog functions`), before then after: total 142 / 142; L1 2 / 2; L2 54 / 56; L3 64 / 60; L4 1 / 3; L5 21 / 21; verified 107 / 105; verification_required 31 / 31; conflicting 4 / 6; legacy 0 / 0; s4-applicable 77 / 77; edition-specific 0 / 1. The four ids left derived L3 (BUPA and GOODSMVT_GETDETAIL from derived `unchanged` at repository tier; CENTRAL_CHARACT_CREATE and EQMT_INSTALL from derived `not_applicable` at repository tier). The two contested names now sit at L2 `conflicting_sources`. The two released records sit at L4 on the day of writing, not L5, for one measured reason: `lib/evidence/depth.ts` `fresh()` compares `lastVerifiedAt` with `new Date().toISOString()` (UTC), and at write time (2026-09-22 01:48 +03:00) the UTC date was still 2026-09-21, so `lastVerifiedAt: "2026-09-22"` read as one day in the future (`fresh("2026-09-22", "2026-09-21") === false`, `fresh("2026-09-21", ...) === true`). Every other L5 input is satisfied (official URL, release 2025.001, no successor needed, all xrefs resolve); both read L5 from 2026-09-22 00:00 UTC on. The access date was kept at 2026-09-22 as instructed rather than back-dated. The edition-specific count of 1 is `fm:BAPI_GOODSMVT_GETDETAIL`: its first evidence is the Public Edition 2608 page (`edition: public-cloud`), and report-coverage flags a row whenever any source carries a cloud edition, even though the authored status is on-premise.

## Writer-level normalisations applied
- `reviewer`: none of the four records carries the field. The GOODSMVT_GETDETAIL draft carried "NEO researcher draft, functions batch 6 (the writer drops this field by house convention)"; its fixedRecord already omits it. BUPA notes (12) states the convention.
- Two shared `Evidence` consts hoisted to file level so a status source and its evidence entry cannot drift, matching the `GOODSMVT_ODATA_API` / `ALM_ORDER_READ_ALL_V2` precedent: `BP_A2X_API` (BUPA evidence[0] and status.source) and `MATDOC_READ_API` (GOODSMVT_GETDETAIL evidence[1] and status.source). A new `DATE22 = "2026-09-22"` constant carries every accessedAt / lastVerifiedAt of the batch.
- `fm:BAPI_EQMT_INSTALL` notes (6): the auditor's downgrade quoted 'creates a new entry in the equipment usage list' as official text without naming the page. The writer re-ran `scripts/sap-help-search.mjs` on 2026-09-22 (query: the sentence itself, product SAP_S4HANA_ON-PREMISE) and found it verbatim in the snippets of 'Installing/Dismantling from the Equipment Master Record' (loio 0d79bb53707db44ce10000000a174cb4, 2025.001) and 'Installing/Dismantling Equipment from the Functional Location Master Record' (loio 0179bb53707db44ce10000000a174cb4, 2025.001), both already listed in notes (6). Notes (6) gained that attribution with the fuller snippet sentence 'When you install a piece of equipment, the system - if it is set up accordingly - creates a new entry in the equipment usage list. This contains the current data for the piece of equipment', including the Customizing condition, and the statement that neither snippet names EQUZ or ILOA. Nothing else in the record was touched.
- `fm:BAPI_EQMT_INSTALL` alias `BAPI_EQMT_INSTALLFL` registered as the draft asked (the academy lesson's spelling), checked against the universe: it resolves to nothing else, no collision.
- Conflict pattern: both contested records use the flat form (one repository evidence at `verificationLevel: "conflicting_sources"`, as `fm:BAPI_MEASUREMENTPOINT_CREATE` and `fm:BAPI_ALM_NOTIF_LIST_FILTER` do), not the `conflictingEvidence` array form (`fm:BAPI_MEASUREMENTDOCUM_CREATE`, `fm:BAPI_ALM_NOTIF_TASK_ADD`). Both forms yield the same tier, but `lib/evidence/resolve.ts` counts `conflicts` from the array only, so the evidence block shows 0 conflicts beside the "מקורות סותרים" pill for these two, as it already does for the two flat precedents. CENTRAL_CHARACT_CREATE's notes (6) uses the word conflictingEvidence in the sense the file header uses it for batch 4 (the contradiction is carried as evidence at the conflicting tier, not only as a note). Open data/UI consistency point for the catalog; not changed here because the audited drafts were written as audited.
- Em/en dashes: 0 added in authored Hebrew. Seven U+2012 figure dashes entered the file inside verbatim quotes of the Public Edition 2608 snippet ('Goods Movement ‒ Create', 'Goods Movement ‒ Read', ...), the character the help.sap.com index emits; the file had 0 before. The em-dash count (86, all inside verbatim SAP titles, repository labels and pre-existing comments) is unchanged.
- `test/evidence-schema.test.ts`: no change. The functions catalog graduated on 2026-09-02; it is already in the graduated repoRef test and not in `FOUNDATION_RECORDS` (only the objects catalog remains there).
- Gates run after the write: `tsc --noEmit` 0, `tsc --noEmit -p tsconfig.test.json` 0, `npm test` 201 pass / 0 fail, `validateRecords` over the four records alone and merged with every overlay = 0 problems, coverage as above. Not committed.

## refuted
- (none in this batch: all 4 audited drafts survived verification and were written with their auditors' downgrades applied.)

## conflicts
- `fm:BAPI_BUPA_CREATE_FROM_DATA`: (1) no official record prints the technical name; the closest official text is the BOR method 'BAPI CreateFromData of the business object Business Partner' (User Exit CACS3000 page, 2025.001) and the BAPI channel listed on 'Editing Business Partner Data'; existence, parameters (PARTNERCATEGORY, CENTRALDATA, ADDRESSDATA, BUSINESSPARTNER, RETURN) and the SE37 release flag are repository-only, and the sweep's verified-docs / yes / Released values are `verified()` defaults with a Tier-3 mirror (sapdatasheet.org) in the source string. (2) `data/function-intel.ts` files the module under PP-PI while the official pages put the business partner under LO-MD-BP; it is cross-module (PM uses it for EBAN.FLIEF suppliers). (3) `data/sapData.pppi.ts` also lists the module on the T134 row, a blueprint misfiling already queued in the tables catalog (batch 9). (4) F3163 / F1053A (migration-object app IDs) are absent from `data/fiori/apps.ts`; I_BusinessPartner (Released, 2023.latest) and DEBMAS / CREMAS are not universe ids, so none is an xref. (5) The exact-quoted query returned 2 empty-snippet records for the researcher and 1 for the auditor on 2026-09-22; the count is recorded as run-bounded. (6) The 'Configuration Guide IS-H' PDF clause was dropped at the auditor's instruction (file not present in the scratchpad); the Policy Management 5.3 and WN_OP2025_FPS01_EN.pdf negative checks stand. Needs SE37 / BAPI Explorer.
- `fm:BAPI_CENTRAL_CHARACT_CREATE`: the repository contradicts itself on whether the name exists and on its business domain. `data/bapi-enrichment.sweep.ts` marks it `invalid-name` at confidence high and redirects to BAPI_CHARACT_CREATE (not a universe id); `data/function-intel.ts` carries a full non-inferred classification-characteristic record (CHARACTDETAIL, CHARVALUESNUM/CHAR, CT04, AUSP, 'זמין ב-ECC' / 'זמין ב-S/4HANA' without source); `data/sapData.pppi.ts` lists it on PLKO and PLMK as a QM master-inspection-characteristic creator. The official 2025.001 documentation names BAPI_CHARACT_CREATE / BAPI_CHARACT_CHANGE (migration object 'Characteristic') and the OData service API_CLFN_CHARACTERISTIC instead; the QM information sheet names BOR object QPMK with SaveReplica, IDoc QPMK01. Neither reading has an official anchor for this name. F2219 absent from `data/fiori/apps.ts`; CABN / CAWN / AUSP / QPMK / CT01 / CT05 not universe ids. Needs SE37 / BAPI Explorer, then a correction of function-intel and the PP-PI blueprint whichever way it falls.
- `fm:BAPI_EQMT_INSTALL`: four layers disagree. `data/bapi-enrichment.pm.ts` marks it `invalid-name` (Tier-3 SE37 mirror + SAP Community) and proposes BAPI_EQUI_INSTALL / BAPI_EQUI_DISMANTLE, neither named by any official record either; `data/function-intel.ts` (EQUIPMENT, FUNCTLOC/SUPEQUI, RETURN, IE4N / IE02, EQUI / ILOA, PM-2, non-inferred), `data/sapData.pm.ts#EQUI` funcs, `data/domain-detail.ts#pm-equipment` and `data/academy/lessons/pm-generated.ts` (third spelling BAPI_EQMT_INSTALLFL, trust verified-docs, source label SAP Help Portal, Plant Maintenance (S/4HANA), no URL; the source string carries a dash the record does not reproduce) present it as existing. Three spellings of the install-target parameter (FUNCTLOC/SUPEQUI, FUNCLOC / SUPEQUI, FUNCLOC / SUPERIOR_EQUIPMENT + INSTALL_DATE), none SE37-checked. The merged registry row measured on 2026-09-22 still carries stability 'Released' and IE01 / IE02 / IE03 / IH08 / IE05 beside the invalid-name flag (derived before the fix, not corrected here). Fiori id drift for Manage Technical Objects: F2079 in the blueprint vs F2730A in `data/fiori/apps.ts`, neither verified; no xref. The 'Function Imports for Equipment' page (DismantleEquipment) surfaces only at 2023.latest. KBA 2878950 seen in the 'PM - Equipment' snippet, not carried. Needs SE37 / BAPI Explorer for all three names, then a correction of all four repository layers.
- `fm:BAPI_GOODSMVT_GETDETAIL`: (1) On-Premise availability is repository-only (sweep defaults verified-docs / yes / Released); the name is printed officially only in the Public Edition 2608 BAPIs/IDocs table and in the SAP ERP 6.0 EHP7 'Material Segmentation Across Logistics (New)' snippet (ECC), and the Public Edition row 'Goods Movement ‒ Read / SAP_COM_0108' is attributed by table order, not by one contiguous fragment. (2) `audit/s4-enrichment/BASELINE.md:56` records tone 'changed' for the page (derived from the MKPF attachment) while fromFuncRegistry derives `unchanged`; the authored status now supersedes both. (3) Auditor corrections carried: the Archiving Material Documents (MM-IM) loio 75bcb653 sits under the Supply Chain deliverable (path 677f0a4e...), not Sourcing and Procurement; the item-key triple attributed to 'Cancel Material Documents at Item Level' was never returned in a snippet and was removed from the evidence and the recommendation (header key kept from the 'Cancel at Header Level' snippet); the MBGMCR IDoc description is truncated in every snippet and is no longer quoted. (4) F1077 (Material Documents Overview) absent from `data/fiori/apps.ts`; BAPI_GOODSMVT_CANCEL, MIGO_DIALOG, MATDOC and I_MaterialDocumentItem_2 are not universe ids (MATDOC reached through `obj:material-document`); I_MaterialDocumentItem is deprecated since 2021 with successor I_MaterialDocumentItem_2, so the CDS hint was not adopted as a successor. Needs SE37 / BAPI Explorer for the parameter list and release flag.
- Cross-record, method: as in batches 4 and 5, help.sap.com search snippets and counts are query- and run-dependent, so every count in this batch is a day-bounded measurement. HTTP 200 from help.sap.com/docs is a JavaScript shell, not proof that a loio exists. The sc4sap MCP failed to connect in every session of this batch (researchers, auditors, writer), so no SE37 check confirms or contradicts any name; the four SE37 questions above stay open.

# Functions batch 7 (2026-09-22): 4 drafts audited, 4 written, 0 refuted

Written by the single writer from the adversarial verdicts. `verdict.fixedRecord` taken verbatim where present (2 records: `fm:ARCHIVE_GET_NEXT_OBJECT`, `fm:BAPI_ALM_NOTIF_DATA_MODIFY`; both checked against the researcher drafts before writing: the three GET_NEXT_OBJECT downgrades (MATDOC line count vs occurrence count, the ARCHIVE_NEW_OBJECT / ARCHIVE_SAVE_OBJECT snippet provenance, the second Develop Read Programs query) and the four DATA_MODIFY downgrades (Orders (CS-SE/PM-WOC-MO) deliverable, the 'Extensibility Changed' summary row instead of 'Type Changed', the truncated 'to check' snippet, the 5 / 18 / 2 re-run counts) are all present). `fm:ARCHIVE_OPEN_FOR_WRITE` = researcher draft + its single downgrade (notes: the PM_QMEL / PP_ORDER archiving-object names used in recommendedAction attributed to 'Dependencies (CS-BD/PM-EQM-SF-MPC)', loio 669fce5314894208e10000000a174cb4, 2025.001). `fm:BAPI_ALM_NOTIF_DATA_DELETE` = researcher draft + its four downgrades (an authored `verification_required` status block with `release: null` / `source: null`, the CLOSE / CHANGEUSRSTAT / TASK_ADD convention, so the evidence block no longer shows the derived 'ללא שינוי ב-S/4HANA' at the official tier; evidence[4]: the exact-name count made run-bounded, 18 to 21 On-Premise, and the 'BAPI_ALM_NOTIF' query 0 to 2 unrelated empty-snippet records, named; notes: the DATA_ADD comparison made run-bounded and the 'why no authored status' sentence reworded to 'why an authored verification_required status'). The ADK pair carries an authored `unchanged` on help.sap.com records that print the module name (ABAP Platform 2025 FPS01 for GET_NEXT_OBJECT; two S/4HANA 2025 FPS01 S_ARCHIVE authorization pages for OPEN_FOR_WRITE) plus fully-read static ADK pages; DATA_MODIFY carries `released_api_available` on the documented PATCH operations of API_MAINTNOTIFICATION and claims no successor; DATA_DELETE stays `verification_required` on purpose: no official record names it, and the OData service documents DELETE for failure effects (2023 FPS02) and task lists (2021 FPS01) only. Two KBA numbers in the batch, both on `fm:BAPI_ALM_NOTIF_DATA_MODIFY`: 1923267 and 1619709, read from their public userapps.support.sap.com previews (title, Symptom, Environment, Product, Keywords) and cited by their me.sap.com/notes urls (HTTP 401 without an S-user, as the MANIFEST predicts); Cause and Resolution were not read. No SAP Note number carried.

Coverage (`npm run report:coverage -- --catalog functions`), before then after: total 142 / 142; L1 2 / 2; L2 56 / 57; L3 60 / 58; L4 1 / 1; L5 23 / 24; verified 105 / 105; verification_required 31 / 31; conflicting 6 / 6; legacy 0 / 0; s4-applicable 77 / 76; edition-specific 1 / 1. Per id (measured through the same builder path as report-coverage, HEAD file vs merged file): DATA_MODIFY L3 to L5 (official URL, release 2025.001, all 24 xrefs resolve in the app, fresh); DATA_DELETE L3 to L2 (an authored verification_required status is 'not decided' by design, exactly as CLOSE / CHANGEUSRSTAT / LIST_FILTER / TASK_ADD; its level is sap_official_verified from the four help.sap.com evidences, so the verified count does not move and s4-applicable drops by one); ARCHIVE_GET_NEXT_OBJECT and ARCHIVE_OPEN_FOR_WRITE stay L3 (level repository_verified to sap_official_verified, derived to authored with a release) because `tx:AOBJ` (and, on OPEN_FOR_WRITE, `tx:DB15`) resolve in the validator's universe (ROUTE_MANIFEST.tcodes) but not in the app (`txHref` returns null: no transaction page exists for them, while SARA and SE37 have one), and L4 requires every xref to resolve in the app. The xrefs were kept as audited: they are the house precedent (`table:ADMI_RUN` carries the same `tx:AOBJ` / `tx:DB15` xrefs) and dropping them would raise the depth score by hiding a real cross-reference, not by adding evidence. Both records read L5 the day the app documents AOBJ and DB15, or an auditor revisits the xrefs.

## Writer-level normalisations applied
- `reviewer`: none of the four records carries the field (house convention); no draft carried it.
- Three shared `Evidence` consts hoisted to file level so a status source and its evidence entry cannot drift (the `GOODSMVT_ODATA_API` / `BP_A2X_API` / `MATDOC_READ_API` precedent): `ARCHIVE_FILES_ADK` (GET_NEXT_OBJECT evidence[0] and status.source), `S_ARCHIVE_AUTH_OBJECTS` (OPEN_FOR_WRITE evidence[0] and status.source), `MAINTNOTIF_OPS_PATCH` (DATA_MODIFY evidence[0] and status.source; a separate const from `MAINTNOTIF_OPS_CRUD` because the claim text differs). Each was asserted byte-identical to its record's evidence[0] before hoisting. `DATE22` carries every accessedAt / lastVerifiedAt.
- `fm:ARCHIVE_OPEN_FOR_WRITE` notes: the auditor's downgrade quoted the loio 669fce53 snippet as 'PM_QMEL Maintenance notifications'; the writer re-ran `scripts/sap-help-search.mjs` on 2026-09-22 and the returned snippet carries a break there ('PM_QMEL Maintenance &hellip; notifications'), so the inserted sentence quotes it with the break marked and adds the contiguous 'Maintenance notifications PM_QMEL Service notifications SM_QMEL Maintenance orders and service orders ... PM_ORDER' fragment from the deliverable overview page 'Data Archiving in Plant Maintenance and Customer Service (PM/CS)' (loio a0cfba538c95b54ce10000000a174cb4, 2025.001, date 2026-02-24), returned by the same run.
- `fm:BAPI_ALM_NOTIF_DATA_DELETE` evidence[4]: the verdict's replacement text carried a doubled apostrophe ('What''s New'), a quoting artefact; written as What's New. The '46 records scanned' clause was qualified as the researcher's run (the auditor's re-run also yielded 0 ALM_NOTIF hits in title or snippet, per the verdict), because the count in the same sentence became a range.
- `fm:BAPI_ALM_NOTIF_DATA_DELETE` notes: the sentence stating that the derived 'ללא שינוי ב-S/4HANA' claim 'remains in place' was qualified: with the authored status, `pickStatus` (`authored ?? derived`, lib/evidence/s4-status.ts) no longer displays it; the derived claim is still what the registry says.
- The verdict's gaps[1] instruction for DATA_DELETE addressed a researcher-facing summary field that is not part of the record; nothing to apply.
- `test/evidence-schema.test.ts`: no change. The functions catalog graduated on 2026-09-02; it is already in the graduated repoRef test and not in `FOUNDATION_RECORDS` (only the objects catalog remains there).
- Em/en dashes: 0 added (0 in the new-record region, lines 7722 to 8521 of the merged file); file-level counts unchanged (87 em, 1 en, 7 figure dashes, all pre-existing inside verbatim titles, labels and comments).
- Gates run after the write: `tsc --noEmit` 0, `tsc --noEmit -p tsconfig.test.json` 0, `npm test` 204 pass / 0 fail, `validateRecords` over the four records alone and merged with every overlay = 0 problems, the emitted TypeScript round-trips to the audited JSON with sorted keys (identical per record), no duplicate id, coverage as above. Not committed.

## refuted
- (none in this batch: all 4 audited drafts survived verification and were written with their auditors' downgrades applied.)

## conflicts
- `fm:ARCHIVE_GET_NEXT_OBJECT`: (1) the sweep's verified-docs / 'Released' / function group ARCH values come from a Tier-3 SE37 mirror (sapdatasheet.org / se80.co.uk), so the derived 'unchanged at repository tier' the screen showed until now rested on mirrors; the authored status rests on ABAP Platform 2025 FPS01 records. (2) `data/function-intel.ts` files the module under PP-PI while the canonical listing derives it from the PM blueprint (ADMI_RUN); it is cross-application (Basis / ADK). (3) Interface: no read source lists the parameters; only 'The parameter OBJECT_ID must syntactically be CHAR' is official; ARCHIVE_HANDLE and OBJECT_OFFSET are repository-only. (4) The S/4HANA product set indexes the Archive Files topic only at 1709 Latest; 2025.001 and 2023.latest queries returned no record naming the module (search-bounded). Needs SE37.
- `fm:ARCHIVE_OPEN_FOR_WRITE`: (1) signature (OBJECT, ARCHIVE_HANDLE), the RFC attribute and the 'Released' stability label are repository-only (`verified()` defaults in the sweep). (2) The only page describing the module's role is the SAP Library EM 7.0 EHP1 mirror of the NetWeaver ADK topic, labelled edition ecc for lack of a nearer token; the Help search service does not return it under ABAP Platform 2025 FPS01 or NetWeaver 7.5. (3) The PM/CS and PP-PI archiving guides (PM_ORDER, PR_ORDER) never name the module; the link is the repository's. (4) function-intel files it under PP-PI; it is a platform (ADK) module. Needs SE37.
- `fm:BAPI_ALM_NOTIF_DATA_DELETE`: (1) no official record names the BAPI in any scope; existence, release status, function group IWOPM, the BOR-method assignment and the delete tables (NOTITEM, NOTIFCAUS, NOTIFACTV, NOTIFTASK, NOTIFPARTNR) are repository-only. (2) Dead sweep patch (`if (cur)` in lib/bapi-registry.ts registry()): the sweep describes operationType Change / category Execution / scope 'פריט/סיבה/פעולה', the PM addition Delete / Notification / five sub-objects; the app shows the addition. (3) BUS7051 (suggested by the research brief) appears in no record; BUS2038 is the documented notification object, and the method assignment is unverified. (4) NOTITEM here vs NOTIFITEM on DATA_MODIFY, both unverified. (5) IWON_NOTIFICATION is not a universe id (prose only). (6) KBA 1923267 appears only inside the repository source string quoted on the repository claim; it is not carried in a kba field on this record (it is on DATA_MODIFY). Needs SE37 / BAPI Explorer.
- `fm:BAPI_ALM_NOTIF_DATA_MODIFY`: (1) the name is printed only in two KBA previews, never on help.sap.com; 'Released', IWOPM, the parameter tables and the _X structures are repository-only. (2) Tasks coverage in API_MAINTNOTIFICATION undecided (the 2022 What's New speaks of task lists; no PATCH row for tasks in any snippet window). (3) 'Carry Out a Mass Change' (loio 69cab653) is filed under Orders (CS-SE/PM-WOC-MO) by the search record; the draft's Notifications attribution was corrected. (4) Public Cloud unverified (cloudSupport unknown; 0 relevant SAP_S4HANA_CLOUD records). (5) KBA Cause / Resolution unread (S-user). Needs SE37 / BAPI Explorer.
- Cross-record, method: as in batches 4 to 6, help.sap.com search snippets and counts are query- and run-dependent, so every count in this batch is a day-bounded measurement (DATA_DELETE 18 to 21 / 0 to 2, DATA_MODIFY 4 to 5 / 15 to 18). HTTP 200 from help.sap.com/docs is a JavaScript shell, not proof that a loio exists. The sc4sap MCP failed to connect in every session of this batch (researchers, auditors, writer), so no SE37 check confirms or contradicts any name.
- App-side consistency point (not a record defect): `tx:AOBJ` and `tx:DB15` are ROUTE_MANIFEST tcodes without a transaction page, so every record that xrefs them (table:ADMI_RUN, fm:ARCHIVE_GET_NEXT_OBJECT, fm:ARCHIVE_OPEN_FOR_WRITE) passes every validator rule yet is capped at L3 by `depthOf`.

# Functions batch 8 (2026-09-22): 4 drafts audited, 4 written, 0 refuted

Written by the single writer from the adversarial verdicts. `verdict.fixedRecord` taken verbatim where present (2 records: `fm:BAPI_ALM_NOTIF_PUTINPROGRESS`, `fm:BAPI_EQUI_CREATE`; both taken from the auditors' scratchpad copies and checked field by field: every downgrade string present exactly once, every superseded string absent, xrefs 17 / 21 with `fm:BAPI_EQMT_INSTALL` dropped on EQUI_CREATE as instructed). `fm:BAPI_EQUI_INSTALL` = researcher draft + its downgrades applied by exact-once replacement (the Response Parameters label in both sentences of evidence[0].claim, the abridged SRC_HELP label in evidence[3].claim, `reviewer` dropped, status.source re-pointed at the edited evidence[0]); status unchanged (released_api_available, on-premise, 2025.001, no successor). `fm:BAPI_GOODSMVT_GETITEMS` = draft + the two mandatory edits (auditor's copy, verified) + the optional F1077 note, applied only after the writer re-ran `scripts/sap-help-search.mjs` on 2026-09-22 and found 'Material Documents Overview App ID: F1077' verbatim in the snippet of loio 7cc07e548af58e4ce10000000a4450e5 (Inventory Management and Inventory (MM-IM), 2025.001); F1077 is absent from `data/fiori/apps.ts`, so it stays out of xrefs. `fm:BAPI_EQUI_CREATE`, refuted in batch 3, is now written on its re-researched draft.

Coverage (`npm run report:coverage -- --catalog functions`), before then after: total 142 / 142; L1 2 / 2; L2 57 / 57; L3 58 / 54; L4 1 / 1; L5 24 / 28; verified 105 / 105; verification_required 31 / 31; conflicting 6 / 6; legacy 0 / 0; s4-applicable 76 / 76; edition-specific 1 / 2. Per id: the four ids had no overlay record and sat at derived L3 (registry-derived `unchanged` at repository tier, already counted verified); after the write all four measure L5 / sap_official_verified / released_api_available / authored, 0 conflicts (measured through the same builder path as report-coverage). Edition-specific 1 to 2 is GETITEMS: `rowOf` takes the first private/public-cloud evidence row's edition, and its Public Edition 2608 row sets it, as GETDETAIL did in batch 6.

## Writer-level normalisations applied
- `reviewer`: none of the four records carries the field. The EQUI_INSTALL and GETITEMS drafts carried it and it was dropped; the two fixedRecords already omitted it.
- Four shared `Evidence` consts hoisted to file level so a status source and its evidence entry cannot drift (the `GOODSMVT_ODATA_API` / `BP_A2X_API` / `MATDOC_READ_API` precedent): `MAINTNOTIF_SET_IN_PROCESS` (PUTINPROGRESS evidence[0] and status.source), `MAINT_MGMT_APIS_OVERVIEW` (EQUI_CREATE evidence[2] and status.source), `EQUI_INSTALL_FLOC` (EQUI_INSTALL evidence[0] and status.source), `MATDOC_READ_ITEMS` (GETITEMS evidence[1] and status.source). `MATDOC_READ_ITEMS` is a sibling of `MATDOC_READ_API` (same loio 78f5a8461d554cc38b3af2d07d6f9c8e, same DATE22), not an extension of it: extending would have rewritten the audited GETDETAIL claim (which closes on GETDETAIL, not GETITEMS) into a text no auditor saw whole; the GETITEMS note (1) is permissive ('הכותב רשאי'), so the sibling stays within the verdict. `DATE22` carries every accessedAt / lastVerifiedAt.
- Em/en dashes: 1 em dash added, inside the verbatim sweep label ('SAP Help Portal ... verified 2026-07-15', quoted with its dash) in EQUI_CREATE evidence[3].claim, the same precedent three existing records already carry; 8 U+2012 figure dashes added inside verbatim Public Edition 2608 quotes in GETITEMS (evidence[0].claim 7, status.he 1), the character the help.sap.com index emits (the batch-6 GETDETAIL precedent); 0 in authored Hebrew. File level: em 87 to 88, en 1 to 1, figure 7 to 15.
- Header: the batch-8 paragraph was added, and the Tier-1 sentence now also names the three help.sap.com topic pages behind the EQUI_INSTALL record that the researcher rendered in a browser and read in full (a channel the file did not list before; the auditor confirmed the rendered Response Parameters label but did not replay the direct-call query tally or the five PDF reads).
- `test/evidence-schema.test.ts`: no change. The functions catalog graduated on 2026-09-02; it is already in the graduated repoRef test and not in `FOUNDATION_RECORDS` (only the objects catalog remains there).
- Gates run after the write: `tsc --noEmit` 0, `tsc --noEmit -p tsconfig.test.json` 0, `npm test` 204 pass / 0 fail, `eslint data/verification/functions.ts` 0, `validateRecords` over each record alone and merged with every overlay = 0 problems, all 72 xrefs resolve, the emitted TypeScript round-trips to the audited JSON with sorted keys (identical per record; 58 records, 0 duplicate ids; each status.source is the same object as its evidence entry), coverage as above. Not committed. The worktree also carries unrelated uncommitted changes from another session (app/neo/*.css, components/neo-shell/data/tables-detail-view.tsx, scripts/qa/ai-states-check.mjs, audit/ux-2026-09/shots/after-r6/*); untouched.

## refuted
- (none in this batch: all 4 audited drafts survived verification and were written with their auditors' downgrades applied.)

## conflicts
- `fm:BAPI_ALM_NOTIF_PUTINPROGRESS`: (1) no help.sap.com record prints the name in any scope (On-Premise 4 to 5 unrelated hits, SAP_ERP 17, SAP_S4HANA_CLOUD 0, run-dependent); the name is printed only in the public Symptom preview of KBA 2541226, whose Cause and Resolution need an S-user. Released, function group IWOPM, BOR object BUS2038 and the parameter names are repository-only (verified-docs 2026-07-14, no URL), not checked live. (2) Two repository layers carry different parameter lists (PM_ADDITIONS: NUMBER, LANGU, LANGUISO, TESTRUN, SYSTEMSTATUS, USERSTATUS, RETURN; the dead SWEEP patch: NUMBER, RETURN): an internal inconsistency on parameters, not on existence, hence repository_verified rather than conflicting_sources. (3) Whether the OData function import SetMaintNotifToInProcess also maintains QMEL-PHASE (the KBA's BAPI-alone symptom) is stated by no snippet. (4) The Function Import page (7bd31588) and the service overview (f430cbb1) surface under On-Premise only at 2023.latest; under Cloud the overview came back at 2602.500 on re-run while the Function Import page came back at 2608.500. (5) evidence[2] cites the CO deliverable's suffixed loio 2519d553…-1067, the record the search actually returns; the bare loio answers HTTP 200 (shell) but is returned only under FI-GL, Financial Operations, PS and Internal Service Request.
- `fm:BAPI_EQUI_CREATE`: (1) release status, parameter structures (DATA_GENERAL, DATA_SPECIFIC; DATA_INSTALL and EXTERNAL_NUMBER only in the PM record), function group and BOR object (EquipmentPM in the repository; BUS1088 confirmed by no snippet) are repository-only and not checked in SE37; three layers, two parameter summaries. (2) Spelling divergence: the Simplification List prints BAPI_EQUI_GET_DETAIL while the repository and `fm:BAPI_EQUI_GETDETAIL` use GETDETAIL; recorded, not settled. (3) SAP Note 0002270107 (Batch Input for EAM, printed in the Simplification List) and KBA 2878950 (2021.002 migration-page snippet) are text-only mentions: no verified me.sap.com URL, not in repository data, not carried as fields. (4) Fiori: W0028 (printed on the migration page) is not in `data/fiori/apps.ts`; `fiori:F2730A` is the repository id whose contradiction (F2079 blueprint, F1827 tx-intel) sits in research-queue-fiori.md. (5) `fm:BAPI_EQMT_INSTALL` dropped from the xrefs: `data/bapi-enrichment.pm.ts` marks the name non-existent and its record is verification_required; the install relation goes through `fm:BAPI_EQUI_INSTALL`. (6) The 'Create Equipment' operation page (fc818bd5) surfaces only at 2023.latest; the 2025.001 create row is a join of two snippets of the Operations page (d1e3c797) by table order. (7) ECC availability rests on the repository and on the Simplification List (a conversion document); no SAP ERP record prints the name, and the EHP3 Release Notes name only BAPI_EQUI_CHANGE and four BAPI_FUNCLOC_*.
- `fm:BAPI_EQUI_INSTALL`: (1) no official record prints the name in any scope (On-Premise 6 unrelated hits, SAP_ERP 10, SAP_S4HANA_CLOUD 0; five fully-read PDFs and the PM - Equipment migration page name only BAPI_EQUI_CREATE); existence, the Released label and the target-parameter spelling (FUNCLOC / SUPEQUI in PM_ADDITIONS vs FUNCLOC / SUPERIOR_EQUIPMENT, INSTALL_DATE in the dead SWEEP patch) are repository-only, sourced from a Tier-3 SE37 mirror (sapdatasheet.org) plus SAP Community, both outside the allowlist. (2) BAPI_EQUI_DISMANTLE: no official record, not a universe id, not in function-intel; prose only. (3) The release in which InstallEquipment / DismantleEquipment joined API_EQUIPMENT was found in no What's New record (2020 to 2022 scanned). (4) The Necessity / Comment columns of the InstallEquipment request table rendered empty, so property mandatoriness is undetermined. (5) 'Install or Dismantle Equipment' (4373216a) is returned by the search service only at 2023.latest (old title 'Function Imports for Equipment') and under Cloud 2608.500; the 2025.001 evidence rests on the browser-rendered page.
- `fm:BAPI_GOODSMVT_GETITEMS`: (1) On-Premise availability is repository-only (sweep `verified()` defaults: verified-docs, s4OnPremSupport yes, stability Released); the name is printed officially only in the Public Edition 2608 BAPIs/IDocs table (contiguous row, SAP_COM_0108 and SAP_COM_0156) and in the SAP ERP 6.0 EHP7 'Material Segmentation Across Logistics (New)' snippet (ECC; the sentence introducing that list was not returned). Exact-name search on SAP_S4HANA_ON-PREMISE: total 0. (2) Parameter names ('Selection' as a repository description, GOODSMVT_ITEMS, RETURN), the BOR object / method and the no-COMMIT read are repository-only. (3) I_MaterialDocumentItem is deprecated since 2021 in favour of I_MaterialDocumentItem_2 (not a dataset id); no official source presents a CDS view or the OData service as the BAPI's successor, so no successor. (4) F1077 (Material Documents Overview) is printed by the MM-IM guide snippet and re-verified by the writer, but absent from `data/fiori/apps.ts`: notes only, never an xref. (5) Cross-record finding for `fm:BAPI_GOODSMVT_GETDETAIL` (batch 6): today's Read Material Documents snippet prints the full item key example (MaterialDocument='4900021060', MaterialDocumentYear='2017', MaterialDocumentItem='0001') while that record's recommendedAction says the full item key was returned by no snippet; queued as a correction for GETDETAIL, not applied here.
- Cross-record, method: as in batches 4 to 7, help.sap.com search snippets and counts are query- and run-dependent, so every count in this batch is a day-bounded measurement (PUTINPROGRESS 4 to 5 / 17 / 0, EQUI_CREATE 6 to 7, EQUI_INSTALL 6 / 10 / 0, GETITEMS 0 and SAP_ERP 10 with 9 snippet-less). HTTP 200 from help.sap.com/docs is a JavaScript shell, not proof that a loio exists. The sc4sap MCP failed to connect in every session of this batch (researchers, auditors, writer: 'MCP error -32000: Connection closed' at session start), so no SE37 check confirms or contradicts any parameter, function group or release status.

# Functions batch 9 (2026-09-22): 4 drafts audited, 4 written, 0 refuted

Written by the single writer from the adversarial verdicts, read from the workflow journal (the same draft and verdict objects the orchestrator relayed; each was fingerprinted against the auditors' scratchpad copies before use). `verdict.fixedRecord` taken verbatim where it was complete (1 record: `fm:BAPI_MEASUREMENTDOCUM_CREATEM`; equal, with sorted keys, to the auditor's scratchpad copy and to the researcher draft plus its single notes edit, the per-scope count sentence made run-bounded with the 'Read a Measurement Document' Cloud hit named). `fm:BAPI_MAINTENANCEPLAN_CREATE` = researcher draft + its two edits (evidence[1].claim: the second API_MAINTENANCEPLAN occurrence in WN_OP2021_EN.pdf is row 16 of the 'The following APIs are impacted' table of section 13.19 Currency Code Conversion, Cross Components chapter, not a second hit in section 2.1.6; `reviewer` dropped); the result is equal to the auditor's fixed-BAPI_MAINTENANCEPLAN_CREATE.json. `fm:BAPI_MATERIAL_GET_DETAIL` = researcher draft + the verdict's structured edits (evidence[4].claim: the Support Content table has four columns, BAPI / Description / Comments / Core/Retail, with Background and Dialogue as inner header rows; notes: the exact-name counts replaced by the run-bounded sentence; `reviewer` dropped); the replacement claim the verdict supplied was asserted to be the draft claim with exactly that one column sentence changed. `fm:BAPI_MATERIAL_BOM_GROUP_CREATE` = researcher draft + its eight downgrades applied by exact-once replacement (evidence[0].claim: the overview page's technical name API_BILL_OF_MATERIAL_SRV_0002 and the origin of the '(Version 2)' label appended; status.he: the batch sentence quoted with the overview page's truncated caveat; recommendedAction: '(בעמוד הסקירה: API_BILL_OF_MATERIAL_SRV_0002)' added; evidence[3].claim: the three different blueprint labels MAST / STKO / STPO; notes (5): the misattributed 'It is based on the OData protocol ...' quote replaced by the reproduced snippet text; notes (5): the 2022 What's New record kept on the writer's own re-run; notes (10): the Cloud DEX 'BOMGroup' attribute claim replaced by the measured On-Premise entity page; the method sentence kept as confirmed). Two records carry `released_api_available` on a documented OData alternative and claim no successor (Create Header of Bills of Material (Version 2) for BOM_GROUP_CREATE; Read Product Master Data of API_PRODUCT_SRV for MATERIAL_GET_DETAIL); two stay `verification_required` at `conflicting_sources` because the repository contradicts itself about whether the name exists at all (MAINTENANCEPLAN_CREATE in the flat form of `fm:BAPI_MEASUREMENTPOINT_CREATE`, MEASUREMENTDOCUM_CREATEM in the `conflictingEvidence` form of its sister `fm:BAPI_MEASUREMENTDOCUM_CREATE`). One KBA number in the batch: 2545698 on `fm:BAPI_MATERIAL_BOM_GROUP_CREATE`, read by the researcher from the public userapps.support.sap.com preview (title, Symptom, Environment, Product, Keywords; the BAPI name in Keywords only) and cited by its me.sap.com/notes url; Cause and Resolution were not read. No SAP Note number carried: 2917243 (seen in the 'PM - Measurement document' migration-page snippet) is quoted in the CREATEM notes only, and 0002267140 (printed as the Business Impact note of Simplification item 5.1.5) is quoted inside the GET_DETAIL evidence claim only.

Coverage (`npm run report:coverage -- --catalog functions`), before then after: total 142 / 142; L1 2 / 2; L2 57 / 59; L3 54 / 50; L4 1 / 1; L5 28 / 30; verified 105 / 103; verification_required 31 / 31; conflicting 6 / 8; legacy 0 / 0; s4-applicable 76 / 76; edition-specific 2 / 3. Per id before (measured through `fromFuncRegistry` + `evidenceBlock`, the report-coverage path): all four sat at derived L3 / repository_verified with no overlay record (MAINTENANCEPLAN_CREATE and MEASUREMENTDOCUM_CREATEM 'לא רלוונטי' from the invalid-name registry flag; BOM_GROUP_CREATE and MATERIAL_GET_DETAIL 'ללא שינוי ב-S/4HANA' from the def()/verified() defaults). After: BOM_GROUP_CREATE and MATERIAL_GET_DETAIL L5 / sap_official_verified / released_api_available / authored, 0 conflicts; MAINTENANCEPLAN_CREATE and MEASUREMENTDOCUM_CREATEM L2 / conflicting_sources / verification_required (authored, `release: null`, `source: null`), 1 conflict each, needsVerification true. verified 105 to 103 and conflicting 6 to 8 are the two contested ids leaving the repository-verified count for the conflicting tier; s4-applicable is unchanged because the two released ids were already counted (derived unchanged) and the two contested ids moved from not_applicable to verification_required, both outside the count; edition-specific 2 to 3 is MEASUREMENTDOCUM_CREATEM: `rowOf` takes the first private/public-cloud evidence row's edition, and its evidence[3] (the Business Accelerator Hub configuration guide) is labelled public-cloud.

## Writer-level normalisations applied
- `reviewer`: none of the four records carries the field. The MAINTENANCEPLAN_CREATE, BOM_GROUP_CREATE and MATERIAL_GET_DETAIL drafts carried it and it was dropped (the MPLAN and GET_DETAIL verdicts name the convention); the CREATEM fixedRecord already omitted it.
- Two shared `Evidence` consts hoisted so a status source and its evidence entry cannot drift: `BOM_API_CREATE_HEADER` (BOM_GROUP_CREATE evidence[0] and status.source) and `PRODUCT_A2X_READ` (MATERIAL_GET_DETAIL evidence[0] and status.source). `PRODUCT_A2X_READ` is a sibling of `PRODUCT_A2X` (same deliverable 18fe3fab96864826bfa0be0de4f65b85, loio 2bd6b919 vs 74aa2b58). MATERIAL_GET_DETAIL evidence[1] cites the same loio 74aa2b58 as `PRODUCT_A2X` but with a different audited claim and accessedAt, so it stays inline rather than reusing the SAVEDATA const (the batch-8 MATDOC_READ_ITEMS precedent: never rewrite an audited claim into a text no auditor saw whole). The two verification_required records have no const (`source: null`).
- BOM downgrade (6), the 2022 What's New record: kept. The writer re-ran `scripts/sap-help-search.mjs "ODATA API - Bills of Material" --size 8 --json` on 2026-09-22 (scratchpad b9_search_odata_api_bom_2022.json): loio 6b9d83ceac5d44ecb2db79f50e6dda06 returned as the first record, deliverable What's New in SAP S/4HANA 2022, versionId 2022.000, date 2024-10-08, snippet 'The ODATA API Bills of Material now enables you to perform the following: Create preliminary component and convert the preliminary component to stock or nonstock item'. The note now says the audit did not reproduce it and the writer did, quoting that snippet.
- BOM downgrade (7), notes (10): the 'BOMGroup BOM Group' attribute claim for the Cloud DEX page (loio 37bc6f4e) is no longer asserted; the measured On-Premise 2025.001 entity page 'Bill of Material' (APIs for Product Lifecycle Management, loio 474346711c1e4bfcbdec6035de1d65db) replaces it. The writer reproduced both fragments on 2026-09-22 (b9_search_bom_entity_bomgroup.json: 'Bill of Material Technical name: MaterialBOM Use With this entity, you can read, create, delete, and update BOM header' and 'BOMGROUP BOM group Optional' in one snippet). The closing sentence was reworded so the two Cloud records remain the two not attached as evidence and the entity page is notes-only; the note states that whether the service creates several alternatives in one call is still undetermined (gaps[4]).
- BOM downgrade (5), notes (5): the writer's own re-run for loio 8a8b08aab6cd47f4a3e35e8bd8d86ca2 (b9_search_bom_overview_0002.json) returned the technical-name sentence, 'Header and items can also be newly created or updated with batch in the OData service call. The OData model supports batch processing of multiple requests' and 'Service Structure The service contains header and item node for'; the truncated caveat 'It is not possible to use POST request in a batch on header and …' sat in the auditor's snippet window only, and the note attributes it so. status.he carries the caveat as the verdict prescribed.
- BOM downgrade (4), second half: the conflicts[0] edit addresses the researcher's wrapper, not the record; applied in the conflicts line below.
- Em/en dashes: 0 added (0 in the new-record region, lines 9660 to 10778 of the merged file); file-level counts unchanged (88 em, 1 en, 15 figure, all pre-existing inside verbatim titles, labels and quotes).
- Header: the batch-9 paragraph was added, and the Tier-1 sentence now also names the browser-rendered pages of batch 9 (the 'PM - Maintenance plan' migration page behind MAINTENANCEPLAN_CREATE; the Material Master BAPIs, List of Available BAPIs and Definition of BAPI Search Connectors pages behind MATERIAL_GET_DETAIL), the fully-read What's New 2021 and 2025 FPS01 PDFs, and the fully-read Business Accelerator Hub configuration-guide PDF behind MEASUREMENTDOCUM_CREATEM.
- `test/evidence-schema.test.ts`: no change by this batch. The functions catalog graduated on 2026-09-02; it is already in the graduated repoRef test and not in `FOUNDATION_RECORDS` (only the objects catalog remains there). The worktree carries an uncommitted diff on that file from another session (it adds `data/best-practices/pm-processes.ts` to BPS and DATA_FILES; mtime 08:51, before this write); untouched.
- Gates run after the write: `./node_modules/.bin/tsc --noEmit` reports TS5097 only in `scratchpad/validate-bp-file.mts`, an untracked file of another session at the repository root (created 08:53 to 09:01, not part of this batch, not touched); with only `scratchpad/**` excluded through a temporary extending tsconfig kept in the session scratchpad, exit 0. `tsc --noEmit -p tsconfig.test.json` 0; `npm test` 207 pass / 0 fail; `eslint data/verification/functions.ts` 0; `validateRecords` over the four records alone and merged with every overlay = 0 problems; all 67 xrefs resolve (22 + 18 + 10 + 17); the emitted TypeScript round-trips to the audited JSON with sorted keys (identical per record; 62 records, 0 duplicate ids; for the two released records status.source is the same object as the evidence entry); coverage as above. Not committed.

## refuted
- (none in this batch: all 4 audited drafts survived verification and were written with their auditors' downgrades applied.)

## conflicts
- `fm:BAPI_MAINTENANCEPLAN_CREATE`: (1) no help.sap.com record prints the name in any scope (exact-name query: On-Premise 9 to 10 records across the researcher's and the auditor's runs, locked 2025.001 and 2023.latest 5 each, Public Edition 3, SAP_ERP and unfiltered 21 each; all without the name, run-dependent), neither fully-read What's New PDF (2021, 2025 FPS01) contains BAPI_MAINTENANCEPLAN, and the fully-read 'PM - Maintenance plan' migration page (2025 FPS01) names MPLAN_CREATE, not a BAPI, for the Create Maintenance Plan step. (2) Repository split 2 vs 4: `data/bapi-enrichment.pm.ts` (invalid-name, confidence high, source a Tier-3 SE37 mirror plus SAP Community) and the academy lesson say the name does not exist and point to MPLAN_CREATE / MPLAN_CHANGE; `data/function-intel.ts` (PLAN_CATEGORY / CYCLE, MAINTENANCEPLAN, IP01 / IP10 / IP30, MPLA / MHIO), the PM blueprint (MPLA funcs), `data/transactions.ts` (IP01 funcs) and `data/domain-detail.ts` present it as existing; the merged registry row carries invalid-name beside stability Released and operationType Create. (3) MPLAN_CREATE, MPLAN_CHANGE, MPLAN_ITEM_CREATE and I_MaintenancePlanBasic are not universe ids: no xref, no alias, no successor. (4) F3622, F3621, F5325, F2774, W0192 and F5356 are absent from `data/fiori/apps.ts`; fiori:F4072 and fiori:F2828 are navigation anchors only. (5) BUS2028 is returned by no record; the only official object id found is the Enterprise Services 'Maintenance Plan' business object with the released operation MaintenancePlanERPCreateRequestConfirmation_In (SAP APPL 6.06), an SOA operation, not a BAPI, so not a status source. (6) 'Operations for Maintenance Plan' surfaces only at 2023.latest On-Premise / 2608.500 Cloud, and the strategy-plan create page only under Cloud. Needs SE37 / BAPI Explorer in ECC and in the target S/4HANA system.
- `fm:BAPI_MATERIAL_BOM_GROUP_CREATE`: (1) no product documentation page of S/4HANA, SAP ERP or Cloud Public Edition prints the name (exact-name query: On-Premise 7 fuzzy empty-snippet records, SAP_ERP 8, Cloud 2, reproduced by the auditor); it appears in the Keywords line of KBA 2545698 and in three Support Content wiki pages ('List of BAPI's': LO-MD-BOM, BUS1080, 'Creation of a material BOM group'; 'ECC standard Batch Input programs'; a signed community example), so existence, LO-MD-BOM and BUS1080 are secondary-source facts, and the parameters (BOMGROUP, VARIANTS, ITEMS, ALL_ERROR, RETURN), the function group and the release status are repository-only (def() defaults; a sweep label 'SAP Help Portal' without URL). (2) Blueprint labels: `data/sapData.pppi.ts` names the BAPI 'יצירת עץ מוצר (S/4 BOM API)' (MAST), 'יצירת עץ מוצר (S/4)' (STKO) and 'יצירת פריטי עץ מוצר' (STPO) (generated file, not hand-edited), while the sources found show a classic LO-MD-BOM BAPI listed among the ECC BOM-load interfaces (CS01, RCSBI010 to RCSBI040, BOMMAT); no official source presents it as a new S/4HANA API. (3) `data/function-intel.ts#CSAP_MAT_BOM_CREATE` prefers the BAPI for new scenarios without official backing: the 2025 FPS01 pages point new interfaces to the OData Bills of Material (Version 2) service, and the official 'PP - Material BOM' migration object uses CSAI_BOM_MAINTAIN and CSAP_MAT_BOM_MAINTAIN. (4) Fiori name: the blueprint writes 'Manage Bills of Material' (table:STKO mentions F1814) while the 2025 FPS01 pages and the Fiori library say 'Maintain Bill of Material (F1813)'; neither id is in `data/fiori/apps.ts`. (5) Two repository parameter lists (function-intel: one table input; pppi: IMP ALL_ERROR and TAB RETURN). (6) Service naming: the operation URIs say API_BILL_OF_MATERIAL_SRV;v=2, the overview page snippet API_BILL_OF_MATERIAL_SRV_0002, and '(Version 2)' is the Operations page title; whether the service creates several alternatives in one call is undetermined (the entity page lists an optional BOMGROUP attribute only). (7) The Simplification List was not re-read for this record; the MATNR-extension effect on the BAPI interface stays open at table:MARA. Needs SE37 / BAPI Explorer (BUS1080).
- `fm:BAPI_MATERIAL_GET_DETAIL`: (1) the name is printed only on the Support Content page 'Material Master BAPIs' (no product release; four-column table with Background and Dialogue header rows) and in repository data; no S/4HANA On-Premise, SAP ERP or Cloud record prints it (exact-name totals index-dependent: On-Premise 1 to 3, SAP_ERP 18 to 21, Cloud 3; 0 snippets name it), so availability in 2025 FPS01 is a repository claim (def() / verified() defaults). (2) Parameters: Support Content says 'client, plant and valuation area level'; the repository lists MATERIAL, PLANT, MATERIAL_GENERAL_DATA and RETURN only; valuation-area parameters are unverified and table:MBEW is not an xref. (3) Simplification item 5.1.5 does not name the module; the long material-number parameter name of this module appears in no source; the piece list needs an S-user. (4) F1602 (Manage Product Master Data) is printed officially but absent from `data/fiori/apps.ts`. (5) The 'SAP recommends ... Product Master (A2X) ...' sentence is returned for two loios (74aa2b58 and a8661dda); cited to 74aa2b58 as in the SAVEDATA record. (6) The ECC 6.0 MM release-notes chapter (74 pages) has 0 BAPI_MATERIAL hits. Needs SE37 / BAPI Explorer.
- `fm:BAPI_MEASUREMENTDOCUM_CREATEM`: (1) no official record prints the name in any scope (exact-name query: On-Premise 4, SAP_ERP 6 to 9, Cloud 1 to 3 across the two runs, all without snippet; one Cloud hit, 'Read a Measurement Document' 2608.500, concerns measurement documents but not the name), neither fully-read document contains it (WN_OP2025_FPS01_EN.pdf; the Hub 'Mass Creation of Measuring Documents' guide with 0 'BAPI' occurrences); the documented multiple-create channel is the $batch operation of API_MEASUREMENTDOCUMENT and the documented RFC module is the single-processing MEASUREM_DOCUM_RFC_SINGLE_001. (2) Repository split: `data/bapi-enrichment.pm.ts` invalid-name (Tier-3 mirror plus SAP Community) vs `data/function-intel.ts` (MEASUREMENT_DOCUMENTS / RETURN, IK34, IMRG / IMPTT), the PM blueprint IMRG funcs and `data/domains.ts#pm-measuring-points` in a third spelling, CREATEMULT (32 characters, over the 30-character fm id limit; no alias; the fix belongs in domains.ts). (3) IK34 and IK22 are named in no official snippet (repository tx-intel only). (4) SAP Note 2917243 is quoted from the migration-page snippet in the notes only and not carried. (5) MEASUREM_DOCUM_RFC_SINGLE_002 / _CANCEL and Fiori measurement apps are not universe ids. Needs SE37 / BAPI Explorer.
- Cross-record, method: as in batches 4 to 8, help.sap.com search snippets and counts are query- and run-dependent, so every count in this batch is a day-bounded measurement (MAINTENANCEPLAN_CREATE 9 to 10, BOM_GROUP_CREATE 7 / 8 / 2, MATERIAL_GET_DETAIL 1 to 3 / 18 to 21 / 3, MEASUREMENTDOCUM_CREATEM 4 / 6 to 9 / 1 to 3). HTTP 200 from help.sap.com/docs is a JavaScript shell, not proof that a loio exists. The sc4sap MCP failed to connect in every session of this batch (researchers, auditors, and the writer's session: 'MCP error -32000: Connection closed' at session start), so no SE37 check confirms or contradicts any name, parameter, function group or release status.

---

# Functions batch 10 (2026-09-22): 4 drafts audited, 4 written, 0 refuted

Written by the single writer from the adversarial verdicts (no `fixedRecord` supplied; the listed downgrades applied to each researcher draft). `fm:NOTIF_TASK_READ`: evidence #4 variant sentence corrected (Display, loio 5a32c6ef, is the read-only variant of Find F2071, not the reverse), notes F5797 attribution moved to Feature Comparison for Maintenance Notification Apps. `fm:NOTIF_ACTIVITY_READ`: `reviewer` and the non-schema `lastVerifiedAt_note` removed. `fm:BAPI_MEASUREMENTPOINT_GETLIST`: evidence #3 now states the snippet names I_MeasuringPointData (Released), notes (5) and recommendedAction rewritten to match, 'Get Top N Measuring' marked truncated, `reviewer` and notes (7) removed. `fm:BAPI_OBJCL_CREATE`: the house-style "no reviewer" sentence dropped from notes. The drafts carried no `gaps` field, so the gaps-only downgrades (and the "summary" and "לף אחד" typo items, whose target text was not in the drafts) had nothing to apply to. Two shared Evidence consts hoisted (`NOTIF_ITEM_ACTY_READ`, `EQUI_CLASSIFICATION_OPS`) so each status.source equals its evidence entry.

## refuted
- (none in this batch: all 4 audited drafts survived verification and were written with their auditors' downgrades applied.)

## conflicts
- `fm:NOTIF_TASK_READ`: no official record names the FM (exact-name query 21 On-Premise / 21 SAP_ERP, all empty snippets); no task entity or read operation found in API_MAINTNOTIFICATION snippets, while API_QUALITYNOTIFICATION documents 'Read Quality Notification Task'. QMNUM parameter and Tasks output (`data/function-intel.ts`, inferred) unverified. Fiori F5797 (Display Maintenance Notifications) is not in `data/fiori/apps.ts`, so not an xref; F2071 (Find Maintenance Notifications) likewise absent.
- `fm:NOTIF_ACTIVITY_READ`: no official record names the FM (21 / 21 / 7). The documented OData read is item-level only (MaintNotificationItemActivity), while Data Migration 'PM - Maintenance notification' distinguishes header activities (S_NOTIF_ACTIVITY) from item activities (S_NOTIF_ITM_ACTIVITY); header-activity coverage in OData is open. Repo CDS name I_MaintNotifActivity differs from the documented I_MaintNotificationActyData.
- `fm:BAPI_MEASUREMENTPOINT_GETLIST`: repository contradicts itself on whether the name exists: `data/bapi-enrichment.pm.ts` marks it `invalid-name` (pointing to MSAM_MEAS_POINT_GETDETAIL, itself not found officially), while `data/function-intel.ts`, `data/sapData.pm.ts` IMPTT funcs and `data/knowledge/object-intel.ts` IMPTT present it as existing. The xref `cds:I_MeasuringPoint` rests on the cds.ts record only; the official VDM page documents I_MeasuringPointData, which is not in the universe. IK07/IK08 association repository-only.
- `fm:BAPI_OBJCL_CREATE`: repository disagrees on details only: function-intel lists AUSP/INOB and CL20N/CL02, bapi-enrichment.pm lists KSSK/AUSP/KLAH and CL20N/CL24N. None of AUSP, KSSK, KLAH, INOB or fm:BAPI_OBJCL_CHANGE is in the universe. Function-intel's batch class type '023' is correct only at client/material level (022 at plant level per Batch Management 2025.001). The sweep's 'SAP Help Portal - verified 2026-07-15' source has no URL. Release status, parameters and COMMIT requirement stay repository-only.

## Writer-level normalisations applied
- Em/en dashes added: 0.
- `test/evidence-schema.test.ts`: no change; functions graduated 2026-09-02 and is already in the graduated repoRef test.
- Gates: `tsc --noEmit` 0, `tsc --noEmit -p tsconfig.test.json` 0, `npm test` 212/212 pass. Coverage (functions): L2 59 to 60, L3 50 to 48, L5 30 to 31, verified 103 to 104, verification_required 31 to 29, conflict 8 to 9.

---

# Functions batch 11 (2026-09-22): 6 drafts audited, 6 written, 0 refuted

Written by the single writer from the adversarial verdicts (no `fixedRecord` supplied; the listed downgrades applied to each researcher draft). `fm:BAPI_PR_CREATE`: Simplification List method sentence appended to notes (the writer cites the two official SIMPL PDF urls instead of the auditor's temporary scratchpad path). `fm:BAPI_REQUISITION_GETDETAIL`: no downgrades; one method clause added to its existing Simplification List sentence. `fm:BAPI_RESERVATION_CREATE1`: evidence #2 quote cut at the snippet boundary ('... create and delete reservation', marked truncated), Delete Reservation added to its operation list, status.he attributes API_RESERVATION_DOCUMENT_SRV to loio 025cdccd12e4438f95788d5e3acfdb12 and words the Simplification List negative as search-bounded, method line (ה) added to notes. `fm:BAPI_TASKLIST_CREATE`: Simplification List method line appended to notes, so the status.he sentence stays. `fm:CR_TEXT_READ`: API_WORK_CENTERS dropped from notes (not in the cited snippet); the 'לפי ה-hint' sentence replaced by the named full-text grep of the two SIMPL PDFs. `fm:CS_BOM_EXPL_FLO_RC1`: the drafts carry no `gaps` field, so the gap-4 rewrite (EAM_MAINTENANCE_BOM, loio 21317859a54d477b89e43e4501249157, not a universe id, not a successor) went into notes; the Simplification List sentence now names the grep.

## refuted
- (none in this batch: all 6 audited drafts survived verification and were written with their auditors' downgrades applied.)

## conflicts
- `fm:BAPI_PR_CREATE`: release status, RFC flag and the PRHEADER/PRITEM parameter contract are repository-only (sweep and function-intel); Fiori F2229 (named in the migration snippet) is not in `data/fiori/apps.ts`; IDoc PREQCR102 / PREQCHANGE02 are not in IDOC_BASIC_TYPES and are not tied to the BAPI by any snippet.
- `fm:BAPI_REQUISITION_GETDETAIL`: no official record prints the name (On-Premise 7 / SAP_ERP 21 / Cloud 2, all without it; AFS 6.0 and 6.3 release notes without it). BAPI_PR_GETDETAIL is printed officially but is not a universe id and no source calls it a successor. Repository descriptions differ (function-intel: items, account assignment, source; sweep: items, allocations, texts), and the sweep's requiresCommit / stability / s4OnPremSupport are template defaults.
- `fm:BAPI_RESERVATION_CREATE1`: repository contradicts itself on parameter names (sweep RESERVATIONHEADER / RESERVATIONITEMS vs pppi RESERVATION_GENERAL_DATA / RESERVATION_ITEMS); the only official record printing the name is the SAP ERP EHP7 What's New list. F4839 and F5601 are not in `data/fiori/apps.ts`; table RKPF is not a universe id.
- `fm:BAPI_TASKLIST_CREATE`: repository contradicts itself on whether the name exists (`data/bapi-enrichment.pm.ts` invalid-name on a Tier-3 SE37 mirror plus SAP Community vs `data/function-intel.ts`, the PM blueprint PLKO funcs, `data/domains.ts`, `data/domain-detail.ts` and `data/academy/lessons/pm-generated.ts` presenting it as usable, the last at trust verified-docs). Official 2025 FPS01 pages name EAM_TASKLIST_CREATE / CHANGE / POST and API_MAINTENANCETASKLIST, none of them universe ids. Needs SE37 / BAPI Explorer.
- `fm:CR_TEXT_READ`: no official record names the FM; function-intel marks it `inferred: true` while `data/academy/lessons/pm-generated.ts:171` labels the work-center FM group verified-docs. The service name API_WORK_CENTERS appears only in the uncited sibling record loio 57db94e225e740d5ad1685c0ec6d3c3d; add it as its own evidence before naming the service.
- `fm:CS_BOM_EXPL_FLO_RC1`: no official record names the FM, CS_BOM_EXPL_FLO or RCSBI020; the sweep marks the sibling CS_BOM_EXPL_MAT_RC1 invalid-name (pointing to CS_BOM_EXPL_MAT_V2), and `data/knowledge/object-intel.ts#TPST` names a different variant CS_BOM_EXPL_FLO_V1. EAM_MAINTENANCE_BOM and I_FUNCTIONALLOCBOMLINK are not universe ids. Needs SE37.

## Writer-level normalisations applied
- `reviewer` dropped on all 6 (house convention); `successor: null` dropped on BAPI_TASKLIST_CREATE (the type allows only a canonical id or omission).
- Two shared Evidence consts hoisted (`PR_ODATA_OPS`, `RESERVATION_ODATA_CREATE`) so each status.source equals its evidence entry.
- Simplification List negatives re-checked by the writer: full-text grep of the pdftotext extracts of SIMPL_OP2025.pdf and SIMPL_OP2023.pdf returns 0 hits for BAPI_PR_CREATE, BAPI_REQUISITION_GETDETAIL, BAPI_RESERVATION_CREATE1, BAPI_TASKLIST_CREATE, EAM_TASKLIST, CR_TEXT_READ and CS_BOM_EXPL_FLO; control strings BAPI_GOODSMVT_CREATE (6 / 6) and BAPI_PO_CREATE (2 / 2) hit.
- Em/en dashes added: 0.
- `test/evidence-schema.test.ts`: no change; functions graduated 2026-09-02.
- Gates: `tsc --noEmit` 0, `tsc --noEmit -p tsconfig.test.json` 0, `npm test` 212/212 pass. Coverage (functions): L2 60 to 61, L3 48 to 44, L4 1 to 2, L5 31 to 33, verified 104 to 105, verification_required 29 to 27, conflict 9 to 10.

---

# Functions batch 12 (2026-09-22): 6 drafts audited, 6 written, 0 refuted

Written by the single writer from the adversarial verdicts. `fm:CS_BT_BOM_HEADER_READ`: `verdict.fixedRecord` taken verbatim (key list cut to the snippet, the Malaysia-count sentence reworded as index drift, API_BILL_OF_MATERIAL dropped from notes). `fm:EQUI_TIMESEGMENT_READ`: evidence #4 bounded to the AFS-source migration object, the "history not migrated" inference limited to that object. `fm:EQUIPMENT_DISMANTLE`: the stitched DismantleEquipment quote replaced by the verbatim snippet fragments (evidence #1 and status.source, now one shared const `EQUI_DISMANTLE_FI`); EAM Part 1 quote cut after BAPI_IE4N_DISMANTLE (marked truncated), BAPI_IE4N_EXCHANGE removed from notes, BAPI_IE4N_INSTALL stays sourced to EAM Part 4 (loio 3346ac67364447a3ba2f4efa65b8c014). `fm:EQUIPMENT_TEXT_READ`: no downgrades. `fm:FUNC_LOCATION_LABEL_READ`: RFC_002 parameter quote replaced by the two verbatim snippet pieces; the On-Premise hit count reworded as 18 to 19 (drifts between runs). `fm:ILOA_INHERIT_FROM_FUNCLOC`: the KBA number seen only on a non-allowlisted host removed from notes. The drafts carried no `gaps`/`summary` fields, so gaps-only downgrades had nothing to apply to.

## refuted
- (none in this batch: all 6 audited drafts survived verification and were written with their auditors' downgrades applied.)

## conflicts
- `fm:CS_BT_BOM_HEADER_READ`: no official record names the FM or a CS_BT group; the repo parameter contract (STLNR/STLAL) is `inferred: true` in `data/function-intel.ts`. API_MAINTENANCEBOM (Read Maintenance BOM Header) is documented but not tied to the FM and not a universe id. Needs SE37.
- `fm:EQUI_TIMESEGMENT_READ`: no official record names the FM. Repo drift: function-intel links IE03 while the PM blueprint EQUZ row lists IE02 / IE4N. `cds:I_EquipmentTimeSegment` stays verification_required (the documented public-cloud name is I_EQUIPMENTTIMESEG). The only official time-segment migration statement is scoped to the AFS-source object.
- `fm:EQUIPMENT_DISMANTLE`: the repo's "internal, not RFC, requiresCommit" rests on a sweep sourced to sapdatasheet.org / se80.co.uk (not allowlisted). The authored `released_api_available` covers the scenario (API_EQUIPMENT DismantleEquipment, 2023.latest; 2025.001 record of the same loio not returned today) and replaces the derived `restricted`; keeping `restricted` remains a defensible alternative. BAPI_IE4N_DISMANTLE / BAPI_IE4N_INSTALL are official names but not universe ids; BAPI_IE4N_EXCHANGE was seen in an earlier run only.
- `fm:EQUIPMENT_TEXT_READ`: no official record names the FM or the ITX1 group (sweep source is a string without a link). I_EquipmentText (Released, 2023.latest VDM) is not a universe id, so it is evidence, not an xref.
- `fm:FUNC_LOCATION_LABEL_READ`: no official record names the FM; no `data/bapi-enrichment.*` entry. FUNC_LOCATION_RFC_002 and I_FunctionalLocationLabel are official but not universe ids and not stated as successors. TPLNR / Label contract is `inferred: true`.
- `fm:ILOA_INHERIT_FROM_FUNCLOC`: no official record names the FM; the official Data Transfer pages describe a dialog function only. A KBA title on data origin in BAPI_FUNCLOC_CREATE was seen on userapps.support.sap.com (not allowlisted, body not read) and is not cited.

## Writer-level normalisations applied
- `reviewer` dropped on all 6 (house convention).
- `fm:EQUIPMENT_TEXT_READ` evidence #3 release normalised from "2023 Latest" to "2023.latest" (matches the URL versionId).
- `fm:EQUIPMENT_DISMANTLE` notes (2): the reviewer-addressed sentence reworded as a plain alternative, same content.
- Em/en dashes added: 0 in authored prose; two occur inside verbatim quotes (the SAP deliverable title "Logistics — General (LO)" and the repository sweep source string "... se80.co.uk) — verified 2026-07-15").
- `test/evidence-schema.test.ts`: no change; functions graduated 2026-09-02.
- Gates: `tsc --noEmit` 0, `tsc --noEmit -p tsconfig.test.json` 0, `npm test` 212/212 pass. Coverage (functions): L3 44 to 42, L4 2 to 3, L5 33 to 34, verified 105 to 109, verification_required 27 to 23, conflict 10 to 10.

---

# Functions batch 13 (2026-09-23): 6 drafts audited, 6 written, 0 refuted

Written by the single writer from the adversarial verdicts. `fm:ISCHED_CALL_GENERATE`: `verdict.fixedRecord` applied (the draft with `reviewer` removed). `fm:ILOA_READ`: evidence #1 'ואינו ממצא' corrected to 'ושאינו מאומת' (`deriveStability` in `lib/bapi-registry.ts` returns 'Internal' for a non-BAPI whose status is not verified*). `fm:ILOA_UPDATE`: the KBA Environment list says 'SAP ERP Central Component' where the draft said 'ECC' (the preview's own wording); the optional split of the folded loios (60adb653, b6a1e644, 4ee7f73c, 75e9ecd8) into their own evidence entries was not applied: each folded fact names its loio inline and traces to its own search record, the house pattern the verdict allows. `fm:ISU_FUNCLOC_GETLIST`: the On-Premise count is marked run-dependent (a sixth record, an Intercompany Data Exchange page without snippet, in one run), "indexed under 2023 Latest only" became "returned under 2023 Latest in the searches run", and the $filter string is quoted after the path and the truncation instead of being tied to the FunctionalLocation collection. `fm:K_COSTS_READ`: status.recommendedAction names the page behind F5241, F4603 and F3567 (Analyzing Costs and Settling the Order, Maintenance Management 2025.001, loio 2d32b8ac5466449285b667cf8a02e0d5; the writer re-read its body and found all three ids). `fm:K_ORDER_SETTLEMENT`: the ECC 6.0 EHP8 reading moved out of evidence #4 into its own fifth evidence entry (edition ecc, release ECC 6.0 EHP8, the SAP_ERP 6.18.latest url of the same loio, title and deliverable as the search record returns them), so the S/4 entry carries S/4 facts only; the writer re-read both bodies: identical text (2,750 characters each), 'RKO7KO8G (with order category 30)' in both, K_ORDER_SETTLEMENT in neither.

## refuted
- (none in this batch: all 6 audited drafts survived verification and were written.)

## conflicts
- `fm:ILOA_READ`: no official record names the FM (exact name: On-Premise 21, quoted 8, SAP_ERP 21, Cloud 4; the 46 bodies read in full contain no word starting with ILOA). The ILOAN input and 'ILOA data' output are `inferred: true` in `data/function-intel.ts`. Repo drift: function-intel links IE03, the PM blueprint and the registry list IL02 and IE02. The documented read channels (FUNC_LOCATION_RFC_002; API_EQUIPMENT and API_FUNCTIONALLOCATION; I_FunctionalLocationData and I_EquipmentData, both 'modeled for usage as a DataSource in Business Warehouse') are not universe ids and no source calls them successors. Needs SE37.
- `fm:ILOA_UPDATE`: the only SAP record that prints the name is the Keywords line of KBA 2516482 (IW21 update termination, DBSQL_DUPLICATE_KEY_ERROR on an ILOA insert; cause and solution need an S-user and were not read). The derived `restricted` (registry: internal-unsupported, remoteEnabled no, requiresCommit yes; sweep text: function group ILA0) rests on a sweep sourced to sapdatasheet.org / se80.co.uk, which are not allowlisted, while the two context-only official entries lift the level pill to 'מאומת מול תיעוד SAP רשמי': the pill can overstate what is verified about the module itself (the ILOA_INHERIT_FROM_FUNCLOC pattern). The repository describes an update 'for a technical object'; the KBA context is a notification save. Transaction drift: function-intel IE02 only; registry and sweep IL02 and IE02; `data/transactions.ts` IL02; blueprint 'IL02; IE02 (ירושה)'. If SE37 shows an update-task module, the RETURN output in function-intel needs a recheck. Needs SE37.
- `fm:ISCHED_CALL_GENERATE`: no official record names the FM or the fragments ISCHED / CALL_GENERATE; `lib/verification.ts` lists ^ISCHED_ among its SUSPICIOUS patterns. 'Enterprise Asset Management Part 4' (LOG_EAM_CI_4) says scheduling APIs 'provide the same functions as transaction IP10' without naming them; whether this FM is one of them is the first SE37 / BAPI Explorer question. The documented channels (StartMaintPlnSchedule and ReleaseCall of API_MAINTENANCEPLAN, valid as of 2021 FPS01; the released service operation MaintenancePlanERPScheduleRequestConfirmation_In, SAP APPL 6.06) are not universe ids and not successors. `cds:I_MaintenancePlan` is a navigation anchor only (per the project's cds record: deprecated as of S/4HANA 2021, to be deleted as of 2023, successor I_MaintenancePlanBasic, which is not in the universe).
- `fm:ISU_FUNCLOC_GETLIST`: no official record names it, and no source explains the ISU_ prefix (`lib/verification.ts` lists ^ISU_ among its SUSPICIOUS patterns). Repository drift: the PM blueprint (IFLOT funcs) and `data/function-intel.ts` name ISU_FUNCLOC_GETLIST, while `data/knowledge/object-intel.ts#IFLOT` classesApis names BAPI_FUNCLOC_GETLIST in the same place. No repository record says ISU_FUNCLOC_GETLIST does not exist, so this is drift, not conflictingEvidence. BAPI_FUNCLOC_GETLIST is printed by no official record either and is not a universe id. Needs SE37 of both names.
- `fm:K_COSTS_READ`: no official record names the FM (On-Premise 5, SAP_ERP 21, Cloud 2, all empty snippets; K_COSTS and COSTS_READ in none of SIMPL_OP2025, SIMPL_OP2023, CONV_OP2025, What's New 2025 FPS01, What's New 2021, the ECC 6.0 CO release notes). The authored verification_required replaces a derived `restricted` that came from the naming rule looksInternal in `lib/bapi-registry.ts` (one-letter K_ prefix), not from a source. `data/academy/lessons/pm-generated.ts` and `pp-generated.ts` tag the FM trust 'verified-docs' with SAP Help sources the searches do not reproduce; the blueprint's Fiori cell 'Maintenance Order Actuals (אמת ID)' names no app the searches returned. F4603, F3567, API_MAINTENANCEORDER_0002, I_MaintOrderActualCostDataCube and I_MaintOrdCostDetnStatus are not universe ids; F5241 is added to `data/fiori/apps.ts` only by another session's uncommitted work (not at HEAD), so it is not an xref here. Needs SE37.
- `fm:K_ORDER_SETTLEMENT`: no official record names the FM (On-Premise 0, SAP_ERP 1 unrelated, Cloud 4 empty snippets; K_ORDER_SETTLEMENT, K_ORDER, KO88, KO8G and RKO7KO88 in neither SIMPL list). The authored verification_required (release and source null, the PPCC1 precedent) replaces a derived `restricted` from the same naming rule. Repo drift: the blueprint's RKO7KO88 vs the official RKO7KO8G (order category 30); the repository's KO8G vs the official KO8GH for collective settlement (the page does not state how the two relate); function-intel ties the FM to AUFK, the blueprint to COSP; the call chain in `data/centers/debugging.ts` is unsourced; the academy 'verified-docs' tags are unreproduced; bp:order-settlement-process names the FM on repository records only. A non-SAP catalogue shows only a differently named K_ORDER_SETTLEMENT_RULE (not cited). F4568, F3767 and KO8GH are not universe ids. Needs SE37.
- Cross-record, method: search counts are run-dependent day-bounded measurements. Bodies were read through help.sap.com/http.svc/deliverableMetadata and http.svc/pagecontent (ILOA_READ, ISCHED_CALL_GENERATE; now a MANIFEST row) or in a browser (K_COSTS_READ, K_ORDER_SETTLEMENT). The sc4sap MCP failed to connect in every session of this batch, the writer's included ('MCP error -32000: Connection closed' at session start), so no SE37 check confirms or contradicts any name, parameter, function group or release status.

## Writer-level normalisations applied
- `reviewer` dropped on ILOA_READ, ILOA_UPDATE and ISCHED_CALL_GENERATE (house convention; the other three carried none).
- `DATE23` ("2026-09-23") added beside DATE22; every accessedAt and lastVerifiedAt of the six records uses it.
- One shared Evidence const hoisted (`K_COSTS_SIMPL_CO_OM`) so the status.source of K_COSTS_READ is the same object as its evidence #2 (the draft carried two identical copies).
- `fm:ISU_FUNCLOC_GETLIST` evidence #1: the verdict's run-dependence caveat is merged into the parenthetical that already lists the five records, instead of standing as a second parenthetical right after it; same content.
- Deviation, `fm:K_COSTS_READ`: xref `bp:order-settlement-process` dropped (14 to 13 xrefs). It resolves in the app and in `test/evidence-xref.test.ts`, but `test/evidence-schema.test.ts` builds its universe without `data/best-practices/cross-processes-2.ts` (commit 0c4a12c9 added that file to the xref test only), so the dangling-xref rule failed on it (`npm test` 211/212 before the drop). Dropped instead of editing the shared test; the K_ORDER_SETTLEMENT sibling already leaves the same bp out. Harness gap for the orchestrator: add CROSS_PROCESS_PRACTICES_2 to BPS (and the file to DATA_FILES) in `test/evidence-schema.test.ts`. At 07:01 another session left an uncommitted edit in this worktree that adds it to BPS (DATA_FILES untouched); the drop stays so this batch does not depend on an uncommitted test change, and the xref can come back once that edit is committed.
- `fm:ILOA_READ` evidence #2 keeps its url, the raw help.sap.com/http.svc/elasticsearch query (allowlisted host, reproduces the count); sibling negative-search entries carry no url, and the verdict calls this UX only, not required.
- Em/en dashes added: 0; Cyrillic: 0; CERTAINTY_RE and PLACEHOLDER_RE: 0 hits over every claim, status text and note of the six records (checked although the record level is official).
- Header: the missing batch-12 paragraph and the batch-13 paragraph added.
- `audit/s4-enrichment/MANIFEST.md`: one row documents the content-service channel, as the ISCHED verdict asks once the channel is adopted, and the topic-page fallback row names it; the writer measured the channel on four pages (Scheduling and Automatic Scheduling 2025.001; Settlement of an Order 2025.001 and 6.18.latest; Analyzing Costs and Settling the Order 2025.001).
- `test/evidence-schema.test.ts`: no change; functions graduated 2026-09-02 and is already in the graduated repoRef test.
- Gates: `./node_modules/.bin/tsc --noEmit` 0; `tsc --noEmit -p tsconfig.test.json` 0; `npm test` 212/212 pass; `eslint data/verification/functions.ts` 0; `validateRecords` over the six alone and over every overlay = 0 problems in both the schema-test and the xref-test universes; 85 xrefs, 0 dangling; the fiori xrefs (F2774, F5325) are at HEAD; the emitted TypeScript round-trips to the merged JSON with sorted keys (85 records, 0 duplicate ids). Coverage (functions, 142): L2 61 to 63, L3 42 to 39, L4 3 to 4, verified 109 to 112, verification_required 23 to 20, conflict 10 to 10, s4-applicable 76 to 74 (the CO pair leaves the naming-rule `restricted`). Not committed.

---

# Functions batch 14 (2026-09-23): 6 drafts audited, 6 written, 0 refuted

Written by the single writer from the adversarial verdicts (no `fixedRecord` supplied; the listed downgrades applied to each researcher draft). The drafts were taken byte for byte from the writer's own task message, not retyped. `fm:K_SETTLEMENT_RULE_READ`: no downgrades. `fm:MAINTENANCE_ITEM_READ`: `reviewer` dropped; notes (11) now read 'שדה reviewer אינו נכתב לפי מוסכמת קובץ functions.ts (אף רשומה בו אינה נושאת אותו).' `fm:MAINTENANCE_PLAN_SCHEDULE`: `reviewer` dropped, no other change (the verdict says so; notes (9) keeps its own wording). `fm:MEASUREM_DOCUM_RFC_SINGLE_001`: no downgrades. `fm:MEASUREMENT_POINT_READ`: the optional fifth evidence entry added (the SAP ERP 6.0 EHP8 page of MEASUREM_POINT_RFC_SINGLE_002); the other downgrade concerns the researcher's summary text, not the record. `fm:NOTIF_CAUSE_READ`: '186 נושאים שונים' became '186 צמדי נושא-גרסה שונים (183 loio שונים)' in evidence #2 and in notes; evidence #3 is a shared const used as status.source.

## refuted
- (none in this batch: all 6 audited drafts survived verification and were written.)

## conflicts
- `fm:K_SETTLEMENT_RULE_READ`: no official record names the FM (exact name in On-Premise, SAP_ERP and Cloud and unscoped; 431 saved search records scanned; K_SETTLEMENT, SETTLEMENT_RULE, RULE_READ, KO02, COBRA and COBRB in neither SIMPL_OP2025 nor SIMPL_OP2023). The authored verification_required replaces a derived `restricted` that came from the naming rule `looksInternal` in `lib/bapi-registry.ts` (one-letter K_ prefix), not from a source; depth drops L3 to L2 on purpose. Repository drift: `data/academy/lessons/pm-generated.ts` (line 433) and `pp-generated.ts` (line 681) tag the FM trust 'verified-docs' with SAP Help sources the searches do not reproduce; the call chain and the breakpoint step in `data/centers/debugging.ts` are unsourced; the blueprint fioriApp cell 'Manage Settlement Rules (אמת ID)' is not an app id (the app of that name the search returns is F5695, internal orders, not in the universe); function-intel ties the FM to COBRB and KO02 only, the blueprint also to COBRA and IW32; bp:order-settlement-process names the FM on repository records only. The documented read channels (Read Settlement Rules of a Maintenance Order (Version 2) of API_MAINTENANCEORDER_0002; the Costs tab of F5241) are context, not successors; the first version of the service, API_MAINTENANCEORDER, is documented as deprecated with S/4HANA 2023. Index note: the V2 settlement-rule entity and operation pages come back from search under 2023.latest only (Cloud: 2608.500), while the content service serves the same loios in the 2025.001 build. Needs SE37.
- `fm:MAINTENANCE_ITEM_READ`: no official record names the FM (exact name: On-Premise 16 to 21 per run, SAP_ERP 21, Cloud 19, quoted 21; 236 records, 87 topics; 106 topic bodies read in full, none contains MAINTENANCE_ITEM_READ or a technical name ending in _READ; MAINTENANCE_ITEM_READ, MAINTENANCE_ITEM and ITEM_READ in neither SIMPL list). The WAPOS input and 'Maintenance item' output are `inferred: true` in `data/function-intel.ts`. Transaction drift: function-intel IP02, blueprint and registry IP04 and IP02, `data/transactions.ts` IP01 (funcs), while the official sources name IP06 as the display transaction for a maintenance item, which no layer names. F5356 (Manage Maintenance Items) is printed officially but is not in `data/fiori/apps.ts`. API_MAINTENANCEITEM, the service operation MaintenancePlanERPItemByElementsQueryResponse_In (ECC_MAINTPLNITM002QR), C_MaintenanceItemDEX and I_MaintenanceItemData are not universe ids and no source calls them successors. Needs SE37.
- `fm:MAINTENANCE_PLAN_SCHEDULE`: no official record names the FM (56 search records across three scopes, every body read, none contains it; MAINTENANCE_PLAN_SCHEDULE, PLAN_SCHEDULE, MAINTENANCE_PLAN_ and MaintenancePlanSchedule in neither SIMPL list). Handed to this queue by the record's notes (5): the S/4 line of the module page ('זמין ב-S/4HANA.' from FUNCTION_INTEL.s4 through `components/neo-shell/reference/bapi-data.ts` s4Of) carries no source, and neither does the ECC line 'זמין ב-ECC.'; the fix belongs in `data/function-intel.ts`, whose record is not marked inferred, which is also the only reason `lib/verification.ts#classifyRows` counts the FM among its 34 of 85 verified modules. 'Enterprise Asset Management Part 4' (LOG_EAM_CI_4) documents scheduling APIs with the functions of IP10 without naming them; whether this FM is one of them is the first SE37 / BAPI Explorer question (the same question as ISCHED_CALL_GENERATE). API_MAINTENANCEPLAN (StartMaintPlnSchedule and RestartMaintPlnSchedule, valid as of 2021 FPS01), MaintenancePlanERPScheduleRequestConfirmation_In and I_MAINTENANCEPLANSCHEDULE are not universe ids and not successors. enh:exit:IPRM0001 is not an xref: 'Optimizing the Scheduling' names IPRM0002 and IPRM0005, which are not in the universe. SAP Notes 2270078 and 2270107 are printed by the two Simplification List items and were not read (no sapNote field). Needs SE37.
- `fm:MEASUREM_DOCUM_RFC_SINGLE_001`: printed officially (its own FM page, identical in Maintenance Management 2025.001 and Plant Maintenance (PM) 6.18.latest; the APIs/BAPIs column of the Data Migration object PM - Measurement document, 2025.001). Repository corrections open: `data/function-intel.ts#MEASUREM_DOCUM_RFC_SINGLE_001` (inferred) lists 'POINT/READING/DATE' and a RETURN parameter, while the documented interface has MEASUREMENT_POINT, READING_DATE, READING_TIME and RECORDED_VALUE among 23 import parameters, 4 export parameters, 17 classic exceptions and no RETURN; fix function-intel from the page. The registry's requiresCommit 'yes' does not reflect the COMMIT_WORK parameter, with which the module triggers COMMIT WORK itself. Release status 'Released' and function group IMR0 are repository claims (no page read prints them). Discovery only, not evidence (github.com is not allowlisted): the Cloudification Repository files objectReleaseInfo_PCE2025_1.json and objectReleaseInfoLatest.json (saved by the MEASUREMENT_POINT_READ researcher, re-read by the writer) list MEASUREM_DOCUM_RFC_SINGLE_001 and _002 in function group IMR0 with state notToBeReleased and successor I_MEASUREMENTDOCUMENTTP_2 (PM-EQM-SF-MPC). That concerns the ABAP Cloud (developer extensibility) contract, not the classic On-Premise use the authored unchanged describes; an official page (for example a help.sap.com released-objects list) is needed before the record says it, and I_MEASUREMENTDOCUMENTTP_2 is not a universe id. SAP Note 2917243 is printed on the migration page and quoted in notes only. The record's depth stays L3 because tx:IK21 and tx:IK41 resolve in the id universe but have no transaction page (`resolvesInApp` false); they are sourced xrefs and stay.
- `fm:MEASUREMENT_POINT_READ`: no official record names the FM (8 / 5 / 2 hits, 4 quoted, 15 topics, every body read; the Maintenance Management 2025 FPS01 and Plant Maintenance (PM) EHP8 tables of contents, 1,122 and 646 nodes, hold no MEASUREMENT_POINT title; SIMPL 2025 / 2023 and What's New 2025 FPS01 / 2021 without it). The POINT input and 'Point detail' output are `inferred: true`. Minor drift: function-intel links IK03 only; blueprint and registry IK01, IK02, IK03, IK07 and IK08. tx:IK08 resolves in the id universe but has no transaction page. MEASUREM_POINT_RFC_SINGLE_002, API_MEASURINGPOINT, I_MeasuringPointData and W0030 are not universe ids and not successors. The Cloudification Repository files list neither MEASUREMENT_POINT_READ nor MEASUREM_POINT_RFC_SINGLE_002 (discovery only). Needs SE37.
- `fm:NOTIF_CAUSE_READ`: no official record names the FM (12 queries, 235 records, 186 topic-version pairs / 183 loios; NOTIF_CAUSE_READ, NOTIF_CAUSE, CAUSE_READ, QMUR and 'notification cause' in neither SIMPL list). The QMNUM/FENUM input and Causes output are `inferred: true`. IW67 conflict: the blueprint lists 'IW22; IW67' for QMUR and `data/tcode-catalog.ts` describes IW67 as 'Display Notification Items', while the official 'Linear Data in Reports' (2025.001) lists IW66 and IW67 as Tasks, as `data/tx-intel.ts` does; tx:IW67 is not an xref. The table:QMUR record presents I_MaintNotificationCauseData on the analytics side without the page's 'DataSource in Business Warehouse' restriction; recheck that record. The NOTIFCAUS table of BAPI_ALM_NOTIF_GET_DETAIL, BAPI_ALM_NOTIF_DATA_ADD and BAPI_ALM_NOTIF_DATA_MODIFY is repository-only (`data/bapi-enrichment.pm.ts`). I_MaintNotifItemCauseTP_3 and I_MaintNotificationCauseData are not universe ids. Needs SE37.
- Cross-record, method: search counts are run-dependent day-bounded measurements. Bodies were read through help.sap.com/http.svc/deliverableMetadata and http.svc/pagecontent (the MANIFEST row adopted with batch 13). The sc4sap MCP failed to connect in every session of this batch, the writer's included ('MCP error -32000: Connection closed' at session start), so no SE37 check confirms or contradicts any name, parameter, function group or release status.

## Writer-level normalisations applied
- The six drafts were extracted from the writer's task message in the agent transcript, not retyped; the emitted TypeScript round-trips to the merged JSON with sorted keys (6 of 6 identical; 91 fm records, 0 duplicate ids).
- `reviewer` dropped on MAINTENANCE_ITEM_READ and MAINTENANCE_PLAN_SCHEDULE (the other four carried none).
- Verdict premise checked: the MAINTENANCE_ITEM_READ auditor wrote that tables.ts and enhancements.ts use `reviewer`. Measured: 0 of 357 overlay records carry a `reviewer` field at record or evidence level; the grep hits are notes text that mentions the word. The narrower wording the verdict gives is still true, so it was applied; the sibling notes that say no record in data/verification/** carries the field (K_SETTLEMENT_RULE_READ, MEASUREM_DOCUM_RFC_SINGLE_001, MEASUREMENT_POINT_READ) are true and stay.
- `fm:MEASUREMENT_POINT_READ`, the optional fifth evidence entry: the writer re-ran the SAP_ERP search (the url comes back at 6.18.latest, loio 6d70b65334e6b54ce10000000a174cb4, date 2026-06-12), fetched both bodies through the content service (fallback false, 4,846 characters each, identical under cmp) and got HTTP 200 on the topic url. Edition `ecc` and release `6.18.latest` follow the sibling SAP ERP entry of MEASUREM_DOCUM_RFC_SINGLE_001 (same deliverable 61f8c51bfee94fa78c8835db685249eb) instead of the verdict's suggested `on-premise`: the source is SAP ERP and the Edition vocabulary has `ecc`. The claim names neither MEASUREMENT_POINT_READ nor a successor. The notes' measured evidence-block sentence now says five sources and three context entries (re-measured after writing: sources 5, conflicts 0, level sap_official_verified, needsVerification true, depth L2).
- `fm:MEASUREM_DOCUM_RFC_SINGLE_001` notes: the opening 'מה נבדק בפועל (2026-09-23).' became 'מה נבדק ב-2026-09-23 (בתיעוד הרשמי ובמאגר; לא בוצעה בדיקה חיה במערכת SAP).' Under the project's HQ labels 'נבדק בפועל' reads as a live SAP check (the reasoning of the MEASUREMENT_POINT_READ verdict); the notes describe documentation and repository checks and already say no live check was possible.
- Three shared Evidence consts hoisted (`K_SETTLEMENT_SIMPL_SCAN`, `MEASUREM_DOCUM_RFC_FM_PAGE`, `NOTIF_ITEM_CAUSE_READ`), so each status.source is the same object as its evidence entry (the drafts carried identical copies).
- `DATE23` used for every accessedAt and lastVerifiedAt of the six records.
- Em/en dashes added: 0 in authored prose; one en dash sits inside a verbatim official title ('Display Maintenance Item – Transaction', U+2013 as the SAP_ERP search record returns it). Cyrillic: 0. CERTAINTY_RE and PLACEHOLDER_RE: 0 hits over every claim, title, status text and note of the six records (checked although every record's level is official).
- Header: the batch-14 paragraph added.
- `test/evidence-schema.test.ts`: no change; functions graduated 2026-09-02 and is already in the graduated repoRef test. Follow-up outside this batch: the schema test's BPS now loads `data/best-practices/cross-processes-2.ts` (committed with aac20274), so the `bp:order-settlement-process` xref that batch 13 dropped from fm:K_COSTS_READ would resolve again (K_SETTLEMENT_RULE_READ carries it and passes); DATA_FILES in that test still omits `cross-processes-2.ts`.
- Gates: `./node_modules/.bin/tsc --noEmit` 0; `./node_modules/.bin/tsc --noEmit -p tsconfig.test.json` 0; `npm test` 212/212 pass; `eslint data/verification/functions.ts` 0; `validateRecords` over the six alone and over every overlay = 0 problems in the universe the schema and xref tests build (102 xrefs, 0 dangling). Coverage (functions, 142): L2 63 to 64, L3 39 to 38, verified 112 to 116, verification_required 20 to 16, conflict 10 to 10, s4-applicable 74 to 73 (K_SETTLEMENT_RULE_READ leaves the naming-rule `restricted`). Per id: K_SETTLEMENT_RULE_READ L3 repository_verified restricted to L2 sap_official_verified verification_required; MEASUREM_DOCUM_RFC_SINGLE_001 L3 repository_verified to L3 sap_official_verified (unchanged); the other four stay L2 verification_required, level verification_required to sap_official_verified (context evidence). Not committed.

---

# Functions batch 15 (2026-09-23): 6 drafts audited, 6 written, 0 refuted

Written by the single writer from the adversarial verdicts (no `fixedRecord` supplied; the listed downgrades applied to each researcher draft). The drafts were parsed byte for byte from the writer's own task message in the workflow transcript, not retyped. `fm:NOTIF_ITEM_READ`, `fm:QPK1_CATALOG_READ`, `fm:QPK1_CODE_TEXT_READ`: no downgrades (their verdict problems are informational). `fm:ORDER_TYPE_READ`: the English tail of evidence #2's sourceTitle replaced with the Hebrew wording the verdict gives. `fm:PM_NOTIFICATION_TYPE_READ`: status.he now reads 'מתועדת ב-help.sap.com רק בתיעוד Public Cloud (בגרסאות 2602 ו-2608).', notes (5) replaced with the verdict's paragraph (two Public Cloud VDM pages documented side by side in 2602.500 and 2608.500, not one replacing the other), the optional count caveat appended to evidence #2, `reviewer` dropped. `fm:PRIORITY_DETERMINE`: notes (8) now calls table:QMEL, table:QMSM, table:T003O and table:MPOS navigation anchors of the repository (the pages name no table), the optional BAdI sentence appended to recommendedAction with its snippet-only caveat in notes (7), `reviewer` dropped.

## refuted
- (none in this batch: all 6 audited drafts survived verification and were written.)

## conflicts
- `fm:NOTIF_ITEM_READ`: no official record names the FM (17 queries, 314 records, 243 topic-version pairs; 190 loios counted by the 32-hex base, 196 raw loio values because six SAP_ERP records carry suffixed loios such as `-17` and `-317`; 15 bodies read in full, none contains NOTIF_ITEM_READ or a technical name ending in _READ; NOTIF_ITEM_READ, NOTIF_ITEM, ITEM_READ, QMFE, NOTITEM and 'notification item' in neither SIMPL list). The QMNUM input and Items output are `inferred: true`. IW66 conflict: the blueprint lists 'IW22; IW66' for QMFE and the registry inherits both; `data/tx-intel.ts` describes IW66 as a notification-items list and IW68 as an activities list, and `data/tcode-catalog.ts` has IW66 'Change Notification Items', IW67 'Display Notification Items', IW68 'Change Notification Tasks' and IW69 'Display Notification Tasks'; the official 'Linear Data in Reports' (2025.001, loio 2807244d6b50403682bfb72dce26e428) lists 'Notification Item IW68, IW69', 'Activities IW64, IW65' and 'Tasks IW66, IW67'. So tx:IW66 is not an xref and tx:IW68 and tx:IW69 are. Follow-ups in other records, not changed by this batch: `table:QMFE` (data/verification/tables.ts:9577) recommends current work 'ב-IW22 וב-IW66' and carries tx:IW66; `cds:I_MaintNotificationItem` (data/verification/cds.ts:876) carries tx:IW66 and presents the view also 'ולדיווח אנליטי על פריטי הודעה', without the VDM page's BW-only limit ('We recommend that you only use it for this purpose'). Repository spelling drift in an unverified layer: `data/bapi-enrichment.pm.ts` spells the items table NOTITEM in GET_DETAIL, CREATE, DATA_ADD and DATA_DELETE and NOTIFITEM in DATA_MODIFY.
- `fm:ORDER_TYPE_READ`: no official record names the FM (29 queries; 95 topic bodies read in full, none contains ORDER_TYPE_READ or T003O; ORDER_TYPE_READ, ORDER_TYPE, T003O and T003P in neither SIMPL list; ORDER_TYPE_READ, ORDER_TYPE, T003O and I_OrderType in neither What's New 2025 FPS01 nor 2021). The AUART input and Config output are `inferred: true`. Order-type examples drift, which the record's notes (11) say is reported separately: the official 'Order Types' page (2025.001 and 6.18.latest, loio 62c7b65334e6b54ce10000000a174cb4) lists 'PM01 for maintenance orders (PM) PM04 for refurbishment orders (PM) PM06 for investment orders (PM) SM01 for service orders (CS)', while `bp:maintenance-order-process` (data/best-practices/pm-processes-2.ts:38, :123, :303) gives 'PM03 שיפוץ או השקעה', and section 4.3.2 of the PMU textbook (data/library/pmu-textbook/ch04.ts:597) gives 'PM03 (Refurbishment/פרויקט), PM04 (כיול/אחר)' while the scenario of the same section (:602) uses PM04 for a pump refurbishment. Needs the owner of those texts; the textbook is library content and was not touched. table:T399X and table:TCO01 were left out because their table records document internal conflicts about their identity and their tie to T003O. For `table:T003O` only: the public preview of KBA 2321958 ('T003O and T399X have different AUTYP') states 'Order type (AUTYP) in table T003O is different, than AUTYP in table T399X' with 'SAP S/4HANA, on-premise' in its environment (a preview read on a non-allowlisted host, not evidence). Discovery only (github, not an evidence channel): the Cloudification Repository lists I_ORDERTYPE, I_ORDERTYPEDETAILS, I_ORDERTYPETEXT and I_ORDERTYPEVH as released (PP-VDM) and neither ORDER_TYPE_READ nor T003O.
- `fm:PM_NOTIFICATION_TYPE_READ`: no official record names the FM (23 queries, 406 records, 328 topic-version pairs; the bodies of all 71 name-query topics read, none contains the name, NOTIFICATION_TYPE or TQ80; PM_NOTIFICATION_TYPE_READ, NOTIFICATION_TYPE_READ, NOTIFICATION_TYPE, TQ80, I_PMNotif and 'notification type' in neither SIMPL list; What's New 2025 FPS01 and 2021 without the name, TQ80 there only as view V_TQ80_E in 2021). The QMART input and 'Config' output are `inferred: true`. Audit correction: the researcher's third conflict (a change between 2602.500 and 2608.500) is not recorded, because both Public Cloud VDM pages titled 'Maintenance Notification Types' (loio b3da4be44475463a8b6b4b6286a600c8, I_MAINTENANCENOTIFICATIONTYPE, DIMENSION; loio aeb7eab1db454095b23ec27891da0664, I_PMNOTIFICATIONTYPE) are documented side by side in both releases; researcher gap 4 now reads 'only in Public Cloud documentation (2602 and 2608)'. Open: whether I_PMNotificationType exists on-premise, its release status and its fields (ADT or View Browser in the target system). Consistency follow-up: `table:TQ80` (data/verification/tables.ts:13791) recommends 'לקריאת ההגדרה בקוד להעדיף את מודול הפונקציה PM_NOTIFICATION_TYPE_READ' (with an SE37 caveat), while this record advises no new code on the module before SE37 and I_PMNotificationTypeStdVH for value lists; align the TQ80 wording in the next pass. Unresolved repository conflict, as in table:TQ80: the blueprint gives 'OIAL (פריסת מסך)' for the notification type and `data/table-enrichment.ts:1097` gives 'סוגי הודעה (OIM11/SPRO) = TQ80.'; tx:OIAL and tx:QCC0 stay as registry navigation anchors. The official 'Get Details of Assignment of Detection Method Profile to Notification Type' page (loio a0dce176af054848b83301574dde5c02) shows an example path (MaintNotifDetCode) that differs from its response context ($metadata#MaintNotifTypeDetProfile); not resolved. Count drift on the same day: exact name in SAP_ERP 20 (researcher), 19 (auditor), 16 (writer); partial name NOTIFICATION_TYPE_READ in On-Premise 7, 6, 7; On-Premise 21 and Cloud 2 in all three runs; no title or snippet names the module in any run.
- `fm:PRIORITY_DETERMINE`: no official record names the FM (19 queries, 331 records, 249 topic-version pairs / 238 loios; 248 topic bodies read, none contains PRIORITY_DETERMINE, T356 or TQ80; PRIORITY_DETERMINE, any PRIORITY_ string, T356, ARTPR and 'priority type' in neither SIMPL list). The PriorityType/Priority input and Dates output are `inferred: true`. Repository contradictions: the module's table is TQ80 in the blueprint and T356 in `data/function-intel.ts`; the priorities Customizing transaction is 'SPRO; OIOH' in the blueprint config sheet and 'OIP1 (עדיפויות)' in the academy lesson summary; T356, OIP1 and OIOH are verified against no official source (T356 appears only as a keyword of a KBA preview) and are not in the id universe. `data/academy/lessons/pm-generated.ts:520` puts the module in an objects block at trust 'verified-docs' with a generic SAP Help Portal source for Plant Maintenance (S/4HANA); no page for the name was found (the CARO_TASKLIST_READ / CR_WORKCENTER_READ pattern). BAdI EAMS_LACD_CALCULATION ('BAdI: Calculation of the Final Due Date', What's New 2023 FPS03) is cited in the recommendation from its search snippet only (body unread); it is not in `data/exits.ts` or `data/enhancements.ts`, so it has no page and no xref, and is a candidate for the exits catalog. KBA previews 2771171, 2696247, 2171195 and 2182324 name no module; not evidence, no kba field.
- `fm:QPK1_CATALOG_READ`: no official record names the FM (28 queries, 501 records, 403 topic-version pairs / 363 loios, no QPK1 in any title or snippet; the tables of contents of eight deliverables, 4,069 nodes, hold no QPK1 title; 19 bodies read, none contains QPK1; QPK1, CATALOG_READ, QPCD, QPGR, TQ15, QS41, QS51, QS61, 'catalog type' and 'catalog profile' in neither SIMPL list). The 'Catalog type' input and 'Catalog' output are `inferred: true`. The authored verification_required replaces a derived `restricted` (repository_verified, L3) that came from `looksInternal` in `lib/bapi-registry.ts` (a digit before the underscore in QPK1_), not from a source; depth drops L3 to L2 on purpose. Repository drift: `data/academy/lessons/pm-generated.ts:550` (pm-calibration objects block) labels the module trust 'verified-docs' with a Plant Maintenance Help Portal source the searches did not reproduce; function-intel links QS41 and TQ15, the blueprint and registry QS51 and QS61; QS61 is material specification maintenance per `data/tx-intel.ts` and no official page ties it to catalog reads, so it is not an xref here (table:QPCD already flags 'QS51; QS61' as an internal gap); TQ15 is not in the universe. The same naming rule still marks `fm:QPK1_CODEGROUP_READ` and `fm:QPK1_INSPCHAR_READ` restricted (measured after this batch: repository_verified, L3); candidates for a later batch. Discovery only (github): the Cloudification Repository lists QPK1_CODE_TEXT and QPK1_GP_CODE_ARRAY_SELECTION (function group QPK1, notToBeReleased, CDS successors) and I_CONFIGURABLECODESCATALOG as released, and none of QPK1_CATALOG_READ, QPK1_CODE_TEXT_READ or QPK1_CODEGROUP_READ; membership of QPK1_CATALOG_READ in group QPK1 is unverified.
- `fm:QPK1_CODE_TEXT_READ`: no official record names the FM (35 queries, 555 records, 462 topic-version pairs / 422 loios; no saved record names a module starting QPK1_, SAPLQPK1 or LQPK1; QPK1, QPK1_CODE, CODE_TEXT_READ, SAPLQPK1, QPCD, QPCT, QPGR, QPGT, QIRF_ and ConfigurableCodes in neither SIMPL list, CODE_TEXT only inside 0EH_INJCODE_TEXT). Name uncertainty: the public previews of KBA 2650630 (carried as evidence at supported_secondary_source with its me.sap.com/notes url) and KBA 3542734 print QPK1_CODE_TEXT, with SAPLQPK1 and LQPK1U05, not QPK1_CODE_TEXT_READ; an SE37 check should look for both names. The authored verification_required replaces the derived `restricted` from the same naming rule; depth L3 to L2 on purpose. Repository drift: function-intel links QS41, the blueprint and registry QS51 and QS61 (QS61 is 'Maintain Material Specification' in `data/tx-intel.ts` and `data/tcode-catalog.ts`); `data/academy/lessons/qm-generated.ts:175` labels the module 'verified-docs' with a Quality Management Help Portal source the searches did not reproduce; QPCT, which the repository records as the code-text table, is not in the universe and no official source defining it was found (only KBA previews mention it). Cross-record inconsistency to settle in the next pass: this record carries tx:QS61 as a registry navigation anchor, while `fm:QPK1_CATALOG_READ` leaves it out for lack of an official tie.
- Cross-record, method: search counts are run-dependent, day-bounded measurements (see the PM_NOTIFICATION_TYPE_READ drift above). Bodies were read through help.sap.com/http.svc/deliverableMetadata and http.svc/pagecontent (the MANIFEST row adopted with batch 13). The sc4sap MCP failed to connect in every session of this batch, the writer's included ('MCP error -32000: Connection closed' at session start), so no SE37 check confirms or contradicts any name, parameter, function group or release status. The GitHub Cloudification Repository files and the userapps.support.sap.com KBA previews named in notes are discovery reads on non-allowlisted hosts, not evidence (the one KBA carried as evidence, 2650630, cites its me.sap.com/notes url).

## Writer-level normalisations applied
- The six drafts were parsed (JSON.parse) from the writer's own task message in the workflow transcript, not retyped; the emitted TypeScript round-trips to the merged JSON with identical key order (6 of 6; 97 fm records, 0 duplicate ids).
- `reviewer` dropped on PM_NOTIFICATION_TYPE_READ and PRIORITY_DETERMINE (the other four carried none). Measured: 0 records in data/verification/** carry a `reviewer` field, so the notes of ORDER_TYPE_READ and QPK1_CODE_TEXT_READ that say the record carries none by convention stay true. The third PRIORITY_DETERMINE downgrade targets a sentence of the researcher's summary; the record carries no such sentence, so nothing in it changed.
- `fm:PM_NOTIFICATION_TYPE_READ`: the verdict's conflicts and gaps items address the researcher's summary, not record fields; they are applied in the conflicts entry above. The auditor's ready-made file (scratchpad pmnt/rec.fixed.json) matches the written record except for the two further downgrades (evidence #2 count caveat, `reviewer` dropped), and the researcher's working file (pmnt/rec.json) equals the draft in the task message. The optional count caveat was applied verbatim; the writer's own re-run (SAP_ERP 16, On-Premise 21, Cloud 2, partial 7) is recorded above, not in the record.
- `fm:PRIORITY_DETERMINE`, optional BAdI downgrade: applied after the writer re-ran scripts/sap-help-search.mjs on 2026-09-23. The record 'BAdI: Calculation of the Final Due Date' (What's New in SAP S/4HANA and SAP S/4HANA Cloud Private Edition 2023 FPS03, versionId 2023.003, loio 2050f4c5e9c448b1bf5bba0901c8c066) came back with the snippet 'The new Business Add-In (BAdI) BAdI: Calculation of the Final Due Date ( EAMS_LACD_CALCULATION) allows you to implement your customer-specific …', and 'App Extensibility: Manage Maintenance Orders (Key User)' (Maintenance Management, 2025.001, loio 0b67655d18b7494baa813f1fa6caffb2) with 'You can use this BAdI to implement your customer-specific logic for the calculation of the final … due date'. The verdict's gap sentence went into notes (7) (the record has no gaps field), and EAMS_LACD_CALCULATION was added to the notes (8) list of names outside the id universe (checked: not in data/exits.ts, data/enhancements.ts or the route manifest).
- Six shared Evidence consts hoisted (`MAINTNOTIF_OPS_ITEM_READ`, `ORDER_TYPE_VDM`, `PM_NOTIF_TYPE_VH_WN2023`, `FINAL_DUE_DATE_PRIORITY`, `QM_IDI_RFC_DOWNLOAD`, `QPK1_CODE_TEXT_SIMPL_SCAN`), so each status.source is the same object as its evidence entry (the drafts carried identical copies).
- `DATE23` used for every accessedAt and lastVerifiedAt of the six records.
- Em/en dashes: 0. Cyrillic: 0. CERTAINTY_RE and PLACEHOLDER_RE: 0 hits over every string of the six records (checked although every record's level is official). The one arrow '←' in QPK1_CODE_TEXT_READ notes is a verbatim quote of data/table-enrichment.ts.
- Header: the batch-15 paragraph added.
- `test/evidence-schema.test.ts`: no change; functions graduated 2026-09-02 and is already in the graduated repoRef test.
- Gates: `./node_modules/.bin/tsc --noEmit` 0; `./node_modules/.bin/tsc --noEmit -p tsconfig.test.json` 0; `npm test` 212/212 pass; `eslint data/verification/functions.ts` 0; `validateRecords` over the six alone and over all 97 fm records: 0 problems in the universe the schema test builds (100 xrefs, all exact ids); the 18 distinct evidence URLs of the six records returned HTTP 200 at writing time. Coverage (functions, 142): L2 64 to 66, L3 38 to 36, verified 116 to 120, verification_required 16 to 12, conflict 10 to 10, s4-applicable 73 to 71 (the QPK1 pair leaves `restricted`), edition-specific 3 to 4 (the report counts a record with any private- or public-cloud source; PM_NOTIFICATION_TYPE_READ carries the Public Cloud 2608 VDM page while its status stays on-premise). Not run: `next build` (not in this batch's gate list; its prebuild regenerates data/ai-tree/*.json).

---

# Functions batch 16 (2026-09-23): 6 drafts audited, 6 written, 0 refuted

Written by the single writer from the adversarial verdicts (no `fixedRecord` supplied; the listed downgrades applied to each researcher draft). The drafts were parsed byte for byte from the writer's own task message in the workflow transcript, not retyped, and are equal to the six researcher results in the workflow journal (the verdicts equal the journal's verify results too). `fm:QPK1_CODEGROUP_READ`: both optional downgrades applied (the Code Group body length now reads 'כ-1,650 תווים'; the Simplification List evidence is one shared const). `fm:RESERVATION_READ`: `reviewer` dropped; the second downgrade (delete the fifth `conflicts` entry) addresses the researcher's summary, not a record field, so that entry is simply not copied below. `fm:SCHEDULING_HISTORY_READ`: status.he no longer names the CDS view I_MAINTENANCEPLANSCHEDULE as a documented read channel (it stays in notes and recommendedAction, marked for verification), notes (9) no longer print the two KBA numbers and notes (2) no longer print the SAP Note number, `reviewer` dropped. `fm:SERIAL_NUMBER_CREATE`: a simplification_item evidence entry now backs the OBJK sentence of recommendedAction, the ERP evidence is split so the 'Working With Serial Numbers' quote carries its own URL, the Manage Material Serial Numbers page URL is written into the What's New evidence, and recommendedAction points BAPI_EQUI_CREATE to the record `fm:BAPI_EQUI_CREATE`. `fm:SERNR_ADD_TO_DOCUMENT`: the optional wording fix applied (the MIGO FAQ page names MB_DOCUMENT_PREPARE in its lead sentence and writes its sample code in MB_DOCUMENT_BEFORE_UPDATE). `fm:STATUS_CHANGE_EXTERN`: the order and notification page claims are bounded to the SAP Web UI for Plant Maintenance (business functions LOG_EAM_SIMPLICITY_5 and LOG_EAM_SIMPLICITY_6), tx:IW32 and tx:IW22 are described as navigation anchors, the recommended comparison is against the documented status-profile rules, and `reviewer` dropped (writer normalisation, see below).

## refuted
- (none in this batch: all 6 audited drafts survived verification and were written.)

## conflicts
- `fm:QPK1_CODEGROUP_READ`: no official record names the FM (23 queries, 394 records, 274 topic-version pairs, 252 loios; 23 bodies of 18 topics read in full, none contains QPK1 or CODEGROUP_READ; QPK1, QPK1_CODEGROUP, CODEGROUP_READ, CODEGROUP, QPGR, QPCD, QS41, ConfigurableCodes, I_DefectCodeGroup and BAPI_QPGR in neither SIMPL list). The Catalog/CodeGroup input and 'Code group' output are `inferred: true`. The authored verification_required replaces the derived `restricted` (repository_verified, L3) that `looksInternal` in `lib/bapi-registry.ts` set from the digit in the QPK1_ prefix; depth L3 to L2 on purpose. Transaction drift: function-intel links QS41, the blueprint (PM:QPGR tcodes 'QS51; QS61', s4AltTcode 'QS51 (זהה)') and the registry QS51 and QS61; the official pages read give QS41 and QS42 as edit and display of code groups and codes and QS51 as 'Edit Selected Sets'; QS61 is material specification in `data/tcode-catalog.ts` and `data/tx-intel.ts` with no official tie to code groups, so tx:QS61 is not an xref (the `fm:QPK1_CATALOG_READ` treatment), while `fm:QPK1_CODE_TEXT_READ` still carries it: the batch-15 cross-record inconsistency stays open. `data/tcode-catalog.ts` labels QS42 'Display Catalog' and QS43 'Display Code Groups', while the official migration pages give QS42 as 'Display Code Groups' and SIMPL item 9.6.2 (2025) / 34.2 (2023) gives QS42 as the alternative to QS45 'Display catalog'; QS43 was not checked. Open: the on-premise release status of I_ConfigurableCodesCodeGroup (its VDM page prints none and the On-Premise What's New 2023 'Objects Released for Developer Extensibility in Quality Management' does not list it; only the Cloudification Repository, not an evidence host, marks it released); the Catalogs (QM-PT-BD-CAT) page was not re-read (help.sap.com 403 rate limit), so its sentence is unused; the preview of KBA 2823182 was unreadable. Discovery-only precision for batch 15: `fm:QPK1_CATALOG_READ` notes (7) and the batch-15 conflicts line can be read as QPK1_CODE_TEXT and QPK1_GP_CODE_ARRAY_SELECTION being listed in both Cloudification Repository files; measured on the same md5s, `objectReleaseInfoLatest.json` lists only QPK1_CODE_TEXT (QM-PT-BD-CAT-2CL) and `objectReleaseInfo_PCE2025_1.json` lists both (QM-PT-BD-CAT); a wording check on the sibling, not evidence. Auditor measurement: the Code Group body is 1,657 characters after whitespace collapse (researcher 1,655), hence the rounded figure. Remaining naming-rule candidate in the QPK1 family: `fm:QPK1_INSPCHAR_READ` (measured after this batch: `restricted`, repository_verified, L3).
- `fm:RESERVATION_READ`: no official record names the FM (22 queries, 350 records, 271 topic-version pairs, 236 loios; 21 bodies read in full; 3 domain-restricted web searches; RESERVATION_READ, RESERVATION_, BAPI_RESERVATION, RESERVATION_GETDETAIL and MBRE in neither SIMPL list). The RSNUM input and items output are `inferred: true`. Repository against official: `data/bapi-enrichment.sweep.ts#RESERVATION_READ` points to BAPI_RESERVATION_GETDETAIL without the suffix, while the EHP7 What's New page (loio da262953587d2c3ee10000000a423f68) and the Public Edition 2608 BAPIs/IDocs page (loio 2cf48091d5864284ac4541b86a8737fd) print only BAPI_RESERVATION_GETDETAIL1 and BAPI_RESERVATION_GETITEMS1; not asserted that the unsuffixed name does not exist; correct the repository reference after SE37. `data/academy/lessons/pm-generated.ts` (pm-material) puts the module in an objects block at trust 'verified-docs' with an SAP Help Portal source, and no Help record names it. Stale note in another record: `table:RESB` (data/verification/tables.ts) says RESB is absent from data/sapData.ts (0 matches), but data/sapData.ts is a thin export and data/sapData.pm.ts carries PM:RESB (topic 9); recheck that note. Terminology drift: 'הזמנת רכיבים' / 'הזמנת מלאי' (blueprint, function-intel), 'רזרבציה' (academy, bp:material-staging-and-reservation), 'שמורה' (sweep, fm:BAPI_RESERVATION_CREATE1); the record uses 'שמורה'. Open: whether Retrieve Reservation Document also returns reservations created automatically from maintenance-order components; on-premise availability, parameters and RFC flag of GETDETAIL1 and GETITEMS1 (printed only for ECC 6.0 EHP7 and Public Edition 2608); the Fiori app ID of 'Reservation' (MM-IM 2025.001) is not printed; F4839 and F5601 are not in data/fiori/apps.ts; 'Operations for Maintenance Order (Version 2)' was not read; SAP Note 2229126 is quoted in notes as printed in the SIMPL item, not read, no sapNote field. Search drift: the auditor's re-run of the exact-name On-Premise query returned 6 records ('Setting of Reserves', 'Real-Time Consolidation' 1709 and four eDocument pages of deliverable 8a5001633f284e158df2e917a9aead8f); the counts (6, and 4 quoted) and the empty snippets match the draft and no title or snippet names the FM, but the draft's description of the page kinds ('Claim', Malaysia localization) could not be confirmed title by title. Not contradicted and not a downgrade, so the claim was kept as written.
- `fm:SCHEDULING_HISTORY_READ`: no official record names the FM (11 name queries, 173 records, 124 topics whose bodies were all read; the raw HTML of all 258 bodies re-scanned; 2 domain-restricted web searches; SCHEDULING_HISTORY_READ, SCHEDULING_HISTORY, HISTORY_READ, SCHEDULING_ and MHIS at 0 in both SIMPL lists against a positive control of RISTRA20 and IP30H). The WARPL input and History output are `inferred: true`. SAP's own table in 'Scheduling and Automatic Scheduling' (loio 2d396b50389ff015e10000000a44176d, 2025.001 and 6.18.latest) pairs IP10 with the Deadline Monitoring menu path and IP30 with Schedule, which reads reversed, so the record maps no call-history screen to a transaction code. Repository drift: function-intel links IP10 only, the blueprint and registry IP10 and IP30 (the transactions of the MHIS row); the module page shows the S/4 header 'אמת ב-S/4.' from FUNCTION_INTEL.s4 without a source; the blueprint Fiori cell 'Schedule Maintenance Plans (אמת ID)' stays undecided (the official app that lists the calls is Manage Maintenance Plans, F5325; not written as a fix of the cell); the MHIS field list (WARPL, ABNUM, NTERM, HORIZ) is unverified against an official source (see table:MHIS). Open: existence and release status of I_MAINTENANCEPLANSCHEDULE and C_MaintPlanSchedgOvwQuery (their VDM pages print none); the 2025.001 versions of the API_MAINTENANCEPLAN pages are not returned by the search service (bodies read through the content service, URLs cited at 2023.latest).
- `fm:SERIAL_NUMBER_CREATE`: the repository contradicts itself about whether the name exists: `data/bapi-enrichment.sweep.ts` marks it invalid-name at confidence high with the QA note 'אומת מול מקורות SAP רשמיים: שם זה אינו אובייקט סטנדרטי' (source: the sapdatasheet.org mirror, no URL), while `data/function-intel.ts` (inferred), the PM blueprint (PM:OBJK funcs) and `data/troubleshooting-ext2.ts#equipment-serial-sync` present it as an existing module; written as repository evidence at conflicting_sources. The derived `not_applicable` ('השם אינו אובייקט SAP תקני', repository_verified, L3) asserted non-existence without an official source and is replaced by the authored verification_required. ITOB_SERIALNO_CREATE_SINGLE, the sweep's alternative, appears in no official source checked and is not in the id universe. Cross-record: `table:OBJK` (data/verification/tables.ts) is authored `unchanged` and says no Simplification Item concerns OBJK, while SIMPL 2025 FPS01 item 4.1.11 (pp. 83-84) and 2023 FPS03 item 29.1 (pp. 715-716), 'S4TWL - Object List Number Field Length Extension', name table OBJK, domain OBJKN and data element OBJKNR (INT4 to INT8) and require adapting custom code; `audit/master-completion/simpl-tables-index.json` already maps OBJK to both items. Reopen table:OBJK, its blueprint S/4 and SUM cells included (not changed here). table:OBJK names only F7617 for Manage Material Serial Numbers, while the same loio at 2023.latest prints F6879 (the app ID is release-dependent). Open: the On-Premise Business Accelerator Hub page of API_MATERIALSERIALNUMBER was not located (only CE_API_MATERIALSERIALNUMBER_0001, a Cloud shell); the Fiori library page was not fetched; the Cloud 2608 'Serial Number Apps' page (F7551 'Create Material Serial Number') was seen, not read, not recorded; F7617, F6879, tx:IQ04, table:SER01, EAM_CHECK_MATLSERNUMBER and IQSM0008 are not in the id universe.
- `fm:SERNR_ADD_TO_DOCUMENT`: no SAP record prints the name (help.sap.com search in four scopes, domain-restricted web search, both SIMPL lists, 13 bodies read). SAP-hosted Support Content prints sibling names for the same job by document type: SERNR_ADD_TO_LS (delivery), SERNR_ADD_TO_PP (production order, called 'an unreleased FM') and SERNR_ADD_BAPI_MM (material document); the public preview of KBA 3700073 prints SERNR_ADD_TO_AU. An SE37 check should cover all five names. Function group IPW1 rests on the repository only (the official customer exits EXIT_SAPLIPW1_* tie to IPW1 by ABAP naming inference, not by any page). The derived `restricted` (S/4 On-Premise support 'yes', repository_verified, L3) rested on the sweep's mirror-sourced 'confirmed real but INTERNAL'; replaced, depth L3 to L2 on purpose. Same table:OBJK cross-record finding as above. Repository drift: function-intel links OBJK and SER01 (per the official archiving page the delivery serial-number header), while its test scenario is a goods-issue document and the blueprint says 'שיוך סריאל לתעודת מלאי' (the goods-movement header is SER03 on the same page).
- `fm:STATUS_CHANGE_EXTERN`: no official record names the FM (33 queries, 591 records, 490 topic bodies read in full, none contains the name; 4 domain-restricted web searches; not in either SIMPL list). The derived `unchanged` ('אומת מול תיעוד SAP', repository_verified, L3) rested on the defaults `verified()` in `data/bapi-enrichment.sweep.ts` sets (stability Released, ECC and S/4 support 'yes', RFC 'no' from a missing flag) over a mirror-sourced sweep entry; replaced by the authored verification_required. Precedent is not uniform: `fm:EQUIPMENT_TEXT_READ` (batch 12), also a mirror-sourced sweep entry that no official record prints, keeps its derived `unchanged` (measured after this batch: sap_official_verified, L4), while batches 13 to 16 author verification_required; align it in a later pass (not changed here). Repository inconsistency, not about existence (no conflictingEvidence): function-intel says 'סטטוס משתמש/מערכת', links JSTO and lists a RETURN output; the blueprint, registry and sweep say user status and link TJ30. Tension with `table:JEST`, whose recommended action advises working through STATUS_READ and STATUS_CHANGE_EXTERN without an official source for the name, while this record advises no new build on the module before SE37; documented in notes, not resolved. Open: whether BAPI_ALM_ORDER_MAINTAIN or the notification BAPIs accept a user status (the SCN blogs that SIMPL item 4.1.4 points to are on blogs.sap.com, off the allowlist, unread); whether the order and notification services carry a user-status entity not shown in the help.sap.com pages read (Hub pages are shells); SAP note 2148810 (printed in the order and notification pages) not read, no sapNote field.
- Cross-record, method: search counts are run-dependent, day-bounded measurements (see RESERVATION_READ above). Bodies were read through help.sap.com/http.svc/deliverableMetadata and http.svc/pagecontent. The sc4sap MCP failed to connect in every session of this batch, the writer's included ('MCP error -32000: Connection closed'), so no SE37 check confirms or contradicts any name, parameter, function group or release status. Note and KBA numbers in notes are handled unevenly by the audits: the SCHEDULING_HISTORY_READ audit removed numbers that no cited evidence source or repoRef prints, while the audits of QPK1_CODEGROUP_READ (11 KBA numbers from public previews), SERNR_ADD_TO_DOCUMENT (KBA 3700073) and RESERVATION_READ (SAP Note 2229126, printed in the SIMPL item named only in notes) kept theirs, as batches 14 and 15 did. The written records follow each verdict; settle one rule in a later pass (the MANIFEST allows a number that appears verbatim in an official public page and forbids one typed from memory).

## Writer-level normalisations applied
- The six drafts were parsed (JSON.parse) from the writer's own task message in the workflow transcript, not retyped; the emitted TypeScript round-trips to the merged JSON with identical key order (6 of 6; 103 fm records, 0 duplicate ids).
- `reviewer` dropped on RESERVATION_READ and SCHEDULING_HISTORY_READ (their downgrades) and on STATUS_CHANGE_EXTERN, whose verdict does not list it: a writer normalisation to the house convention (0 records in data/verification/** carry a `reviewer` key, measured), and the value named the record a 'researcher draft'. The other three drafts carried none. The SCHEDULING_HISTORY_READ notes sentence saying the draft's reviewer field is removed on writing stays true.
- `fm:SERIAL_NUMBER_CREATE`, required downgrade: applied by adding the evidence entry (the recommendedAction sentence stays). The claim was written by the writer from the verdict's specification and every quote was re-checked against a local SIMPL_OP2025.pdf with the recorded md5 (c1ccf8ebcd92d51fdc80e4b4873f3b73; qpdf 1,514 pages; 'Document Version: 1.36'); the official URL answered HTTP 200 with content-length 10,585,218 at writing time; SERIAL_NUMBER_CREATE, ITOB_SERIALNO and API_MATERIALSERIALNUMBER 0 hits, BAPI_ 121. Optional split applied: 'Creating an Individual Serial Number | Logistics (SAP ERP 6.0 EHP8)' keeps the ae0eb quotes, and the new 'Working With Serial Numbers | Logistics (SAP ERP 6.0 EHP8)' entry carries the 900eb URL (the search record returns that exact title, deliverable Logistics, 6.18.latest, loio and URL; HTTP 200) with the 'incomplete equipment master record' quote. Checked in the researcher's saved content-service text: the ERP 6.18.latest and 2025.001 bodies of 900eb are identical after the id/build header and hold 258 body words (the auditor's 260 counts the two header tokens). Optional app URL written in words into the What's New evidence (the search record returns loio 03afe500cc8e48ceaae016a216143e52 under Maintenance Management 2025.001; HTTP 200; the saved bodies print 'App ID: F7617' at 2025.001 and 'App ID : F6879' at 2023.latest). Evidence order: API, BAdI What's New, Creating, Working With, SIMPL item, repository (4 entries to 6).
- `fm:SERNR_ADD_TO_DOCUMENT`: the optional wording fix was checked against the saved Support Content body (lead sentence 'you can use method MB_DOCUMENT_PREPARE of BADI MB_DOCUMENT_BADI', sample code 'METHOD IF_EX_MB_DOCUMENT_BADI~MB_DOCUMENT_BEFORE_UPDATE').
- `fm:STATUS_CHANGE_EXTERN`: the verdict's sentence ('after the first quoted sentence') was inserted after the sentence that carries the four quotes (it ends '... User Status for Orders.'), not inside the quote list, so the quotes and the Hebrew grammar stay intact; its wording was checked against the saved content-service bodies of loio 93277455afe50c30e10000000a441470 (2025.001 and 6.18.latest: 'There is an improved UI for doing this on the SAP Web UI for Plant Maintenance', LOG_EAM_SIMPLICITY_5 for the header, LOG_EAM_SIMPLICITY_6 for operations, the 'Set User Status pushbutton' paragraph that holds the grayed-out sentence, and the quickview). evidence[2] and status.source are one const, so they cannot drift.
- Six shared Evidence consts hoisted (`QPK1_CODEGROUP_SIMPL_SCAN`, `RESERVATION_DOC_RETRIEVE`, `MPLAN_CALL_HISTORY`, `MATERIAL_SERIAL_NUMBER_API`, `SERNR_OBJK_SIMPL_SCAN`, `USER_STATUS_ORDER_UI`), so each status.source is the same object as its evidence entry (the drafts carried identical copies; checked equal before hoisting).
- `DATE23` used for every accessedAt and lastVerifiedAt of the six records.
- Em/en dashes: 0. Cyrillic: 0. CERTAINTY_RE and PLACEHOLDER_RE: 0 hits over every string of the six records (SERIAL_NUMBER_CREATE sits at conflicting_sources, so the certainty rule applies to it).
- Header: the batch-16 paragraph added.
- `test/evidence-schema.test.ts`: no change; functions graduated 2026-09-02, is already in the graduated repoRef test, and FOUNDATION_RECORDS holds only the objects catalog.
- Gates: `./node_modules/.bin/tsc --noEmit` 0; `./node_modules/.bin/tsc --noEmit -p tsconfig.test.json` 0; `npm test` 212/212 pass; `eslint data/verification/functions.ts` 0; `validateRecords` over the six alone, over all 103 fm records and over every overlay: 0 problems in the universe the schema test builds (90 xrefs, all exact ids); 34 distinct URLs (evidence url fields and URLs written inside claims and notes) answered HTTP 200 (the two SIMPL PDFs 206 to a range request). Coverage (functions, 142): L2 66 to 70, L3 36 to 32, verified 120 to 121, verification_required 12 to 10, conflict 10 to 11, s4-applicable 71 to 68, edition-specific 4 to 5 (the report counts a record with any private- or public-cloud source; RESERVATION_READ carries the Public Edition 2608 BAPIs/IDocs page while its status stays on-premise). Per id: QPK1_CODEGROUP_READ L3 repository_verified restricted to L2 sap_official_verified verification_required; RESERVATION_READ and SCHEDULING_HISTORY_READ stay L2 verification_required, level verification_required to sap_official_verified (context evidence); SERIAL_NUMBER_CREATE L3 repository_verified not_applicable to L2 conflicting_sources verification_required; SERNR_ADD_TO_DOCUMENT L3 repository_verified restricted to L2 sap_official_verified verification_required; STATUS_CHANGE_EXTERN L3 repository_verified unchanged to L2 sap_official_verified verification_required. No other id changed. Not run: `next build` (not in this batch's gate list; its prebuild regenerates data/ai-tree/*.json). Not committed.

---

# Functions batch 17 (2026-09-23): 6 drafts audited, 6 written, 0 refuted

Written by the single writer from the adversarial verdicts (no `fixedRecord` supplied; the listed downgrades applied to each researcher draft). The drafts were parsed byte for byte from the writer's own task message in the workflow transcript, not retyped, and are equal to the six researcher results in the workflow journal (the verdicts equal the journal's verify results too). `fm:STATUS_OBJECT_CREATE`: both required downgrades applied, 'הזמנות תהליך' becomes the catalog term 'פקודות תהליך' in status.he ('אובייקטים טכניים ופקודות תהליך') and in recommendedAction ('במיקומים פונקציונליים ובפקודות תהליך שנוצרים'); the record now holds no 'הזמנות תהליך'. `fm:STATUS_PROFILE_READ`, `fm:STATUS_TEXT_READ` and `fm:STATUS_USER_CHANGE`: no downgrades, written as drafted. `fm:STRUCTURE_INDICATOR_READ`: the auditor's re-run clause was added to evidence[1] as its own sentence after the sentence that carries the On-Premise count (it ends 'החזירו 21 רשומות כל אחת.'), with one writer word, 'עקבי' ('ואינם ניתנים לשחזור עקבי מהכתובת'), because the writer's own re-run of the same URL on the same day returned the same four records again (see the conflicts entry); the optional change of the cited URL to the SAP_ERP query was not applied, since the claim's parenthesis names the On-Premise query and the added sentence now bounds it. `fm:T352_READ`: the optional downgrade applied, the 'Operations for Catalog Profile' evidence is one shared const behind evidence[3] and status.source.

## refuted
- (none in this batch: all 6 audited drafts survived verification and were written.)

## conflicts
- `fm:STATUS_OBJECT_CREATE`: no official record names the FM (54 queries, 995 records, 776 topic bodies read in full, none contains STATUS_OBJECT_CREATE, STATUS_OBJECT or BSVA; positive control STATUS_CHECK; 4 domain-restricted web searches; STATUS_OBJECT_CREATE, STATUS_OBJECT, BSVA, JSTO, JEST, TJ30, STSMA, STATUS_PROFILE_READ, STATUS_READ, STATUS_UPDATE, BS02, BS03 and BS22 in neither SIMPL list, OIBS only inside ROIBSCAN_*). The derived `restricted` (repository_verified, L3) rested on the internal flag of a mirror-sourced sweep entry (verificationStatus internal-unsupported from `verified()` in `data/bapi-enrichment.sweep.ts`); replaced by the authored verification_required, depth L3 to L2 on purpose (the SERNR_ADD_TO_DOCUMENT and STATUS_CHANGE_EXTERN treatment). Repository layers disagree, not about existence (no conflictingEvidence): the sweep and the registry mark the module Internal and link JSTO only; function-intel says 'זמין ב-ECC.' and 'זמין ב-S/4HANA.', links JSTO and JEST and adds status-profile assignment; `verified()` sets ECC and S/4 support 'yes' and RFC, requiresCommit and requiresSave 'no' as code defaults. Precedent split on page-less transaction xrefs: `table:JSTO` keeps BS02 and BS22 in text only, while `fm:STATUS_CHANGE_EXTERN` and this record carry tx:BS02, tx:BS03 and tx:BS22 as xrefs (they resolve in the universe and have no app page; 22 of 25 xrefs are pages); settle one rule. `data/knowledge/object-intel.ts` lists I_STATUS_OBJECT_MAINTAIN and CL_STATUS_OBJECT as classesApis of JSTO and no official record names either (the only near match is the variable lr_status_object on a PPM Support Content page); check in SE24/SE37 or remove. The function-intel failure scenario 'OBJNR כפול' has no official source (the KBA 2920254 preview is adjacent and does not say which table failed). Open: existence, function group BSVA (repository only; the keyword LBSVAF11 in the KBA 2720065 preview ties an include to BSVA by ABAP naming convention, not by any page statement), release status, RFC flag, parameter contract (OBJNR, STSMA, RETURN), commit/rollback behaviour, and whether the standard creation transactions and BAPIs call the module; the released CA-GTF-VDM status views (I_STATUSOBJECTSTATUSCHANGE, I_STATUSPROFILE, I_USERSTATUS, I_SYSTEMSTATUS and their text views) appear only in the Cloudification Repository, which is not an evidence host, are read views and are not in the id universe; the 'Inspection Lot Status' VDM page was found only at 2023.latest; api.sap.com/api/StatusCode/overview is a JavaScript shell, not read.
- `fm:STATUS_PROFILE_READ`: no official record names the FM (29 queries; 146 topic-version pairs, 125 loios, read in full, none contains STATUS_PROFILE_READ, STATUS_PROFILE, PROFILE_READ, STATUS_READ, BSVA or STSMA; positive control STATUS_CHECK; 4 domain-restricted web searches; in neither SIMPL list). The derived verification_required (bapi-registry, inferred) is kept and tied to the official context (the status-profile structure, word for word the same in SAP ERP 6.0 EHP8 and S/4HANA 2025 FPS01; phase control codes grouped in user status profiles in 2025 FPS01). Repository drift: OIBS is IS-Oil Customizing for catalog/breakdown codes in `data/tx-intel.ts`, while the PM blueprint lists it with BS02 and BS03 for the JSTO and TJ30 rows; function-intel names TJ20 and TJ30, the blueprints and the registry JSTO and TJ30. Tension with `table:JSTO` and `cds:I_ObjectStatus`, whose recommendations name the module without an official source, while this record advises no new build before SE37; documented, not resolved. The module page shows function-intel's 'קיים ב-ECC (אמת ב-SE37).' (inferred), and the blueprints' S/4 column ('ללא שינוי (תואם)', 'ללא שינוי.') concerns the JSTO and TJ30 tables, not the module. Open: existence, function group, release status, RFC flag, parameter contract (STSMA in, User statuses out, inferred), commit/rollback, which tables the module reads, whether it returns phase control codes or texts in the user's language, authorization checks; BS02, BS03, BS22 and OIBS are named by no official page read (SE93); the SAP ERP copy of 'Defining User Statuses' was not located (not asserted absent); no documented channel for reading a status-profile definition in PM or PP-PI was located (search-bounded). Auditor measurements, no change needed: the exact-name SAP_ERP query returned 19 records on re-run where the draft says 20 (inside the stated one-day caveat); the two PP-PI process-message pages exist under SAP_ERP deliverable a0d3efbac8b14fc89b29bf47a1677c86 (deliverable_id 23795365) with bodies byte-identical to 2025.001, so the 'identical to 6.18.latest' claim holds although the record gives no ERP URL for them.
- `fm:STATUS_TEXT_READ`: no official record names the FM (35 queries, 660 records, 517 topic bodies read in full; both SIMPL lists; 3 domain-restricted web searches; 5 KBA previews). Possible mislabel, not asserted: the only status-text FM that SAP-hosted secondary sources name here is STATUS_TEXT_EDIT (keywords of the KBA 2531351 and 2720065 previews; Support Content customer code that passes OBJNR and SPRAS and receives LINE, with USER_LINE in the call pattern), and those are the outputs the repository attaches to STATUS_TEXT_READ; an SE37 check must cover both names. Repository drift, not about existence: function-intel (not inferred) describes system plus user status text with LINE / USER_LINE and says 'זמין ב-ECC.' and 'זמין ב-S/4HANA.'; both blueprints (PM:TJ02T, PP-PI:TJ02T) describe system status text only; the sibling USER_STATUS_TEXT_READ is inferred and says 'אמת ב-S/4.'. Same-page display: the evidence block now shows verification_required at sap_official_verified while the S/4 plate headline still reads 'זמין ב-S/4HANA.' from the function-intel s4 field (needs a separate repository fix, not changed here). The automatic summary of a domain-restricted web search claimed the name appears in KBAs; the five previews read contradict it, and it was not used. Open: the source tables and the language of the returned text (TJ02T and TJ30T are repository mappings; the OData pages read state neither); no status-text property was found in the two API_MAINTNOTIFICATION pages read; the Business Accelerator Hub release state of API_MAINTENANCEORDER;v=2 and API_PROCESS_ORDER_2_SRV was not checked (Hub pages are shells); the Maintenance Order (Version 2) entity page is returned by search only at 2023.latest (its 2025.001 body, identical, was read through the content service); Public Cloud not assessed.
- `fm:STATUS_USER_CHANGE`: no official record names the FM (8 name queries in 4 scopes, 117 records, 105 bodies read in full; positive control PM_ORDER_EXTERN_BOOK_DEFAULT; both SIMPL lists; 3 domain-restricted web searches). Repository overlap, not about existence (no conflictingEvidence): topic 8 of the PM blueprint gives STATUS_USER_CHANGE (PM:TJ30) and STATUS_CHANGE_EXTERN (PM:JEST) the same label 'שינוי סטטוס משתמש', and function-intel (inferred) points to STATUS_CHANGE_INTERN/EXTERN for the SE37 check; possibly one need recorded twice. Layer drift: function-intel links JEST, JSTO and BS22, the blueprint and the registry TJ30, BS02, BS03 and OIBS. The repository evidence is written at verification_required (function-intel inferred, registry requires-verification), as for ORDER_TYPE_READ and RESERVATION_READ, unlike STATUS_CHANGE_EXTERN (repository_verified on the sweep's 'confirmed real'). Official page inconsistency: in 'Find Allowed User Status by Type' the sections Business Context and Use and Features describe 'Find Allowed User Status by Order ID' and ByOrderID message types, while the definition, the technical name and Message Types describe a query by order type (ERP 6.18.latest, 2023.latest and 2025.001); the record quotes only the definition and the technical data. Search-index drift: that page is returned in the On-Premise scope only at 2023 Latest, not with the 2025.001 filter, although the content service returns an identical 2025.001 body. Cross-record tension, not resolved: the S/4HANA phase-control-code context (user statuses only for maintenance orders not processed by phases; YA01 and YA02 of scope items 4HH and 4HI) is absent from `table:TJ30` and `table:TJ30T`, and `table:JEST` recommends working through STATUS_CHANGE_EXTERN while this record advises no new build before SE37. Open: existence, function group, release status, RFC flag, parameter contract (OBJNR/ESTAT in, RETURN out), commit/rollback, whether the module enforces status numbers and authorization, whether the Change Maintenance Order_V1 request carries a user-status element (the ECC_MAINTORDCHGRC1 message structure was not read); an OData user-status channel for orders and notifications was not rechecked here (per `fm:STATUS_CHANGE_EXTERN`, the documented API_MAINTENANCEORDER v2 functions include none). The researcher removed two RLM marks after 'BAdI' in evidence[3] (no rule depends on them). Auditor note, no change needed: the extracted text of 'Configuring Phase Control Codes' shows a space before the final period because the words sit in an emphasis span; the rendered sentence equals the quote.
- `fm:STRUCTURE_INDICATOR_READ`: no official record names the FM (31 queries; 87 topic bodies read in full, 63 of them from the name queries, none contains STRUCTURE_INDICATOR, T370S, T370T or TPLKZ; 2 domain-restricted web searches; in neither SIMPL list and in neither the What's New 2025 FPS01 nor the 2021 PDF). The derived verification_required is kept and tied to the Structure Indicator topic (character for character the same in SAP ERP 6.0 EHP8 and S/4HANA 2025 FPS01). Repository inconsistency, not about existence: the PM blueprint lists the module under T370T, whose three blueprint fields (SPRAS, FLTYP, FLTYPT) are functional-location category text; function-intel names TRART; the only official page naming a table behind the structure-indicator definition is the Public Cloud VDM page ('t370s'), and T370S is not in the id universe (`table:T370T` already records the mix-up). OIA1 and OIMR, which the blueprint gives for T370T and the registry derives as the module's transactions, are unverified (only Oil & Gas records found) and are not xrefs. Search drift: the auditor's re-run of the cited On-Premise exact-name URL on 2026-09-23 returned 0 records (searchType SEMANTIC); the writer's re-run of the same URL on the same day returned the same four Malaysia records (2023 Latest, empty snippets, the loios of the researcher's saved run), and the SAP_ERP exact-name query returned the same five topics (6.05.latest); hence the evidence[1] sentence says the counts are not consistently reproducible. Open: existence, function group, release status, RFC flag, parameter contract (structure indicator in, edit mask and levels out, inferred), commit/rollback, which DDIC table the module reads; the on-premise existence and release status of I_FUNCLOCATIONSTRUCTURE and I_FuncLocationStructureText (documented only in Public Cloud 2608.500, whose VDM page prints no release status; check in ADT or View Browser); the I_FunctionalLocation VDM page read is 2023.latest (no 2025.001 page located).
- `fm:T352_READ`: no official record names the FM (25 queries; 160 topic-version bodies read in full; 2 domain-restricted web searches; T352_READ, T352, RBNR, 'catalog profile', API_CATALOGPROFILE, I_TechnicalReport, OIM1 and QCC0 in neither SIMPL list and in neither What's New PDF). The derived `restricted` (repository_verified, L3, 'מודול פונקציה פנימי', stability Use-With-Caution) came from `looksInternal` in `lib/bapi-registry.ts` (one letter before the underscore in T352_), not from a source; replaced by the authored verification_required, depth L3 to L2 on purpose (the QPK1 treatment). Repository drift: function-intel ties the module to T352B and TQ80 and its description covers both catalog profile and notification type, while the PM blueprint (PM:T352) and the registry tie it to T352; other knowledge layers describe T352 as planner groups (`data/knowledge/object-intel.ts`) and as maintenance organisation structure (`data/knowledge/pm-objects-ext.ts`), and the official records found name T352R (maintenance revisions), not T352 or T352B (recorded in `table:T352`); which DDIC table holds the catalog-profile header is not verified. Official page: two path examples in 'Operations for Catalog Profile' (2025.001) print the segment api_coding instead of api_catalogprofile; the record's recommendedAction asks to verify the path in the system metadata. Open: existence, function group, release status, RFC flag, parameter contract (Key in, Config out), commit/rollback; the Hub page OP_API_CATALOGPROFILE redirects to login (title and URL only); the release state of I_TechnicalReport and I_TechnicalReportText in the target system (only What's New 2022 says released); the Catalog Profile - Read service page is returned by search only at 2023.latest; the KBA 2635697 preview (off-allowlist host, does not name T352_READ) is not cited.
- Cross-record, method: search counts are run-dependent, day-bounded measurements (see STRUCTURE_INDICATOR_READ and STATUS_PROFILE_READ above). Bodies were read through help.sap.com/http.svc/deliverableMetadata and http.svc/pagecontent. The sc4sap MCP failed to connect in every session of this batch, the writer's included ('MCP error -32000: Connection closed' at session start), so no SE37 check confirms or contradicts any name, parameter, function group or release status. The GitHub Cloudification Repository files and the userapps.support.sap.com KBA previews named in notes are discovery reads on non-allowlisted hosts, not evidence; the KBA numbers in the notes of STATUS_OBJECT_CREATE and T352_READ and in the negative-search claim and notes of STATUS_TEXT_READ are quoted as read in public previews (STATUS_TEXT_READ names three me.sap.com/notes URLs in notes); no sapNote or kba field is carried. Status family still without an overlay (measured after this batch): `fm:STATUS_READ` and `fm:STATUS_CHANGE_INTERN` show a derived `restricted` (repository_verified, L3) from an `internal: true` sweep flag (first sweep round, 2026-07-14, source string 'SAP Help Portal + SE37 metadata (sapdatasheet.org)', no URL), the same mechanism behind the `restricted` that STATUS_OBJECT_CREATE carried; `fm:USER_STATUS_CHECK` and `fm:USER_STATUS_TEXT_READ` show a derived verification_required (L2).

## Writer-level normalisations applied
- The six drafts were parsed (JSON.parse) from the writer's own task message in the workflow transcript, not retyped; the emitted TypeScript round-trips to the merged JSON with identical key order (6 of 6; 109 fm records, 0 duplicate ids).
- `reviewer`: none of the six drafts carried one (house convention; 0 records in data/verification/** carry the key).
- `fm:STRUCTURE_INDICATOR_READ`: the verdict's clause was written verbatim except for the added word 'עקבי'. The writer's re-runs (HTTP 200, searchType SEMANTIC): the cited On-Premise URL returned 4 records whose loios equal the researcher's saved run; the SAP_ERP exact-name URL returned the same 5 topics. Without the word, 'ואינם ניתנים לשחזור מהכתובת' would contradict a same-day run that did reproduce the four records.
- Six shared Evidence consts hoisted (`STATUS_OBJECT_ARCHIVING_CONTEXT`, `DEFINING_USER_STATUSES`, `STATUS_DISPLAY_TEXT_CO`, `USER_STATUS_TECH_OBJECTS`, `STRUCTURE_INDICATOR_TOPIC`, `CATALOG_PROFILE_READ_OPS`), so each status.source is the same object as its evidence entry (the drafts carried identical copies; checked equal before hoisting). For T352_READ this is the verdict's optional downgrade.
- `DATE23` used for every accessedAt and lastVerifiedAt of the six records.
- Em/en dashes: 0. Cyrillic: 0. CERTAINTY_RE and PLACEHOLDER_RE: 0 hits over the 419 strings of the six records (checked although every record's level is official). Bidi marks kept as drafted: one LRM after 'registry()' in STATUS_OBJECT_CREATE, 21 RLM before parenthesised Latin in STRUCTURE_INDICATOR_READ.
- Header: the batch-17 paragraph added.
- `test/evidence-schema.test.ts`: no change; functions graduated 2026-09-02, is already in the graduated repoRef test, and FOUNDATION_RECORDS holds only the objects catalog.
- Gates: `./node_modules/.bin/tsc --noEmit` 0; `./node_modules/.bin/tsc --noEmit -p tsconfig.test.json` 0; `npm test` 212/212 pass; `./node_modules/.bin/eslint data/verification/functions.ts` 0; `validateRecords` over the six alone, over all 109 fm records and over every overlay: 0 problems in the universe the schema test builds (110 xrefs, all exact ids); 58 distinct URLs (evidence url fields and URLs written inside claims and notes) answered HTTP 200 (the four PDFs 206 to a range request). Coverage (functions, 142): L2 70 to 72, L3 32 to 30, verified 121 to 125, verification_required 10 to 6, conflict 11 to 11, s4-applicable 68 to 66, edition-specific 5 to 6 (the report counts a record with any private- or public-cloud source; STRUCTURE_INDICATOR_READ carries the Public Cloud 2608.500 VDM page while its status stays on-premise). Per id: STATUS_OBJECT_CREATE and T352_READ L3 repository_verified restricted to L2 sap_official_verified verification_required; STATUS_PROFILE_READ, STATUS_TEXT_READ, STATUS_USER_CHANGE and STRUCTURE_INDICATOR_READ stay L2 verification_required, level verification_required to sap_official_verified (context evidence). No other id changed. Not run: `next build` (not in this batch's gate list; its prebuild regenerates data/ai-tree/*.json). Not committed.

---

# Functions batch 18 (2026-09-23): 6 drafts audited, 6 written, 0 refuted

Written by the single writer from the adversarial verdicts (no verdict carried a `fixedRecord` field; the listed downgrades were applied to each researcher draft by exact, count-asserted substring edits). The drafts and verdicts were parsed (JSON.parse) from the writer's own task message in the workflow transcript, not retyped, and are equal to the six researcher results and the six verify results in the workflow journal. `fm:USER_STATUS_TEXT_READ`: all four downgrades applied: 'עד ארבעה תווים' becomes 'עד ארבע ספרות (four digits, כלשון העמוד)' in status.he and in the 'כלומר' sentence of the shared Defining User Statuses claim (evidence[2] = status.source), ', 3,238 תווים' is dropped from that claim and ', 7,722 תווים' from evidence[3]; the draft's `reviewer` field is removed. `fm:BAPI_PRODVERS_CREATE_REPLACE` and `fm:CR_WORK_CENTER_READ`: no downgrades, written as drafted. `fm:BAPI_ROUTING_GETDETAIL`: the four record downgrades applied (evidence[2] 'טבלה ממוספרת שהמספור בה מגיע ל-2,094' and 'בין 21A ל-620'; evidence[3] the academy self-contradiction sentence and item (5) for data/tx-intel.ts CA03 and data/transactions.ts CA01, repoRef extended); the result is identical to the auditor's corrected record (scratchpad/audrtg/fixed.json). Its fifth downgrade concerns the research summary and gaps (U+200F marks), which are not written to the overlay; the gaps restated below carry no bidi marks. `fm:CSAP_BOM_ITEM_MAINTAIN`: `reviewer` removed (downgrade 1); downgrade 2 rewords the research summary, which is not written to the overlay. `fm:CSAP_MAT_BOM_MAINTAIN`: all three downgrades applied, with the two punctuation adjustments listed under normalisations.

## refuted
- (none in this batch: all 6 audited drafts survived verification and were written.)

## conflicts
- `fm:USER_STATUS_TEXT_READ`: no official record names the FM (33 queries, 566 records; the 117 topic-version bodies returned by the 11 name queries were read in full and none contains USER_STATUS_TEXT_READ, USER_STATUS, STATUS_TEXT, TJ30, TJ30T, STATUS_READ or BSVA; positive controls PM_ORDER_EXTERN_BOOK_DEFAULT and 'CALL FUNCTION STATUS_CHECK'; 3 domain-restricted web searches; in neither SIMPL list; 6 KBA previews). The derived verification_required (bapi-registry, inferred) is kept and tied to the official context (user statuses defined in the status profile with a language-specific key of up to four digits and a short text; untranslated statuses appear in the original language; user status shown with short text or in language-dependent text form for notifications and process orders, identical or near identical in SAP ERP 6.0 EHP8). Name trap in the official documentation: UserStatusText of API_MAINTENANCEORDER (version 2, entity MaintenanceOrder) is documented as a string of phase control code IDs, while UserStatusText of A_ConstructionMeasure in the same guide is documented as the user status text; the recommendation forbids mapping the maintenance-order property to user status text. Repository layers are not uniform, not about existence (no conflictingEvidence): function-intel (inferred) and the PM:TJ30T blueprint row name the FM, the PP-PI:TJ30T row does not (only VIEW_MAINTENANCE_CALL (SM30) and RFC_READ_TABLE); the S/4 plate shows 'אמת ב-S/4.' as its headline and stability 'Internal' as a fact although it is the deriveStability default (separate repository fix, not changed here). Cross-record, no change: `table:TJ30T` rests on Technically Close Production Order (2023.latest) and says no official page defining TJ30T was found; this research found the SAP Multiresource Scheduling, Option for SAP S/4HANA 1.11 configuration page that names TJ30T 'Texts for User Status', and the Release Production Order body at 2025.001 identical to 2023.latest, so `table:TJ30T` can be strengthened later. Auditor re-measurements, applied: the drafted body lengths of Defining User Statuses (3,238; re-measured 3,270) and of the MRS configuration page (7,722; re-measured 7,782) did not reproduce and were dropped. Open: existence, function group, release status, RFC flag, parameter contract (STSMA/ESTAT/LANGU in, Text out, inferred), commit/rollback, which table and fields are read (TJ30T, TXT04 and/or TXT30 are repository mappings) and what comes back when no text exists in the requested language; the SAP ERP 6.18 copy of 'Defining User Statuses' was not located (metadata HTTP 404; not asserted absent); the release status of I_ManufacturingOrderStatus and API_PROCESS_ORDER_2_SRV was not checked (the VDM page prints none, Hub pages are shells); no general OData or CDS channel for the user status text of maintenance orders and notifications was located (search-bounded); the resolutions of KBA 3427807 and 2802757 need an S-user; Public Cloud not assessed.
- `fm:BAPI_PRODVERS_CREATE_REPLACE`: no official record names the FM (SAP Help in the On-Premise, SAP ERP and Cloud scopes, domain-restricted web searches, both SIMPL lists in full text). The repository contradicts itself about existence (written as repository evidence at conflicting_sources): `data/bapi-enrichment.sweep.ts` marks it invalid-name from a mirror that is not allowlisted (sapdatasheet.org), its description speaks of the absence of a released BAPI while its QA note asserts non-existence; function-intel (not inferred), the PP-PI blueprint MKAL row (label '(אמת)'), `data/transactions.ts#C223`, `data/domain-detail.ts`, `data/pppi-master-data-facets.ts` and `bp:master-recipe-process` present it as usable, and `data/academy/lessons/pp-generated.ts` contradicts itself (two 'verified-docs' blocks). The registry carries stability 'Released' beside invalid-name; the derived not_applicable (repository_verified, L3, 0 sources) is replaced by the authored verification_required, depth L3 to L2 on purpose. Three different internal names for creating a production version: CM_FV_PROD_VERS_MAINTAIN (sweep), CM_FV_PROD_VERS_DB_UPDATE (a Support Content wiki page, not cited) and FV_PROD_VERS_MAINTAIN_MULTI (the official 2025 FPS01 migration page 'PP - Production version', name only); only the last is printed by an official record. The sweep's 'no released BAPI creates production versions' is not contradicted (the documented service is OData, not a BAPI), but its advice (C223 or CM_FV_PROD_VERS_MAINTAIN) misses the OData V4 service API_PRODUCTION_VERSION (read, create, update; new as of S/4HANA 2022). Report spelling: the migration pages (ERP and staging) print CS_BOM_PORDVER_MIGRATION02, both SIMPL lists CS_BOM_PRODVER_MIGRATION02. Open: existence in ECC and S/4HANA, function group, release status, RFC flag, parameter contract (the PRODVERS table, RETURN and the BAPI_TRANSACTION_COMMIT advice are function-intel), commit/rollback; release, RFC and interface of FV_PROD_VERS_MAINTAIN_MULTI; the Business Accelerator Hub listing of API_PRODUCTION_VERSION was not located, and its availability before 2022 and in Public Cloud was not checked; F6400 is not in data/fiori/apps.ts (no xref); 'BAPI_MATERIAL_SAVEDATA (PVERSIONS)' in the blueprint is supported by no official title or snippet; the 'Manage Production Versions' app id drift queued under `fm:CM_FV_PROD_VERS_READ` (F2568 or F2703) stays unverified, and a separate PEO topic of the same name exists (loio 2f691c439df24825b1b2a4f845a466fa).
- `fm:BAPI_ROUTING_GETDETAIL`: no official record names the FM (20 queries, 338 records, the 103 topic-version bodies of the name queries read in full; 4 domain-restricted web searches; both SIMPL lists and the What's New 2025 FPS01 and 2021 PDFs in full text). The repository contradicts itself about existence (repository evidence at conflicting_sources): `data/bapi-enrichment.pppi.ts` bad() marks it invalid-name ('אינו קיים. אין BAPI לקריאת מסלול') from the SE37 mirror sapdatasheet.org; function-intel, the PP-PI PLPO funcs, `data/domains.ts` and `data/domain-detail.ts#pppi-routing`, `data/pppi-master-data-facets.ts#PLKO`, `data/tx-intel.ts#CA03` (bapis, entry marked verified) and `data/transactions.ts#CA01` (funcs) present it as existing; `data/academy/lessons/pp-generated.ts` contradicts itself (the objects card at 'verified-docs' lists it, a troubleshooting card and a quiz card at 'curated' call it non-existent and 'שם מומצא', another troubleshooting card offers 'CRAP_RECIPE_READ / BAPI_ROUTING_GETDETAIL'). The BAPI page shows 'ממשק משוחרר (Released)' as interface stability next to 'השם אינו אובייקט SAP תקני': `lib/bapi-registry.ts` derives stability from the pre-enrichment record and bad() sets invalid-name without resetting it (deriveStability would map invalid-name to 'Obsolete'); a code fix outside the overlay. The same page shows 'זמין ב-ECC.' and a parallel CDS view I_RoutingHeader from function-intel with no source; I_RoutingHeader is not in the universe, and for I_Routing, I_RoutingOperation and I_RoutingOperationComponent (facets, academy) no official page was found. The derived not_applicable (repository_verified, L3, needsVerification=false) is replaced by the authored verification_required at conflicting_sources, depth L3 to L2 on purpose. Auditor corrections applied: the Support Content 'List of BAPI's' numbering runs to 2,094 (2,058 distinct numbered rows), not 1,471 sequential rows, and its releases range 21A to 620, not 30A to 620; the repository inventory in evidence[3] is completed. Open: existence, official name and purpose, function group, release status, RFC flag, parameters (GROUP/GROUPCOUNTER in, OPERATIONS/SEQUENCES out, repository only), exceptions, commit/rollback, and the BUS1012 methods in the BAPI Explorer; KBA 1644841, 2576424 and 1955381 read as public previews only (Cause and Resolution need an S-user; the 1644841 sentence is about updating routings); CARO_ROUTING_READ is tagged 'Not released' in the KBA 2576424 keywords and is not in the universe; API_PRODUCTION_ROUTING and API_MASTER_RECIPE are context only and not in the universe; Hub pages not read.
- `fm:CR_WORK_CENTER_READ`: no official record names the FM (15 name queries in four scopes, 116 records, 112 name-query bodies and 148 bodies in total read in full; 3 domain-restricted web searches; both SIMPL lists with positive controls). The repository contradicts itself about existence (repository evidence at conflicting_sources): the sweep marks it invalid-name (confidence high, 'SAP Help Portal + SE37 metadata (sapdatasheet.org)', 2026-07-14, no URL), its QA note asserts non-existence while its description says only 'לא אומת בשם זה'; function-intel (inferred), PP-PI:CRHD funcs, `data/domains.ts` and `data/domain-detail.ts` (pppi-resource, pppi-work-center), the CRHD facet, `data/transactions.ts` CR01 and CRC1 and the academy lessons pp-work-center, pp-resource and pp-capacity ('verified-docs', a label no official record found reproduces) present it as usable. The derived not_applicable (repository_verified, L3, 0 sources) is replaced by the authored verification_required, depth L3 to L2 on purpose. Name drift: the PM blueprint gives CR_WORKCENTER_READ and the PP-PI blueprint CR_WORK_CENTER_READ for the same CRHD row (both ids in the universe, neither verified); one SE37 check should cover both names plus the sweep's CR_WORKSTATION_READ and CR_RFC_WORKCENTER_LIST (CR_WORKSTATION_READ appears only as a keyword of the KBA 3171162 public preview, a discovery read on a non-allowlisted host). Domains, domain-detail and the facet list the FM under bapis although it is not a BAPI; the function-intel s4 field 'זמין ב-S/4; CDS I_WorkCenter.' carries no source; the PP-PI blueprint descriptionEn for CRHD, 'Object type (A=work center)', is a field label (already queued under `table:CRHD`). Auditor note, no change needed: the key properties WorkCenterInternalID and WorkCenterTypeCode in recommendedAction come from the Read Work Center page (loio 1e5f218e776d4dbebc7955a12e35c86f, 2025.001), cited in notes only; the auditor re-fetched it and its text matches. Open: existence, function group, release status, RFC flag, parameters (OBJID/ARBPL/WERKS in, 'Resource detail' out), exceptions, commit/rollback; S/4HANA availability of the ECC enterprise service Read Work Centre (documented in the ERP 6.05 and 6.06 deliverables only; an On-Premise query on the operation name did not return it); F6175 is not in data/fiori/apps.ts and the Manage Resources app id is not printed on its Help pages; the api.sap.com WORKCENTER_0001 overview and fal.cloud.sap are shells; the collective KBA printed on the Work center/Resource migration page was not read.
- `fm:CSAP_BOM_ITEM_MAINTAIN`: the derived restricted (internal flag of the sweep patch, mirror-sourced) contradicts the S/4HANA 2025 FPS01 documentation, which lists the FM in 'List of Available BAPIs' (CSAP group, the same in SAP ERP 6.0 EHP8) and describes the area's APIs as meant for ABAP routines and external programs; the record decides with an authored unchanged, depth L3 to L5. RFC is unresolved (notes only, no conflictingEvidence): the repository says remoteEnabled 'no' ('FM פנימי, לא RFC'), the family page says the BAPIs are intended for RFC and based on DDIC structures, and the SAP Cloudification Repository files on GitHub (not allowlisted, discovery only) label it remote-enabled with successor CSAP_MAT_BOM_MAINTAIN in the classification file and notToBeReleased with successor BDEF I_BILLOFMATERIALTP_2 (not in the universe) in the release-info files. Inside the repository: the sweep says 'הקורא אחראי לשמירה' while requiresCommit and requiresSave are 'no' (verified() defaults); the main module is PM in the registry and PP-PI in function-intel; the academy's 'verified-docs' label has no URL. The OData documentation contradicts itself: the Constraints of the 'Bills of Material' overview say the BOM header and items cannot be deleted and DELETE raises an exception, while 'Operations for Bills of Material (Version 2)' lists Delete BOM Header and Delete BOM Item. Open: parameter interface (MATERIAL/PLANT/USAGE, T_STPO and return are function-intel), RFC flag, release status, commit/rollback and whether calls must sit between CSAP_MAT_BOM_OPEN and CSAP_MAT_BOM_CLOSE (only the sibling descriptions suggest it), function group at SE37 level, which BOM types it handles (the IB02 link comes only from the PM blueprint STPO row), Public Cloud availability; SAP Notes 2267873, 2267880, 2210904 and 2214906 printed in the SIMPL lists were not read (S-user); KBA 2545698 names the FM in its keywords only. Research summary wording (not published): its '0 סתירות' counts computed conflicts between evidence items, not the prose conflicts above.
- `fm:CSAP_MAT_BOM_MAINTAIN`: the repository record contradicts itself: its description says 'לא מסומן כ-BAPI Released רשמי' while releasedStatus 'Released · RFC' is a def() default, which the derived status repeated; no official source either way, and the authored explanation drops the label. RFC within the CSAP group: def() marks this FM remoteEnabled 'yes', the sweep marks CSAP_MAT_BOM_CREATE, CSAP_MAT_BOM_READ and CSAP_BOM_ITEM_MAINTAIN 'FM פנימי, לא RFC'; no official decision (not conflictingEvidence: different modules, a datum no official source states). The RETURN parameter the repository lists does not match the family description (ERROR exception and CAPI log), though no page excludes it for this FM. The PP-PI blueprint labels the FM 'תחזוקת עץ מוצר (ECC)' in the MAST row while the 2025 FPS01 pages list it as available (interface list, migration object); fix recommended, together with releasedStatus and remoteEnabled in `data/bapi-enrichment.pppi.ts`. Auditor findings applied: the tx-intel inventory is completed (CS01, CS02, CS20, IB02); 'BAPIs for the Log' is now cited inside the evidence claim (it anchors the SLG1 / CAPI part of recommendedAction and the tx:SLG1 xref); the notes (1) counts are bounded to the author's run (a same-day re-run returned 15 On-Premise records, including a new 'Material BOM Maintenance' PFCG record whose snippet does not name the FM, and 20 Cloud records). Open: release status, RFC flag, parameter signature, commit/rollback (SE37 and the in-system FM documentation); whether the interface was extended for the long material number (the SIMPL piece lists need an S-user); Public Cloud; identity of the implementation between releases (documentation identity does not prove unchanged code); F1813 is not in data/fiori/apps.ts (no xref).
- Cross-record, method: the sc4sap MCP failed to connect in the researchers' sessions and in the writer's session ('MCP error -32000: Connection closed'), so no SE37 check confirms or contradicts any name, parameter, function group, release status or RFC flag. The GitHub Cloudification Repository files and the userapps.support.sap.com KBA previews named in notes are discovery reads on non-allowlisted hosts, not evidence; the only kba field is 1644841 on `fm:BAPI_ROUTING_GETDETAIL` (me.sap.com/notes URL, supported_secondary_source); no sapNote field; SAP Note numbers printed in the SIMPL lists are quoted in notes as printed. Search counts are run-dependent, day-bounded measurements. Repository fixes outside the overlays, for a separate pass: the mirror-sourced invalid-name entries (sweep: BAPI_PRODVERS_CREATE_REPLACE, CR_WORK_CENTER_READ; pppi bad(): BAPI_ROUTING_GETDETAIL); registry stability not reset on invalid-name; academy 'verified-docs' labels without a reproducible source; the '(ECC)' blueprint label and the def() release and RFC defaults on CSAP_MAT_BOM_MAINTAIN.

## Writer-level normalisations applied
- The six drafts and verdicts were parsed (JSON.parse) from the writer's own task message in the workflow transcript, not retyped, and checked equal to the journal's researcher and verify results; the merged records were emitted by script and round-trip to the merged JSON with identical key order (6 of 6; 115 fm records, 0 duplicate ids; the 109 earlier records unchanged and in the same order).
- `reviewer`: removed from the two drafts that carried it (USER_STATUS_TEXT_READ, CSAP_BOM_ITEM_MAINTAIN), per house convention and the CSAP_BOM_ITEM_MAINTAIN verdict.
- `fm:CSAP_MAT_BOM_MAINTAIN` evidence[3]: the verdict's replacement 'data/tx-intel.ts מונה אותו ברשומות CS01, CS02, CS20 ו-IB02' was applied verbatim; because the original sentence continued elliptically ('ו-data/transactions.ts ל-CS01'), the writer added a comma and restored that clause's verb ('ו-data/transactions.ts משייך אותו ל-CS01'), with no change in meaning. Notes (1): the verdict's clause was inserted verbatim after the three exact-name counts as ', נכון להרצה של הכותב (הרצה חוזרת ... אינו נוקב בשם);', its internal semicolon rendered as an opening parenthesis so the query list that follows still reads as one list. Evidence[0] and status.source received the same 'BAPIs for the Log' sentence (they remain one shared const).
- Six shared Evidence consts hoisted (`USER_STATUS_TEXT_DEFINITION`, `PRODUCTION_VERSION_API`, `ROUTING_RECIPE_READ_APIS`, `WORK_CENTER_API`, `CSAP_BOM_ITEM_LIST_S4`, `CSAP_MAT_BOM_LIST_S4`), so each status.source is the same object as its evidence entry (the drafts carried identical copies; checked equal before and after the downgrades).
- `DATE23` used for every accessedAt and lastVerifiedAt of the six records.
- Text gate over the 412 strings of the six records: Cyrillic 0; CERTAINTY_RE and PLACEHOLDER_RE 0 hits; en dashes 0; one em dash, kept, inside the verbatim official deliverable title 'Logistics — General (LO)' in the sourceTitle of `CSAP_BOM_ITEM_LIST_S4` (the search service returns the deliverable with that character; same form as `fm:CP_RECIPE_READ` 'Master Recipe | Logistics — General (LO)' and the idocs, transactions and enhancements overlays); the Hebrew copy has none. Bidi marks kept as drafted: 5 RLM in CR_WORK_CENTER_READ (after 'OData' before API_WORK_CENTERS). `fm:USER_STATUS_TEXT_READ` notes (6) keep 'הקוד בן ארבעת התווים': it names the repository's TXT04 field in the list of unverified points, not the official page, and is outside the verdict.
- Header: the batch-18 paragraph added.
- `test/evidence-schema.test.ts`: no change; functions graduated 2026-09-02, is already in the graduated repoRef test, and FOUNDATION_RECORDS holds only the objects catalog.
- Gates: `./node_modules/.bin/tsc --noEmit` 0; `./node_modules/.bin/tsc --noEmit -p tsconfig.test.json` 0; `npm test` 212/212 pass; `./node_modules/.bin/eslint data/verification/functions.ts` 0; `validateRecords` over the six alone, over all 115 fm records and over all 387 overlay records: 0 problems in the universe the schema test builds (100 xrefs, all exact ids); 40 distinct URLs (evidence url fields and URLs written inside claims and notes) answered HTTP 200 (the five PDFs 206 to a range request). Coverage (functions, 142): L2 72 to 75, L3 30 to 25, L5 34 to 36, verified 125 to 123, verification_required 6 to 5, conflict 11 to 14, s4-applicable 66 to 66, edition-specific 6 to 6. Per id: BAPI_PRODVERS_CREATE_REPLACE, BAPI_ROUTING_GETDETAIL and CR_WORK_CENTER_READ L3 repository_verified not_applicable to L2 conflicting_sources verification_required; CSAP_BOM_ITEM_MAINTAIN L3 repository_verified restricted to L5 sap_official_verified unchanged; CSAP_MAT_BOM_MAINTAIN L3 repository_verified unchanged to L5 sap_official_verified unchanged; USER_STATUS_TEXT_READ stays L2 verification_required, level verification_required to sap_official_verified (context evidence). No other id changed. Not run: `next build` (not in this batch's gate list; its prebuild regenerates data/ai-tree/*.json). Not committed.
