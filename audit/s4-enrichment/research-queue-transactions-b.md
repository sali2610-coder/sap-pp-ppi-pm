# Research queue · transactions catalog, shard B (S/4HANA enrichment)

Kept by the single writer for `data/verification/transactions-b.ts`. One line per id that was
refuted or deferred at the adversarial-verification gate, and one line per repository
conflict the audited records surfaced. Batch 1 written 2026-09-24 (access date stamped
2026-09-24): 8 drafts audited, 8 written, 0 refuted. Three were taken from
`verdict.fixedRecord` (`tx:MB04`, `tx:MB05`, `tx:MB56`), one was written as drafted
(`tx:MB22`, plus the auditor's optional deliverable note), four were re-derived from the
verdict text with the listed downgrades applied (`tx:MBRL`, `tx:ME21N`, `tx:ME24`,
`tx:ME25`). No record carries a `reviewer` field (house convention across the overlay
files); the audit trail lives in each record's `notes`. Writer deviations from the drafts,
all cosmetic or strengthening, are listed at the end.

Batch 2 written 2026-09-24 (access date stamped 2026-09-24): 8 drafts audited, 7 written,
1 refuted (`tx:ME42`). Six were taken from `verdict.fixedRecord` (`tx:ME27`, `tx:ME28`,
`tx:ME2L`, `tx:ME2M`, `tx:ME2N`, `tx:ME43`) and one was re-derived from the verdict text
with the listed downgrades applied (`tx:ME41`). Still no `reviewer` field on any record.
Batch-2 conflicts and writer deviations are listed in their own sections below.

Batch 3 written 2026-09-24 (access date stamped 2026-09-24): 8 drafts audited, 8 written,
0 refuted. An earlier writer run for this batch stopped after declaring the shared
status-source constants and appending the unaudited `tx:ME49` draft (resume mechanism:
commit bd416cd9); this run finished from that state, recovering the ME49 draft from the
file. Six were taken from `verdict.fixedRecord` (`tx:ME47`, `tx:ME51N`, `tx:MIGO_GR`,
`tx:MK01`, `tx:MK02`, `tx:MK03`) and two were re-derived from the draft with the listed
downgrades applied (`tx:ME48`, `tx:ME49`). Still no `reviewer` field. Depth of the eight
ids (`report-coverage.mjs --ids`): all were L3 `repository_verified` on the committed file;
now ME47, ME48, ME49, ME51N, MIGO_GR and MK03 are L5 `sap_official_verified`, and MK01 and
MK02 are L3 `conflicting_sources` with an authored `replaced` status (see conflicts).
Batch-3 conflicts, the pending F1991 catalog patch and the writer deviations are listed in
their own sections below.

Batch 4 written 2026-09-24 (access date stamped 2026-09-24): 1 draft re-audited, 1 written,
0 refuted. `tx:ME42`, refuted in batch 2, was re-drafted by the repair step and re-audited
(every batch-2 problem marked RESOLVED, two new wording downgrades); it was taken from
`verdict.fixedRecord`, which already carries both downgrades. Still no `reviewer` field.
Depth (`report-coverage.mjs --ids`): `tx:ME42` moved from L3 `repository_verified` with the
derived status 'unchanged' to L5 `sap_official_verified` with the authored `restricted`.
Batch-4 conflicts and writer deviations are listed in their own sections below.

Batch 5 written 2026-09-24 (access date stamped 2026-09-24): 8 drafts audited, 8 written,
0 refuted. Six were taken from `verdict.fixedRecord` (`tx:MK05`, `tx:MK06`, `tx:MMBE`,
`tx:MMBE_OLD`, `tx:MMPV`, `tx:MR21`), one was written as drafted (`tx:MM17`: the first audit
sent it to the repair step, and the re-audit of the repaired draft listed no problem and no
downgrade), and one was re-derived from the draft with the four listed downgrades applied
(`tx:OMS2`). Still no `reviewer` field. The records were generated from the audited JSON in
the workflow journal, not retyped, and checked after insertion (deep-equal, field-level diff
against the audited JSON). Depth (`report-coverage.mjs --ids`, before and after): `tx:MMBE`,
`tx:MMBE_OLD` and `tx:MMPV` moved from L3 `repository_verified` with the derived status
'unchanged' to L5 `sap_official_verified` (MMBE and MMPV authored `unchanged`, MMBE_OLD authored
`replaced` with successor `tx:MMBE`). `tx:MM17` (`restricted`), `tx:MR21` (`changed`) and
`tx:OMS2` (`unchanged`) moved from `verification_required` to `sap_official_verified`, and
`tx:MK05` / `tx:MK06` (`replaced`, successor `tx:BP`) to `conflicting_sources`, but these five
stay at depth L1: none has a tx-intel / tx-detail record, so the page structure (purpose,
process, tables, BAPIs; 3 needed for L2) is missing, the same cap as `tx:ME2L` / `tx:ME2M` /
`tx:ME2N` in batch 2. Catalog totals (`npm run report:coverage -- --catalog transactions`):
L3 461 to 458, L5 78 to 81, verified 575 to 578, verification_required 1238 to 1233, conflict
5 to 7, s4-appl 577 to 582; L1 stays at 1277. Batch-5 conflicts and writer deviations are
listed in their own sections below.

Batch 6 written 2026-09-25 (research and adversarial audit 2026-09-24; access date stamped
2026-09-24): 8 drafts audited, 7 written, 1 refuted (`tx:MCXA`, see below). Five were taken
from `verdict.fixedRecord` (`tx:OMSL`, `tx:XK01`, `tx:XK03`, `tx:MCXB`, `tx:MCXC`) and two were
re-derived from the draft with the listed downgrades applied (`tx:XK02`, one downgrade;
`tx:MCVA`, nine). Still no `reviewer` field. The records were generated from the audited JSON
(extracted verbatim from the writer prompt as recorded in the workflow transcript, not retyped)
by a script that asserts every edit matches exactly once; after insertion all 7 deep-equal the
expected objects, and a field-level diff against the audited JSON shows only the changes listed
under the batch-6 writer deviations. Depth (`report-coverage.mjs --ids`, before and after):
`tx:OMSL` moved from `verification_required` to `sap_official_verified` (authored `unchanged`),
and `tx:MCVA`, `tx:MCXB` and `tx:MCXC` from `verification_required` to `sap_official_verified`
(authored `compatibility_scope`), but all four stay at depth L1: none has a tx-intel / tx-detail
record, so the page structure is missing (the cap recorded for `tx:ME2L` and in batch 5).
`tx:XK01`, `tx:XK02` and `tx:XK03` moved from `repository_verified` with the derived status
'unchanged' (XK01, XK03) or 'changed' (XK02) to `conflicting_sources` with an authored
`replaced` (successor `tx:BP`), and stay at L3 while the official conflict rows stand. Catalog
totals (`npm run report:coverage -- --catalog transactions`): verified 581 to 582,
verification_required 1230 to 1226, conflict 7 to 10, s4-appl 585 to 589; L1 1277, L3 456, L4 2
and L5 83 unchanged. Batch-6 conflicts and writer deviations are listed in their own sections
below.

Batch 7 written 2026-09-25 (research and adversarial audit 2026-09-25; access date stamped
2026-09-25): 9 drafts audited, 9 written, 0 refuted. `tx:MCXA`, refuted in batch 6, was
re-drafted (the 2023 FPS03 row now says MCXA is printed in the body list and in Other Terms),
re-audited with no problem and no downgrade, and written as drafted; its rows first read on
2026-09-24 (evidence[0], evidence[1] with its nested Fiori Apps Library row, evidence[3]) keep
DATE24, and the re-read 2023 FPS03 row carries DATE25. The other eight were taken from
`verdict.fixedRecord` (`tx:MCXX`, `tx:MSC1N`, `tx:MSC2N`, `tx:MSC3N`, `tx:MSC4N`, `tx:QA32`,
`tx:QC20`, `tx:QC21`); each fixedRecord already carried its verdict's downgrades. Still no
`reviewer` field. The records were generated from the audited JSON (extracted verbatim from this
writer's prompt in the workflow transcript, not retyped) by a script that asserts every edit
matches exactly once; after insertion all 9 deep-equal the expected objects, every
`status.source` is the same object as one of the record's evidence rows, and a field-level diff
against the audited JSON shows only the changes listed under the batch-7 writer deviations.
Depth (`report-coverage.mjs --ids`, before and after): `tx:MSC1N`, `tx:MSC3N` and `tx:MSC4N`
moved from L3 `repository_verified` with the derived status 'unchanged' to L5
`sap_official_verified` with an authored `unchanged`, `tx:MSC2N` the same way to an authored
`s4_native`, and `tx:QA32` to an authored `fiori_alternative_available`. `tx:MCXX`, `tx:QC20`
and `tx:QC21` moved from `verification_required` to `sap_official_verified` (authored
`compatibility_scope`, `unchanged`, `unchanged`), and `tx:MCXA` to `conflicting_sources`
(authored `compatibility_scope`; the Fiori Apps Library row nested as `conflictingEvidence` sets
the derived level), but these four stay at L1: none has a tx-intel / tx-detail record (the cap
recorded since batch 5). Catalog totals (`npm run report:coverage -- --catalog transactions`,
measured before and after this write): L1 1278 to 1279, L3 451 to 446, L5 87 to 91, verified
583 to 586, verification_required 1225 to 1221, conflict 10 to 11, s4-appl 589 to 593; L4 2,
legacy 2 and edition 3 unchanged. One row of that movement is not this batch: FIX-18 (commit
`ce8b9f8c`, committed between the two measurements) rebuilt `data/tx-intel.ts#MMBE_OLD`, which
moved `tx:MMBE_OLD` from L5 to L1; the batch itself accounts for L3 -5 and L5 +5. Batch-7
conflicts and writer deviations are listed in their own sections below.

Batch 8 written 2026-09-25 (research and adversarial audit 2026-09-25; access date stamped
2026-09-25): 8 drafts audited, 7 written, 1 refuted (`tx:QM13`, see below). Six were taken from
`verdict.fixedRecord` (`tx:QDV3`, `tx:QE51N`, `tx:QGA1`, `tx:QGP1`, `tx:QM10`, `tx:QM50`) and one
was re-derived from the draft with its eight listed downgrades applied (`tx:QI03`). Still no
`reviewer` field. The records were generated from the audited JSON (extracted verbatim from this
writer's prompt in the workflow transcript, not retyped) by a script adapted from the batch-7
generator that asserts every edit matches exactly once; after insertion all 7 deep-equal the
expected objects, every `status.source` is the same object as one of the record's evidence rows,
and a field-level diff against the audited JSON shows only the changes listed under the batch-8
writer deviations. Depth (`report-coverage.mjs --ids`, before and after): `tx:QDV3`, `tx:QGA1`
and `tx:QGP1` moved from L3 `repository_verified` with the derived status 'unchanged' to L5
`sap_official_verified` with an authored `fiori_alternative_available`, `tx:QE51N` and `tx:QM10`
the same way to an authored `unchanged`, and `tx:QI03` to an authored `restricted`. `tx:QM50`
moved from `verification_required` to `sap_official_verified` (authored
`fiori_alternative_available`) but stays at L1: it has no tx-intel / tx-detail record (the cap
recorded since batch 5). Catalog totals (`npm run report:coverage -- --catalog transactions`,
measured before and after this write): L3 443 to 437, L5 94 to 100, verified 596 to 597,
verification_required 1211 to 1210, s4-appl 600 to 601; L1 1279, L4 2, conflict 11, legacy 2
and edition 3 unchanged. Only the seven rows of this batch changed between the two `--ids`
measurements. Batch-8 conflicts and writer deviations are listed in their own sections below.

## refuted

- (none in this batch: all 8 audited drafts survived verification and were written with downgrades applied.)
- `tx:ME42` (batch 2, 2026-09-24): refuted at the adversarial gate, not written. Auditor problems: (1) unsupported claim contradicted by the cited source in both editions: the draft says the item names no successor transaction, Fiori app or API for ME42, while the item's 'Business Process Related Information' section (SIMPL_OP2023.pdf.txt ~line 50575, p. 958; SIMPL_OP2025.pdf.txt ~line 81090, p. 1430) prints verbatim 'If you want to use functionality related to sourcing of goods and services, you can use the following apps: Manage RFQs (F2049), Monitor RFQ Items (F2425), Request for Quotation Types (F4149), Manage Supplier Quotations (F1991), Compare Supplier Quotations (F2324)', the instruction to close open RFQs created via old transactions and set them to status "Completed", and the role SAP_BR_PURCHASER; the draft omits the section. (2) Truncated quote presented as verbatim: 'ME41, ME42, ME43, ME44, ME45, ME47, ME48, ME49' closes after ME49, while the source lists 14 codes one per line with a description column (ME41 Create, ME42 Change, ME43 Display, ME44 Maintain Supplement, ME45 Release, ME47 Maintain, ME48 Display, ME49 Price Comparison List, ME4B, ME4C, ME4L, ME4M, ME4N, ME4S). (3) Fiori library omission: `scripts/fal-app.mjs F2049 --release S32OP` prints 'Manage RFQs | Transactional / SAP Fiori elements | Published | component MM-FIO-PUR-RFQ', 'roles: SAP_BR_PURCHASER (R0128, Purchaser)', 'OData: MM_PUR_RFQ_MAINTAIN_SRV 0001 (S4CORE 109)' and 'GUI transactions: leading ME41; related ME42, ME43'; the draft's '--tcode ME42 returned none' holds for leading apps only, and its 1A0/1L2/1XF hunt is a red herring. (4) Universe constraint not stated: fiori:F2049 is not in `data/fiori/apps.ts`, so it cannot be a successor and 'deprecated' would fail replacement-no-successor; 'restricted' is defensible only on that ground and the record must say so, or carry a catalogPatch for F2049 built verbatim from the fal-app output (pipeline decision). (5) notes (6) misreports a measured value: txDetail('ME42') via `scripts/alias-loader.mjs` returns disposition 'available' (not 'unchanged'), supersededBy [], risk 'low', trust 'partial'; txStatusMap()['ME42'] = 'unchanged' is correct. (6) Page references off by one: the 2023 item starts on p. 957 and its transaction list plus apps are on p. 958; the 2025 item starts on p. 1429 and the list plus apps are on p. 1430. (7) Minor: evidence[4] quotes the tx-intel descHe with '-' where the source has an em dash; descTech reads 'שינוי מסמך RFQ (category A) ב-EKKO/EKPO. תיעוד שינויים. מסך קלאסי.'. Re-research: quote the item in full in both editions with the corrected pages, add the F2049 fal-app row, choose restricted-with-reason or deprecated-with-catalogPatch, and fix the measured txDetail value.
- (batch 3, 2026-09-24: none refuted; all 8 audited drafts written with the auditors' downgrades applied.)
- (batch 4, 2026-09-24: none refuted; `tx:ME42` from the batch-2 entry above was re-drafted, re-audited and written as `restricted`, so that entry is closed.)
- (batch 5, 2026-09-24: none refuted; all 8 audited drafts written, six from `verdict.fixedRecord`, `tx:MM17` as re-audited, `tx:OMS2` with its four downgrades applied.)
- `tx:MCXA` (batch 6, 2026-09-24): refuted at the adversarial gate after the repair round, not written. Auditor problem: the repair added a false statement about the source. The 2023 FPS03 evidence row says 'בגרסה זו אין לפריט סעיף Other Terms', but item 34.1 S4TWL - Quality Management Information System (QMIS) in the 2023 list does have an Other Terms section, at the end of the item after the QM Analytics app table and just before '34.2 S4TWL - Redundant Transactions in QM' (`scratchpad/official/SIMPL_OP2023.pdf.txt` line 45150, list at 45151-45154), holding the same 36 codes and ending with MCXA. The first-round auditor was wrong on this point, and the repair turned that mistake into a claim that the section does not exist (HOUSE-RULES §3.2). In the 2023 FPS03 item, as in the 2025 FPS01 one, MCXA is named twice: once in the body list and once in Other Terms. Everything else checked out: item 9.6.8 in 2025 FPS01 (lines 45808-45870: component QM-QC-IS, note 0002270193, Other Terms and body lists of 36 codes each, 12 MCO*, 12 MCV*, 12 MCX*, the compatibility-scope quote, SAP Note 2269324 / ID 473, 'is not the target architecture', 'will be removed in the SAP Fiori launchpad', apps F3239, F3273, F3383, F3382, F2428, F3584, F3583); document versions 1.36 and 1.35 match the PDF headers; `fal-app.mjs MCXA --release S32OP` printed what the draft says; the sap_help record 'Quality Evaluations (QMIS)' (versionId 2025.001, loio e30db753128eb44ce10000000a174cb4, date 2026-02-24) matches verbatim; `compatibility_scope` with release '2025 FPS01' and the 2025 row as source is valid; xrefs tx:MCVA, tx:QA32 and tx:QA33 resolve; `data/tcode-catalog.ts#MCXA` (line 870) matches; no em dashes and no certainty words. Re-research: rewrite the 2023 row to say MCXA is printed in the body list and in Other Terms (line 45150, list 45151-45154), then re-audit.
- (batch 7, 2026-09-25: none refuted; `tx:MCXA` from the batch-6 entry above was re-drafted with the 2023 FPS03 row corrected, re-audited with no problem and no downgrade, and written as drafted, so that entry is closed.)
- `tx:QM13` (batch 8, 2026-09-25): refuted at the adversarial gate after the repair round, not written. Auditor problems: (1) new factual error in status.he: the draft says the simplification item 'מפרט כלא זמינות רק את עטיפות ה-ITS/MiniApp QM13WP, WAO_QM13WP ו-QPQM13', but both texts of S4TWL - QM WEB Workplace (MiniApps) based on ITS Services (`scratchpad/official/SIMPL_OP2025.pdf.txt` item 9.6.4 around line 45390, `SIMPL_OP2023.pdf.txt` item 34.7 around line 45905) also list WAO_QPQM13 'QM iView Selection Variant Task', plus many objects not tied to QM13 (QM10WP, QA32WP, IQS8WP and others), so 'רק' is false and breaks HOUSE-RULES §3.2. (2) evidence[2] and evidence[3] give an incomplete list of the QM13-related objects in the item: they name QM13WP, WAO_QM13WP and QPQM13 and leave out WAO_QPQM13, which both lists print; evidence[3] says 'אותה רשימת אובייקטים' and repeats the same incomplete three-object list. (3) Minor inference: the Fiori Apps Library output for QM13 never prints the labels S15OP or S16OP, so the mapping '(1909, 1909 FPS01)' in evidence[0], status.source.claim and status.he is inferred from the neighbours S14OP=1809 FPS02 and S17OP=1909 FPS02, not printed by the source. Checked and passing after the repair round: `fal-app.mjs QM13 --release S32OP` prints 'Display Quality Notification Tasks - Worklist', SAP GUI, Published, QM-QN, the roles SAP_BR_QUALITY_ENGINEER / _MANAGER / _TECHNICIAN, the catalogs SAP_QM_BC_NOTIF_DSP / SAP_QM_BC_NOTIF_TASK, intent QualityNotificationTask-displayWorklist, releases S6OP=1610 through S14OP, then S17OP through S32OP, PCE S29 through S32, S36=2602, S37=2608, and no predecessor or successor, and the evidence[0] claim matches it; the name fix is correct; status.source is filled in; the `data/tx-intel.ts#QM13` row is accurate (line 486: Tasks - Worklist, QMSM/QMEL, similar QM10/11/12, needs-verification); the body of help topic 5747ba53... (2025.001, loio as truncated in the verdict) was fetched again and the quoted 'single-level list ... worklist functions ... Notifications Items Tasks Activities' sentence matches it word for word; sap-help-search 'Selecting and Evaluating Quality Notifications' returns 21 records with that loio first; the xrefs tx:QM10, tx:QM11, tx:QM12 and table:QMSM resolve; no SAP Note number is asserted; no em dashes and no banned certainty words. Re-research: drop 'רק' from status.he, list all four QM13-related objects (QM13WP, WAO_QM13WP, QPQM13 and WAO_QPQM13 'QM iView Selection Variant Task') in evidence[2] and evidence[3], and state the missing S15OP / S16OP without naming their releases, then re-audit. Until then `tx:QM13` keeps its generated record (derived 'unchanged' at `repository_verified`, L3, measured before and after this write).

## conflicts

- `tx:MB04` — `data/tx-intel.ts#MB04` says "זמין ב-S/4HANA" and names an "Advanced Subcontracting Cockpit"; the Simplification List item S4TWL - AVAILABILITY OF TRANSACTIONS IN MM-IM (2025 FPS01 item 15.3.9, 2023 FPS03 item 27.6) lists MB04 among the MB codes replaced by MIGO / BAPI_GOODSMVT_CREATE (code exists, menu call raises an error), and no official record prints "Advanced Subcontracting Cockpit" (MM-IM documentation says "Subcontracting Cockpit"). The documented Fiori alternative, Post Subsequent Adjustment (F5476, loio 79824a704b7742ff8252c0c8575fbea3), is not in `data/fiori/apps.ts`. Same F0843 naming conflict as `tx:MIGO` (officially Post Goods Receipt for Purchasing Document; `apps.ts` calls it Post Goods Movement).
- `tx:MB05` — `data/tx-intel.ts#MB05` calls the code "זמין ב-S/4HANA" and describes it as "התאמת צריכה לפי תפוקה (Subsequent Adjustment by Output Quantity)"; the same Simplification List item lists MB05 twice as replaced, and no official title or snippet found on 2026-09-24 supports the "by output quantity" description (help.sap.com queries listed in the record's notes). The ECC role page (SAP Library NW 7.0 SPS12) documents subcontracting subsequent adjustment under MB04, not MB05. The function of MB05 stays "דורש אימות במערכת SAP".
- `tx:MB22` — `data/tx-intel.ts#MB22` (and `#MB21`) pair the code with "Manage Reservations (F1622)"; no official record names an app of that name or that id (query 'F1622' returns F1522 / F1602 / F1652 / F2600 / F2596 / F6122 only). The documented apps in 2025.001 are Manage Manual Reservations (F4839) and Manage Reservation Items (F5601), plus Job Scheduling - Manual Reservations (F7554); none is in `data/fiori/apps.ts`. MBVR and RKPF are not in the id universe.
- `tx:MB56` — no record in `data/tx-intel.ts`, `data/lifecycle.ts` or `data/transactions.ts` (the builder derives "לא קיים תיעוד מאומת במאגר"); only `data/tcode-directory.ts` and `data/process-guides.ts` describe it and tie it to CHVW, which no official record names. `audit/master-completion/simpl-tcode-index.json` attributes the MB56 mention to item 30.9 (S4TWL - ANSI/ISA S95 Interface) because the 2023 list prints '30.10S4TWL' … '30.40S4TWL' without a space; the sentence is in item 30.27 (S4TWL - Batch History, p. 799). The Business Function LOG_PP_BATCH_HISTORY is named in item 30.28, not 30.27. The BAdI VB_GET_POS_PLANT (loio 8ffdb753128eb44ce10000000a174cb4) is not in `data/exits.ts`.
- `tx:MBRL` — `data/tx-intel.ts#MBRL` says "זמינה" with fiori "Post Goods Movement (MIGO)"; the Simplification List item lists MBRL twice as replaced (2025 FPS01 15.3.9, 2023 FPS03 27.6). Post Goods Movement is the Web GUI wrapper of MIGO, not a separate Fiori app for return deliveries. No `data/lifecycle.ts` row. The 2025.001 Goods Movement (Retail) role page still lists "Return delivery for material document MBRL" (recorded as documentation drag, as for `tx:MBST` / `tx:MB01`).
- `tx:ME21N` — `data/tx-intel.ts#ME21N` and `data/lifecycle.ts#ME21N` pair F0842A with "Create Purchase Order (Advanced)"; officially (Sourcing and Procurement 2025.001; Fiori Apps Library Apps('F0842A')/S32OP via `scripts/fal-app.mjs`) F0842A is Manage Purchase Orders (Version 2), predecessor F0842, and Create Purchase Order - Advanced carries the app id ME21N (SAP GUI). The word "deprecated" in the `s4` text (about ME21) is caught by OBSOLETE_RE in `components/neo-shell/data/tx-detail.ts`, so the derived layer showed "הוחלף ב-S/4HANA" without a successor; the authored status now overrides it. F0842A is not in `data/fiori/apps.ts`. What's New 2023 FPS02 / 2025 pages write "App ID F0842" while Sourcing and Procurement 2025.001 writes F0842A; the library explains it (Version 2 vs predecessor).
- `tx:ME21` / `tx:ME22` / `tx:ME23` (spillover, records in `data/verification/transactions.ts`) — those records state that no Simplification Item names the classic family or leave the check open; item 14.2.8 (2025 FPS01) / 38.2 (2023 FPS03) S4TWL - Classic MM-PUR GUI Transactions replacement names ME21, ME22, ME23, ME24, ME25, ME27, ME28, ME51, ME52, ME53, ME54, ME59, MR01, MR1M under "Transactions are NO longer supported in SAP S/4HANA on-premise edition" (Business Impact note 2267449, FAQ 1803189), while the Enjoy Purchase Order page (2025.001, loio 8082cf535b804808e10000000a174cb4) still says "you can still use transactions ME21, ME22 and ME23, and there are no functional limitations". Owned by the transactions.ts writer.
- `tx:ME24` — `data/tx-intel.ts#ME24` describes the code as the classic (non-Enjoy) change-purchase-order screen and lists itself in `obsolete`; the official role page (2025.001 and 6.18.latest, body read) names it "Maintain PO supplement ME24" (the classic change screen is ME22). "קיים אך לא מומלץ" should read "לא נתמך לפי פריט פישוט". No successor is named by any official source (ME22N in tx-intel is an inference).
- `tx:ME25` — `data/tx-intel.ts#ME25` says "קיים ב-S/4HANA אך מיושן" without citing the Simplification Item and lists itself in `obsolete` (`obsolete: ['ME25']`), which the tx-detail index skips; the builder derives "changed" only because the `s4` text matches CHANGED_RE ("המומלצת"). No official source names ME21N as the declared successor of ME25 specifically, so the record is `simplified` without a successor. No Fiori app for purchasing exists in `data/fiori/apps.ts`.
- cross-cutting — the MM-IM role pages of 2025.001 (Goods Movement (MM-IM), Goods Movement (Retail), Reservations (MM-IM)) and the MM-PUR role page (Process Purchase Orders) still list the replaced / no-longer-supported codes (MB04, MBRL, ME24, ME25); every record in this batch reads that as documentation drag of the role menu, never as proof of support, consistent with `tx:MB01` / `tx:MBST` in transactions.ts.
- `tx:ME27` (batch 2): `data/tx-intel.ts#ME27` says 'זמין ב-S/4HANA; ME21N עם doc type STO מועדף. Advanced Intercompany Sales/STO ב-S/4 מרחיב זאת.' and does not list itself in `obsolete`; item 14.2.8 (2025 FPS01) / 38.2 (2023 FPS03) S4TWL - Classic MM-PUR GUI Transactions replacement names ME27 under 'Transactions are NO longer supported in SAP S/4HANA on-premise edition'; no official record found prints 'Advanced Intercompany Sales/STO'. Plant Return and Store Return with Stock Transport Order (2025.001, loio b760bd534f22b44ce10000000a174cb4, body read) still documents 'Stock transport order (ME21N or ME27)' as the "old" order (documentation drag, as for ME24 / ME25). `fal-app.mjs --tcode ME27` (S32OP): no leading app, no GUI app entry. ME2ST, EKKO, EKPO, EKET are not in the id universe.
- `tx:ME28` (batch 2): `data/tx-intel.ts#ME28` names 'Fiori Approve Purchase Orders / flexible workflow' (s4) and 'My Inbox / Approve Purchase Orders' (fiori) as the alternative without an official source; the same Simplification item names ME28 under 'NO longer supported' and no successor; Apps('ME28')/S32OP prints SAP GUI, status 'Deprecated', predecessors/successors '-'. The gap between 'NO longer supported' (item) and 'Deprecated' (library) stays open for a live check plus SAP Notes 1803189 / 144081 (S-user). `data/tcode-catalog.ts#ME28` prints 'Release Purchasing Document' while help.sap.com and the library print 'Release Purchase Orders'. The Release Purchase Orders (MM-PUR) role page (2025.001 and 6.18.latest, bodies read) still lists 'Release purchase order ME28' (documentation drag).
- `tx:ME2L` / `tx:ME2M` / `tx:ME2N` (batch 2): `audit/master-completion/simpl-tcode-index.json` maps the 2023 mention of all three (SIMPL_OP2023.pdf.txt line 70372) to item '60.9 S4TWL - Retail Additionals'; the PDF text places that line inside item 60.28 S4TWL - Retail iViews (heading at line 70337, Note 2370183; 60.9 starts at line 69231 and does not mention them). Index bug, not used as a basis for any claim. The item's 'the purchase order Fiori app' carries no app id, so no successor or fiori xref was set. `data/tx-intel.ts` has no ME2L record (the code appears in the alternative lists of ME23N and ME5A), no ME2M record (mentioned by ME22N, ME23N, ME5A) and no ME2N record; `data/tcode-catalog.ts` names ME2M 'Purchase Orders by Material' and ME2N 'Purchase Orders by PO Number', matching the role page. Because the page structure (3 authored facts, `lib/evidence/depth.ts`) comes from those repository records, all three sit at depth L1 in `report-coverage` despite sap_official_verified evidence and an authored status; a tx-intel record for each would lift them.
- `tx:ME41` / `tx:ME43` (batch 2; `tx:ME42` refuted, see above): `data/tx-intel.ts#ME41` says 'זמין ב-S/4HANA; לתהליכי sourcing מתקדמים SAP מכוונת ל-Ariba Sourcing / Central Procurement.' (consultant field: 'ב-S/4 קיים RFx ב-Ariba/SAP Sourcing כחלופה אסטרטגית') and `#ME43` says 'זמין ב-S/4HANA' with an empty fiori field; neither cites item 14.4.1 (2025 FPS01) / 38.7 (2023 FPS03) S4TWL - RFQ Simplified Transaction, which prints 'The following transactions are deprecated in SAP S/4HANA: ME41 ME42 ME43 ME44 ME45 ME47 ME48 ME49 ME4B ME4C ME4L ME4M ME4N ME4S' and points to Manage RFQs (F2049), Monitor RFQ Items (F2425), Request for Quotation Types (F4149), Manage Supplier Quotations (F1991), Compare Supplier Quotations (F2324). None of the five ids is in `data/fiori/apps.ts`, so no successor can be set (ME41 written as `simplified`, ME43 as `fiori_alternative_available`); Apps('F2049')/S32OP prints 'GUI transactions: leading ME41; related ME42, ME43' and empty predecessor/successor fields. Adding F2049 to `data/fiori/apps.ts` from the fal-app output would unlock `deprecated` + successor for ME41 / ME42 / ME43. SAP Note 2332710 (printed in both items) was not read.
- `tx:ME47` / `tx:ME48` / `tx:ME49` (batch 3): `data/tx-intel.ts#ME47` says 'זמין ב-S/4HANA; Ariba Sourcing מנהל הצעות באופן אלקטרוני כחלופה.', `#ME48` says 'זמין ב-S/4HANA.' and `#ME49` says 'זמין ב-S/4HANA; Ariba Sourcing מספק award/scoring מתקדם כחלופה.', all three with an empty fiori field and none citing item 14.4.1 (2025 FPS01) / 38.7 (2023 FPS03) S4TWL - RFQ Simplified Transaction, which names all three under 'deprecated in SAP S/4HANA'. The derived layer showed all three as 'unchanged' (`report-coverage.mjs --ids` on the committed file, 2026-09-24); the authored statuses now override it (ME47 and ME48 `fiori_alternative_available`, ME49 `simplified`). `Apps('F1991')/S32OP` prints 'GUI transactions: leading ME47; related ME48' and 'predecessors: -; successors: -'; F1991 is not in `data/fiori/apps.ts` (pending patch below), so neither record carries a successor. No official record read ties ME49 to a single app; F2324 (Compare Supplier Quotations), F2425 and F4149 were not run through `fal-app.mjs`. The 2025.001 role pages 'Maintain Quotation (MM-PUR)' (body read for ME47, snippet for ME48 and ME49) and 'General Display Functions in Purchasing (MM-PUR)' (snippet) still list the codes; read as a deprecation notice, not a removal, as for `tx:ME43`.
- `tx:ME51N` (batch 3): `data/tx-intel.ts#ME51N` fiori field reads 'Create Purchase Requisition - F1643 / Create Purchase Requisition Advanced' (em dash in the source), while the official records read (Feature Comparison for Managing and Creating Purchase Requisitions, 2025.001, body read; `Apps('F2229')/S32OP`) name Manage Purchase Requisitions - Professional (F2229), with ME51N as its leading GUI transaction. F1643 was not checked against an official record, so which number is right stays open. The s4 text 'ME51 הישן deprecated' matches OBSOLETE_RE ('deprecat') in `components/neo-shell/data/tx-detail.ts`, so the derived layer showed ME51N itself as 'replaced'; the official records document ME51N as current and the authored status 'unchanged' now overrides it. F2229 is not in `data/fiori/apps.ts`.
- `tx:MIGO_GR` (batch 3): the draft's notes said no dedicated repository record exists; `data/tx-intel.ts#MIGO_GR` does exist (line 422), so the writer added it as a repository row and kept the old finding in notes (Old → New). Its consultant field lists 'MIGO_GO (GI), MIGO_TR (transfer), MIGO_GS', while both official 2025.001 search records (Goods Movement (MM-IM), loios 3b07b753128eb44ce10000000a174cb4 and 3e07b753128eb44ce10000000a174cb4) print MIGO_GO as 'Goods Receipt for Order' and MIGO_GI as 'Goods Issue'. Its s4 and fiori fields name 'Goods Receipt for Purchase Order' as the Fiori alternative; not checked against the Fiori Apps Library (next step: `fal-app.mjs --tcode MIGO_GR --release S32OP`). The 2025.001 activity table that lists MIGO_GR also lists MB1B, MBST and MBSU (replaced per item 15.3.9): documentation drag, as for `tx:MB01` / `tx:MBST`. MIGO_GO, MIGO_GI, MIGO_TR and MIGO_GS are not in the id universe.
- `tx:MK01` / `tx:MK02` / `tx:MK03` (batch 3): `data/tx-intel.ts` says 'חסומה ב-S/4HANA' for all three (BP as the route), consistent with item 5.1.27 (2025 FPS01) / 3.19 (2023 FPS03) S4TWL - Business Partner Approach, but OBSOLETE_RE in `components/neo-shell/data/tx-detail.ts` does not match 'חסומה', so the derived layer showed all three as 'unchanged'; the authored `replaced` (successor `tx:BP`) now overrides it. The operational page 'Maintaining Vendor Master Jurisdiction Code (MM)' (S/4HANA 2025.001, loio 896bd0531d8b4208e10000000a174cb4, deliverable 'United States') still prints 'You can also use transaction MK01 to create or MK02 to change' in its search snippet (and body, per the MK01 audit): kept as a `conflicting_sources` row on both MK01 and MK02, so both sit at depth L3 until a live S/4HANA check shows whether MK01 and MK02 redirect to BP. The same search also returns 'Basis Market Data' pages (2025.001 and 2023.003) whose snippet prints 'Vendor MK01 - Create MK02 - Change (Current)'; not cited, same drag pattern. FLVN01 is printed only by `data/tx-intel.ts#MK01`, not by the item. `tx:XK01` and `tx:FK01` have no overlay record.
- `tx:ME42` (batch 4): `data/tx-intel.ts#ME42` says 'זמין ב-S/4HANA; Ariba Sourcing חלופה אסטרטגית.' and does not cite item 14.4.1 (2025 FPS01) / 38.7 (2023 FPS03) S4TWL - RFQ Simplified Transaction, which lists ME42 on the 'Change' row under 'The following transactions are deprecated in SAP S/4HANA'. Measured on 2026-09-24 through `scripts/alias-loader.mjs`: txDetail('ME42').s4.disposition 'available', supersededBy [], risk 'low', trust 'partial', and txStatusMap()['ME42'] 'unchanged' (the derived status `report-coverage` showed before this write); the authored `restricted` now overrides it. `Apps('F2049')/S32OP` prints 'GUI transactions: leading ME41; related ME42, ME43' and empty predecessor/successor fields; F2049 is still not in `data/fiori/apps.ts` (0 hits at write time), so the record carries no successor. Adding F2049 from the fal-app output would unlock `deprecated` + successor for ME41 / ME42 / ME43 (see the batch-2 line above). SAP Note 2332710 (printed in both items) was not read (S-user).
- `tx:ME41` page range (found while writing batch 4, not edited): the ME42 re-audit measured the 2023 FPS03 item 38.7 as pp. 957-959 (transaction list and apps on p. 958, end of the role list on p. 959) and the 2025 FPS01 item 14.4.1 as pp. 1429-1431 (the SAP_BR_BUYER line on p. 1431). The inline 2023 row of `tx:ME41` cites pp. 957-958 while its claim also quotes the role split, which ends on p. 959. The audited record was left as written; a later pass can widen the range to 957-959. (`tx:ME43` and the `ME41_SIMPL2025` row cite 1429-1430 and quote only the transaction list, which sits on p. 1430.)
- `tx:MK05` (batch 5): the S/4HANA 2025.001 EWM page 'Configure Goods Receipt Control' (loio 1fd5fc0b81db4825a4d7681e8f6228c3; search 'MK05 block vendor purchasing', default scope SAP_S4HANA_ON-PREMISE, 21 hits) prints 'Supplier Block (transaction MK05)' in its snippet, while item 5.1.27 (2025 FPS01) S4TWL - Business Partner Approach lists MK05 under 'Transactions that get redirected to transaction BP'. Kept as a `conflicting_sources` row (documentation drift, the pattern recorded for `tx:MK01` / `tx:MK02`) until SE93 or a run of MK05 in a live S/4HANA on-premise system settles it. Item 3.19 (2023 FPS03) names MK05 in neither of its lists, so status and release rest on 2025 FPS01. Unread lead for the releases before 2025: 'Re-direction to Business Partner (BP) Transaction' (What's New in SAP S/4HANA 2022, versionId 2022.000, loio 220bd05aa56c49318c4fae0173cc10d4); the `tx:MK06` research read its body for MK06, and what it says about MK05 was not checked. There is no tx-intel / tx-detail record for MK05; the code appears as a pointer in the whenNot field of `data/tx-intel.ts#MK02`.
- `tx:MK06` (batch 5): the official sources classify MK06 differently. What's New in SAP S/4HANA 2022 ('Re-direction to Business Partner (BP) Transaction', body read) calls it deprecated and redirected to BP; item 3.19 (2023 FPS03, Document Version 1.35) lists it under 'Transactions that are obsolete' and not in the redirected list; item 5.1.27 (2025 FPS01, Document Version 1.36) lists it under 'Transactions that get redirected to transaction BP'. The 2023 row is `conflicting_sources`, and the status anchors on the 2025 FPS01 row. What would settle it: running MK06, or SE93, in an S/4HANA 2023 on-premise system. There is no tx-intel / tx-detail record for MK06.
- `tx:MM17` (batch 5): item 13.1.3 (2025 FPS01) / 48.5 (2023 FPS03) S4TWL - Manufacturer Part Number prints 'Transactions MM17 and MMCC are not available for MPN materials in SAP S/4HANA' and says nothing about other materials, hence `restricted`. The first draft cited a PP/DS page URL as the Simplification List and attributed to 13.1.3 a note number the item does not print; both were fixed in the repair round before the re-audit. The repository has `data/tcode-directory.ts#MM17` but no tx-intel / tx-detail record, hence depth L1.
- `tx:MMBE` (batch 5): the fiori field of `data/tx-intel.ts#MMBE` reads 'Stock - Single Material' with the id F1077 (the source separates the two with an em dash); `fal-app.mjs F1077` prints 'Material Documents Overview', leading MB51, not MMBE. The search record 'Material inventory balance' (Data Migration, 2025.001) prints 'Stock - Single Material (F1076)'; F1076 was not run through `fal-app.mjs` and is not in `data/fiori/apps.ts`, so the correct id for the repository field stays open. Item 13.13.4 (2025 FPS01) / 56.21 (2023 FPS03) S4TWL - Unit of Measure (UoM) Group enahncement (IS-OIL-DS-HPM) mentions MMBE for the stock of a UoM newly added to a group (the researcher's reading that it does not was corrected by the auditor); industry-specific, not used for the status, carried into recommendedAction.
- `tx:MMBE_OLD` (batch 5): `data/tx-intel.ts#MMBE_OLD` carries MM02 content (descHe, descTech, process, consultant, tables and BAPIs describe changing the material master); only its module and area fields are cited. The same record supplies the page structure that lifts MMBE_OLD to L5, so it should be rewritten from item 15.3.9 (2025 FPS01) / 27.6 (2023 FPS03) S4TWL - AVAILABILITY OF TRANSACTIONS IN MM-IM. Before this write the derived layer showed MMBE_OLD as 'unchanged' (repository_verified, L3); the authored `replaced` (successor `tx:MMBE`) now overrides it. The Fiori app 'Stock Overview' that the item names has no app id in any record read (`fal-app.mjs --tcode MMBE_OLD`: none; `--tcode MMBE`: the SAP GUI entry, 0 successors).
- `tx:MMPV` (batch 5): no official record read names 'Close Period for Product Master' as the successor of MMPV; the 2025.001 topic (loio 18d9464a79084a51a9c5d54fc31f3672, snippet only) lists the Fiori app and the classic SAP GUI option side by side, so no successor was set. Items 12.4 (2023 FPS03) and 6.5.10 (2025 FPS01) S4TWL - Technical Changes in Material Ledger with Actual Costing print different application components (2023: FIN-MIG-ML, CO-PC-ACT; 2025: CO-PC-ACT); the draft gave the 2023 value for the 2025 row, and the auditor corrected it.
- `tx:MR21` (batch 5): no successor in any record read. `Apps('F6489')/S32OP` (Change Material Prices) prints empty GUI transactions and empty predecessor and successor fields, and the Feature Comparison page (2025.001, loio 1703f29f66e94ab8a23e016f9f7f2b89, body read) compares F2680 'Manage Material Valuations' with MR21 side by side. `fal-app.mjs --tcode MR21` lists F2680, F4006 and F6684 as apps that lead with MR21; none of F2680, F4006, F6684 or F6489 is in `data/fiori/apps.ts`. The book `data/library/book7/raw/ch2.json` (section Change Material Prices (F6489)) says SAP recommends MR21 when universal parallel accounting (FINS_PARALLEL_ACCOUNTING_BF) is not active: a secondary source, kept in notes without the switch name and not in recommendedAction. The researcher's pointer `data/books/book7.json#F6489` was wrong (that file holds the title entry). There is no tx-intel / tx-detail record for MR21, hence depth L1.
- `tx:OMS2` (batch 5): OMS2 has no tx-intel / tx-detail record of its own (the repository names it in `data/tcode-catalog.ts`, 'Material Type Configuration', area 'קונפיגורציה', in `data/table-enrichment.ts` and in the `table:T134` record), hence depth L1. OMSR, OMSA and OMS9, named in item 5.1.24 (2025 FPS01) / 3.21 (2023 FPS03) S4TWL - Simplified Product Master Tables Related to OMSR Transaction, are not in the id universe (prose only). The IMG menu path in the draft was printed by no cited source and was removed. SAP Note 2267138 (printed in both items) was not read (S-user).
- `tx:OMSL` (batch 6): no official record read on the ECC side prints OMSL. Two SAP_ERP searches ('OMSL transaction material number', 'Define Output Format of Material Number OMSL') returned 21 records each, none printing OMSL in title or snippet (the IMG activity 'Define Output Format of Material Numbers' appears in free text); the ECC continuity rests on `data/tcode-catalog.ts#OMSL` ('Define Material Number Length', area 'קונפיגורציה'), and the record says so. FLETS, the activation transaction named in item 5.1.5 (2025 FPS01) / 3.22 (2023 FPS03) S4TWL - Material Number Field Length Extension, is not in the id universe (prose only). `fal-app.mjs --tcode OMSL` (S32OP, auditor run): no leading app, no GUI app entry. There is no tx-intel / tx-detail record for OMSL, hence depth L1. SAP Note 2267140 (printed in both items) was not read (S-user).
- `tx:XK01` / `tx:XK02` / `tx:XK03` (batch 6): `data/tx-intel.ts` says 'חסומה ב-S/4HANA' for all three (BP as the route), consistent with item 5.1.27 (2025 FPS01) / 3.19 (2023 FPS03) S4TWL - Business Partner Approach, but the derived layer showed XK01 and XK03 as 'unchanged' and XK02 as 'changed' at `repository_verified` (`report-coverage.mjs --ids`, measured before this write: OBSOLETE_RE in `components/neo-shell/data/tx-detail.ts` does not match 'חסומה', the gap recorded for `tx:MK01`, and CHANGED_RE matches 'שינוי ספק' in the XK02 text); the authored `replaced` (successor `tx:BP`) now overrides it. Official operational pages of 2025.001 still name the codes and are kept as `conflicting_sources` rows, so all three stay at L3 until a live S/4HANA on-premise check (SE93, or running the code) shows whether they redirect to BP: 'Partners' (Warranty Claim Processing, Maintenance Management, loio bfd8c353b677b44ce10000000a174cb4; XK01, XK02, XK03), 'Customizing for Stock Transfer' (India, loio b5a2cf8b170c40d6bfda2523c7bda7d7; XK01) and 'Transactions' (Logistics General, loio bcd8c353b677b44ce10000000a174cb4; XK02). Item 15.3.5 (2025 FPS01) S4TWL - Blocked customer or supplier in Inventory Management tells the reader to remove a blocked supplier's plant assignment via XK02 and mentions an upgrade path from 1511/1610/1709; kept as a conflict row on XK02 as well (SAP Note 2629400, printed in the item, not read). Item 13.14.1 (2025 FPS01) / 59.7 (2023 FPS03) S4TWL - Specific fields on Business Partner lists XK01, XK02 and XK03 under 'Transaction not available in SAP S/4HANA' (US Federal Financials context), and item 5.1.30 (2025 FPS01) S4TWL - Business Partner Approach for SAP MDG says the old ERP transactions (XK01 and XK03 named) 'are replaced by transaction BP'. `fal-app.mjs --tcode XK02` and `--tcode XK03` (S32OP): no leading app, no GUI app entry; not run for XK01. SAP Notes 2265093 and 2270420 were not read (S-user).
- `tx:MCVA` / `tx:MCXB` / `tx:MCXC` (batch 6): the repository labels differ from the Fiori Apps Library names (Apps('<code>')/S32OP, component QM-QC-IS). `data/tcode-catalog.ts#MCVA` 'QM Notifications: Overview' / 'הודעות QM: סקירה' against 'Inspection Lot KPIs - Overview by Supplier': a real conflict, flagged in the record, and the repository textbook table 18.1 (`data/library/book5-full.json`) puts MCVA under Supplier / Inspection Lot / Overview, with the supplier notification overview at MCVZ. `#MCXC` 'QM Vendor Analysis (Inspection Lots)' / 'ניתוח ספקים QM (מנות בדיקה)' against 'Inspection Lot KPIs - Quantity Overview by Material' (the same table lists MCXC under Material / Inspection Lot / Overview of Quantities). `#MCXB` 'QM Inspection Results Analysis' against 'Inspection Characteristic KPIs - By Material' (the table lists MCXB under Material / Insp. Characteristics / General; generic rather than contradictory). `data/tcode-catalog.ts` is outside this writer's files, so no label was changed. Item 9.6.8 (2025 FPS01) / 34.1 (2023 FPS03) S4TWL - Quality Management Information System (QMIS) names the QM Analytics apps F3239, F3273, F3383, F3382, F2428, F3584 and F3583 for the 36 QMIS codes as a group, with no per-code mapping, and none of the seven is in `data/fiori/apps.ts`, so no successor or Fiori xref was set. The library prints the on-premise releases of MCVA and MCXB as S6OP to S14OP and S18OP to S32OP (no 1909 release). None of the three has a tx-intel / tx-detail record, hence depth L1. SAP Notes 2269324 and 2270193 were not read (S-user).
- `tx:MCXA` (batch 7): the official tension is kept inside the record. Item 9.6.8 (2025 FPS01) / 34.1 (2023 FPS03) S4TWL - Quality Management Information System (QMIS) says 'The transactions will be removed in the SAP Fiori launchpad for SAP S/4HANA' (future tense, no date), while `fal-app.mjs MCXA --release S32OP` lists MCXA as a Published SAP GUI app ('Inspection Lot KPIs - Overview by Material', technical catalog SAP_TC_QM_BE_APPS:S4QM) with no successor. The Fiori Apps Library row is nested as `conflictingEvidence` under the 2025 FPS01 item row, so the derived level is `conflicting_sources` under the authored `compatibility_scope`. What settles it: SE93 and the launchpad content of catalog SAP_TC_QM_BE_APPS:S4QM on an S/4HANA 2025 FPS01 system. Repository label: `data/tcode-catalog.ts#MCXA` 'QM Inspection Lots Analysis' / 'ניתוח מנות בדיקה QM' differs from the library name 'Inspection Lot KPIs - Overview by Material' (the kind of label FIX-18 corrected for MCVA, MCXB and MCXC); the record cites the label as the repository's without flagging it, and `data/tcode-catalog.ts` is outside this writer's files.
- `tx:MCVA` / `tx:MCXB` / `tx:MCXC` (batch 6, found while writing batch 7): FIX-18 (commit `ce8b9f8c`, 2026-09-25) set the three `data/tcode-catalog.ts` labels to the Fiori Apps Library names ('Inspection Lot KPIs - Overview by Supplier', 'Inspection Characteristic KPIs - By Material', 'Inspection Lot KPIs - Quantity Overview by Material'), so the batch-6 line above is resolved on the repository side. The written records still describe the old labels: the repository rows of all three, MCVA's status.he and notes (ד), and MCXC's recommendedAction, which asks for an SE93 check because the label and the official title differ. Not edited in this batch (audited records outside its nine); they need an audited update that keeps the old finding as Old → New (HOUSE-RULES §3.8).
- `tx:MSC1N` / `tx:MSC2N` / `tx:MSC3N` / `tx:MSC4N` (batch 7): the repository module labels disagree. `data/tcode-catalog.ts` gives PP-PI (area 'אצוות') for all four, `data/tx-intel.ts` gives QM (area 'ניהול אצוות (Batch Management)'), and the Fiori Apps Library (S32OP) gives component LO-BM for all four. MSC1N and MSC4N record the split in their notes, MSC2N names both files, MSC3N cites neither. Item 5.1.8 (2025 FPS01) / 3.3 (2023 FPS03) S4TWL - Logistics Batch Management lists MSC1, MSC2, MSC3 and MSC4 as not available in S/4HANA on-premise 1511; MSC1, MSC2 and MSC3 resolve in the route manifest (MSC4 does not), and their own records were not checked in this batch.
- `tx:QC21` (batch 7): `data/tcode-catalog.ts#QC21` labels QC21 'Maintain Certificate Profile' / 'תחזוקת פרופיל תעודה', while `fal-app.mjs QC21 --release S32OP` prints 'Create Quality Certificate - For Inspection Lot' (business catalog SAP_QM_BC_CERT_OUTGOING; the auditor's re-run also printed intent InspectionLot-createCertificate), and item 9.6.6 (2025 FPS01) / 34.6 (2023 FPS03) S4TWL - ITS services in QM names QC21 under certificates. The record flags the repository label for correction; `data/tcode-catalog.ts` is outside this writer's files. The auditor dropped the xref `tx:QC22` ('Change Certificate Profile' in the same catalog) because no cited source links it to QC21; the QC22 label was not checked.
- `tx:QDV3` (batch 8): `data/tx-intel.ts#QDV3` describes QDV3 as the read-only display of a sampling plan ('Display Sampling Plan', tables QDPS/QDPV), and its s4 field says 'זמין ב-S/4HANA ללא שינוי... ניתן להחליף בדוחות/אפליקציות הצגה כלליות'. Item 9.6.2 (2025 FPS01) / 34.2 (2023 FPS03) S4TWL - Redundant Transactions in QM prints 'QDV3 Display sampling procedure QM-PT-BD-SPL Fiori app Sampling Procedure' under 'The following transactions are replaced by corresponding Fiori apps and are therefore not available in the SAP Fiori launchpad for SAP S/4HANA (but can be called directly with SAP GUI)', and `Apps('F2255')/S32OP` names the app 'Display Sampling Procedure' ('leading QDV3; related -'). Both the label and the s4 text need an audited repository fix; `data/tx-intel.ts` is outside this writer's files. F2255 is not in `data/fiori/apps.ts`, so the record is `fiori_alternative_available` without a successor; adding F2255 would let a later pass reconsider a successor `fiori:F2255`. The 2023 FPS03 item starts on p. 874 and its QDV3 line sits on p. 875 (the auditor's page correction is in the record).
- `tx:QE51N` (batch 8): no repository conflict. Open ends from the audit: `fal-app.mjs --tcode QE51N` (S32OP) lists F2689 'Record Results for Inspection Points' as a leading app, and F2689 is not in `data/fiori/apps.ts`, so it stays in notes; the QE51N library record prints an app documentation link (help.sap.com topic 76FADF57A8A8113CE10000000A44147B, 2025.001) whose body was not read with `sap-help-body.mjs`. QE51 (the code the item removes) and the tables QALS, QAMR and QAVE are not in the id universe.
- `tx:QGA1` (batch 8): `data/tcode-catalog.ts#QGA1` gives QGA1 the English title 'Quality Control Chart for Inspection Lot' (area 'דיווח/ניתוח'), and no official source read supports it. Item 9.6.1 (2025 FPS01) / 34.3 (2023 FPS03) S4TWL - Removed Navigation to Transactions for Evaluations in QM describes QGA1 as 'Display quality score time line' (QM-QC-IS) with the app 'Inspection Lot Detailed Analytics (F3273)', and the snippet of 'Transactions for Control Charts' (2025.001, loio bf65b6531de6b64ce10000000a174cb4; body not read) assigns 'Control charts for inspection lots' to QGC1. The record states the mismatch in notes; `data/tcode-catalog.ts` is outside this writer's files. F3273 (`fal-app.mjs F3273 --release S32OP`: Published, QM-QC-IS, no leading GUI transaction) is not in `data/fiori/apps.ts`, so it stays in prose.
- `tx:QGP1` (batch 8): `data/tcode-catalog.ts#QGP1` labels QGP1 'Print Inspection Instruction' / 'הדפסת הוראת בדיקה', while both S4TWL - Removed Navigation to Transactions for Evaluations in QM items (2023 FPS03 §34.3, 2025 FPS01 §9.6.1) print 'Results history for task list characteristic' (QM-QC-IS) against the app 'Results History (F2428)'. The draft had called the repository title the original ECC description (SE93) that does not contradict the items; no SE93 or ECC source was read, and the record now flags the label for correction. `Apps('F2428')/S32OP` prints QGP1 as a related GUI transaction (leading QGA4; related QE09, QGA2, QGP1, QS28). F2428 is not in `data/fiori/apps.ts`, so xrefs stay empty. SAP Note 2340996 was not read (S-user).
- `tx:QI03` (batch 8): item 9.6.2 (2025 FPS01) / 34.2 (2023 FPS03) S4TWL - Redundant Transactions in QM names the alternative 'Fiori app Quality Info Records Procurement' and prints no app id. `Apps('F2256')/S32OP` ('Display Quality Info Record for Procurement', leading QI03) lists F2256A 'Manage Quality Info Records' as its successor, and the Data Migration search record 'QM - Quality info record' (2025.001, loio 8427c17adbeb4a84a1a0784aa63c586c) prints F2256A beside QI02 and QI03; no official record read settles which of the two apps the item means. Neither F2256 nor F2256A is in `data/fiori/apps.ts` (0 hits at write time), so the record is `restricted` without a successor. The library's release list for the QI03 GUI entry skips S15OP-S17OP, while F2256 lists them.
- `tx:QM10` (batch 8): the s4 field of `data/tx-intel.ts#QM10` says 'אפליקציית Fiori Manage Quality Notifications מחליפה את חוויית ה-worklist', and no official record read supports it: sap-help-search 'Manage Quality Notifications' (SAP_S4HANA_ON-PREMISE, 21 records; top hit 'Managing Quality Notification', PLM, loio 14cb795540729c46e10000000a441470, not an app page), `fal-app.mjs --tcode QM10 --release S32OP` (one leading app, QM10 itself, 0 successors) and `data/fiori/apps.ts` (0 matches). A documented negative, not a denial; the record keeps it as a repository claim. `audit/master-completion/simpl-tcode-index.json` maps QM10 to item 9.6.4 (2025 FPS01) / 34.7 (2023 FPS03) S4TWL - QM WEB Workplace (MiniApps) based on ITS Services because 'QM10' appears in the description column of QM10WP and WAO_QM10WP ('QM10 - Call from Workplace/MiniApp'); QM10 itself is not a listed code. The library's on-premise release list for QM10 skips S15OP and S16OP.
- `tx:QM50` (batch 8): `data/tcode-catalog.ts#QM50` labels QM50 'Worklist: Notification Tasks' / 'רשימת עבודה: משימות הודעה', while both S4TWL - Removed Navigation to Transactions for Evaluations in QM items (2023 FPS03 34.3, 2025 FPS01 9.6.1) print 'Time Line Display of Quality Notifications' (QM-QN) with the app 'Nonconformance Detailed Analytics (F3583)'; the record asks for a repository fix or an SE93 check. F3583 is not in `data/fiori/apps.ts`, so xrefs stay empty (the `tx:QA32` / F2343 precedent). `fal-app.mjs --tcode QM50 --release S32OP`: no leading app, no GUI app entry. No official source read links QM10 to QM50, so no xref was added. QM50 has no tx-intel / tx-detail record, hence depth L1.

## writer deviations (2026-09-24)

- `tx:MB22` — applied the auditor's optional precision: 'Changing a Reservation' 2025.001 (loio 3b64bd534f22b44ce10000000a174cb4) is now noted as served under the Retail deliverable and under Inventory Management and Inventory (MM-IM). Everything else as drafted.
- `tx:MBRL` — prose aligned to "2023 FPS03" (was "2023 FPS3" in status.he, evidence[1].claim and notes); the 2025 FPS01 evidence is a shared constant referenced by evidence[0] and status.source, as the verdict asked.
- `tx:ME21N` — the em dash quoted from tx-intel's fiori string replaced by a hyphen with the note "(במאגר מופיע קו מפריד ארוך)"; two `fiori_library` evidence rows added from `scripts/fal-app.mjs` run on 2026-09-24 (Apps('ME21N')/S32OP and Apps('F0842A')/S32OP), bounded by the script's printed output; the notes sentence "דפי הספרייה הם app shell ולא נקראו" replaced by the fal-app method statement; the body character-count remark (17,213 / 2,728 / 8,631 with the script's two-line header) added so the counts are not read as a mismatch.
- `tx:ME24` — en dashes in the two Simplification List titles replaced by hyphens; evidence[3].release written as the versionId `6.18.latest`; the top-level `reviewer` field dropped (house convention), its content moved into notes.
- `tx:ME25` — no auditor downgrade; writer aligned evidence[3].release from "6.0 EHP8 (6.18.latest)" to the versionId `6.18.latest` (HOUSE-RULES §4, same as ME24) and said so in notes. Content unchanged.
- `tx:MB05` — evidence[0] built with the `SIMPL2023_MMIM` helper re-declared in transactions-b.ts (overlay files carry no value imports, so the helper cannot be imported from transactions.ts); the helper title keeps the em dash of the transactions.ts original so both helpers stay identical.
- Ids not in the universe and therefore not xref'd anywhere in this batch: F5476, F4839, F5601, F7554, F1622, F0842, F0842A (Fiori); MBVR, MIGO_DIALOG, MIGO_GS, MBLB, MB57, MB90, ME9F, ME9FF (tcodes); BAPI_GOODSMVT_CANCEL, BAPI_PO_CREATE1 (FM); MATDOC, MSLB, RKPF, CHVW, EKKO, EKPO, EKET, EKKN, EORD (tables); VB_GET_POS_PLANT, MB_RESERVATION_BADI (BAdIs).

## writer deviations (batch 2, 2026-09-24)

- Shared constants declared for every status source, as the verdicts asked: `ME27_SIMPL2025`, `ME28_SIMPL2025`, `ME2L_ROLE_2025`, `ME2M_ROLE_2025`, `ME2N_FAL_S32OP`, `ME41_SIMPL2025`, `ME43_SIMPL2023`; the `_writerNote` key inside the ME28 fixedRecord dropped.
- `tx:ME2L`: the fixedRecord's `reviewer: "researcher"` dropped (house convention: no record in the overlay carries a reviewer field). Content otherwise verbatim.
- `tx:ME2N`: evidence[3].release for the 2025 Simplification List PDF aligned from '2025.001' to '2025 FPS01' (the shard convention for that PDF; the ME2L verdict asked for the same alignment on its identical row). The FAL row and the status keep '2025.001' as audited.
- `tx:ME43`: the auditor's optional status adopted verbatim (`fiori_alternative_available`, release 2023 FPS03, source = the 2023 item constant); the notes sentence about 'no full status' rewritten to say what was written and why `deprecated` stays blocked. En dashes in the two Simplification List titles replaced by hyphens; the em dash in the FAL sourceTitle replaced by ' · '.
- `tx:ME41`: no fixedRecord; re-derived from the draft with every listed downgrade applied (em dashes removed, evidence[2].sourceTitle as 'Title | Deliverable' with the technical name moved into the claim, tx-intel s4 and consultant fields quoted verbatim, the identical role split in both items, 2023 pages 957-958, SAP Ariba Sourcing as the item's example rather than a recommendation) plus the optional 'רשומת ה-FAL מתעדת' wording. Additionally: the em dash inside the quoted tx-intel descHe ('Request for Quotation — RFQ') replaced by a comma, the en dash in the 2023 title by a hyphen, and one audit-trail sentence appended to notes.
- `tx:ME27`, `tx:ME28`, `tx:ME2M`: fixedRecord taken verbatim (ME2M from `fixedRecord.record`).
- The sentence 'הרשומה אינה נושאת שדה reviewer, כמוסכמת הקטלוג' added to the notes of ME2L, ME2M, ME2N, ME41 and ME43 (ME27 and ME28 already carried it).
- Ids not in the universe and therefore not xref'd in this batch: F2049, F2425, F4149, F1991, F2324 (Fiori); ME2ST, WRBDL, WRBDL2 (tcodes named only in prose); EKKO, EKPO, EKET (tables).

## pending catalog patch (batch 3, not applied: `data/fiori/apps.ts` is outside this writer's files)

- `fiori:F1991` for `tx:ME47` (and `tx:ME48`): values bounded by `scripts/fal-app.mjs F1991 --release S32OP`, printed on 2026-09-24 (auditor run and writer re-run, same output). id F1991; name Manage Supplier Quotations; slug manage-supplier-quotations; type Transactional (SAP Fiori elements, Published); module MM; component MM-FIO-PUR-RFQ; role SAP_BR_PURCHASER (R0128, Purchaser); business catalog SAP_PRC_BC_PURCHASER_PIR 'Purchasing - Source Assignment'; technical catalog SAP_TC_PRC_COMMON; intent SupplierQuotation-manage; OData MM_PUR_QTN_MAINTAIN_SRV 0001 (S4CORE 109); guiTx ['ME47', 'ME48'] (leading ME47, related ME48); predecessors and successors '-'; on-premise releases from 1610 (S6OP) to 2025 FPS01 (S32OP); trust 'verified-docs'; s4OnPrem 'yes'; cloud 'unknown' (only the S32OP record was verified; S32PCE, S36 and S37 lines are printed but not checked); ecc 'ECC: ME47/ME48 (SAP GUI)'; docs link topic e4eadbb277ab4372b8635d0a8bb9141e (2025.001); cite https://fioriappslibrary.hana.ondemand.com/sap/fix/externalViewer/index.html#/detail/Apps('F1991')/S32OP. Still to write before the patch compiles against `FioriApp` (`lib/fiori/types.ts`): he, purpose, problem, explain, only from the item sentence 'suppliers can submit their quotations which are later available to the purchaser' or from the docs topic read with `sap-help-body.mjs`. Once merged: `tx:ME47` can take `successor`/xref `fiori:F1991` (status could then move to `deprecated`), and `tx:ME48` an xref.

## writer deviations (batch 3, 2026-09-24)

- Resumed state: the header comment and the seven shared status-source constants (`ME47_SIMPL2025`, `ME48_SIMPL2023`, `ME51N_FEATURE_COMPARISON_2025`, `MIGO_GR_GOODS_MOVEMENT_2025`, `MK01_SIMPL2025`, `MK02_SIMPL2025`, `MK03_SIMPL2023`) from the earlier run were checked against the fixedRecords and kept; the unaudited `tx:ME49` draft was replaced by the downgraded record. Every `status.source` given as the string 'evidence[0]', a pointer claim or a placeholder in the relayed JSON is the shared constant object.
- `sapNote` dropped from the simplification rows of `tx:ME51N` (2267445) and `tx:MK02` (2265093): the validator's sap-note-format rule requires a me.sap.com/notes url or a repoRef, and these rows cite the help.sap.com PDF. The verdicts marked the field optional; the numbers stay in titles and prose.
- `tx:ME47`: ' — SAP ERP' in the ECC row title replaced by ' · SAP ERP' (the verdict's em-dash sweep missed it); 'הרשמה הקבוצתית' corrected to 'הרשימה הקבוצתית'; 'הקוד קיים ותקין להרצה' reworded to the item's own 'currently available' (the same downgrade the ME49 auditor required for the identical sentence); 'Compare Supplier Quotations F2324 להשוואה, מקביל ל-ME49' reduced to 'the item also lists F2324' (no source ranks the apps; the ME48 auditor struck the same kind of claim); the closing instruction now says open RFQs created in the old transactions, as the item prints, instead of quotations created in ME47; 'יש להוסיפה משם catalogPatch המצורף' in recommendedAction replaced by 'יש להוסיפה שם', with the pointer to the patch values moved to notes.
- `tx:ME48`: no fixedRecord; re-derived with every listed downgrade. The replacement for 'ME48 (Display Quotation) מנויה בשמה' applied to evidence[0] only, because the draft's evidence[1] never carried the parenthetical. Both optional role-page rows added (URLs copied from the re-run search 'ME48 display quotation', 21 hits). Writer addition: a `fiori_library` row for F1991 (fal-app output re-run on 2026-09-24, audited under ME47) printing 'related ME48', and status.he / recommendedAction reworded so the record no longer says F2049 was the only one of the five apps checked; the verdict's 'אף אחת מהן אינה מאומתת כ-successor' is bounded to the two records checked (F2049, F1991).
- `tx:ME49`: draft recovered from the file; every listed downgrade applied. Also: a second em dash in the repository claim (before 'ניסוח עצמאי') replaced by '; זהו'; 'כך שאין אף מקור רשמי' softened to 'כך שלא נמצא מקור רשמי' (a documented negative, HOUSE-RULES §3.3); the F2049 row written as its own inline row (no hoisting, `tx:ME41` untouched); an audit-trail sentence and the F1991 finding (ME49 not printed) appended to notes.
- `tx:ME51N`: en dashes in both Simplification List titles replaced by hyphens; '2023 FPS3' aligned to '2023 FPS03' in status.he and notes; status.he names the item 'S4TWL - MM-PUR WebDynpro Applications' (HOUSE-RULES §3.5) instead of 'פריטי S4TWL של WebDynpro'; the dropped recommendedAction sentence kept in notes, as the verdict asked.
- `tx:MIGO_GR`: 'כפעילות מתועדת ופעילה' reduced to 'כפעילות מתועדת' in evidence[1] (a snippet documents an activity, not the system state; same reading as the ME49 verdict); the doubled 'בשדה Other Terms' phrase in evidence[2] merged; the false negative in notes (ג) corrected and a repository row for `data/tx-intel.ts#MIGO_GR` added (see conflicts).
- `tx:MK01`: taken from the fixedRecord; an audit-trail sentence appended to notes.
- `tx:MK02`: writer addition of a `conflicting_sources` row for the S/4HANA 2025.001 page 'Maintaining Vendor Master Jurisdiction Code (MM)', whose snippet (re-run search 'Create Vendor Purchasing MK01', --product SAP_S4HANA_ON-PREMISE, 21 hits) prints 'MK02 to change'; the same page is the conflict row of `tx:MK01`, and HOUSE-RULES §3.4 keeps both official sides. This moves MK02 from L5 to L3. Key order aligned to the file (evidence before status).
- `tx:MK03`: em dash in the repository claim replaced by a semicolon (the verdict missed it); 'שני ה-Note Number זהה' rewritten as 'שניהם עם Note Number 2265093'; 'וכי אין ממשק ייעודי חלופי מלבדה' (an unsourced negative) replaced by the item's own statement that BP is the user interface for business-partner maintenance.
- Ids not in the universe and therefore not xref'd in this batch: F1991, F2049, F2229, F1643, F2324, F2425, F4149 (Fiori); MIGO_GO, MIGO_GI, MIGO_TR, MIGO_GS, ME4B, ME4C, ME4L, ME4M, ME4N, ME4S, MK05, MK06 (tcodes named only in prose); EKKO, EKPO, KONP, LFA1, LFM1, LFB1 (tables).

## writer deviations (batch 4, 2026-09-24)

- `tx:ME42`: taken from `verdict.fixedRecord`; both listed downgrades were already in it (F4149 under SAP_BR_BUYER in recommendedAction, 'לתהליכי sourcing' in status.he). The record was generated from the audited JSON and checked after insertion: evidence, status (apart from `source`), xrefs and lastVerifiedAt deep-equal the fixedRecord.
- `reviewer: "repairer (pipeline)"` dropped (house convention: no record in the overlay carries a reviewer field).
- `status.source` is the shared `ME42_SIMPL2025` const, which is also evidence[1] (the 2025 FPS01 item row). The verdict carried a re-typed copy of the same row with a shorter claim; title, url, release and level are identical, and every quote in the shorter claim appears in the evidence[1] claim (checked).
- notes: two sentences appended, the audit trail (refuted in batch 2, repaired, re-audited, downgrades included) and the statement that the record supersedes the generated `tx:ME42` in `data/verification/transactions-auto.ts` (the transactions.ts convention).
- status.he kept at four sentences as approved. The auditor flagged this as style only (HOUSE-RULES §1 asks for one or two sentences) and left it to the writer. It was not trimmed, so the approved wording stays intact; `tx:ME41` in batch 2 carries a longer one.
- The generated record's context row for `data/tcode-catalog.ts#ME42` ('Change Request for Quotation') was not carried over, because the title stays in the registry (`lib/tx-registry.ts` reads the catalog `en` field). Each of its official context rows (both 'Process RFQ (MM-PUR)' snippets and both Simplification List items) is covered by a researched row.
- Ids not in the universe and therefore not xref'd: F2049, F2425, F4149, F1991, F2324 (Fiori); ME44, ME45, ME4B, ME4C, ME4L, ME4M, ME4N, ME4S (tcodes named only in prose); EKKO, EKPO (tables inside the quoted tx-intel descTech).

## writer deviations (batch 5, 2026-09-24)

- Shared status-source constants, one per record, each the record's own evidence row and also its `status.source`: `MK05_SIMPL2025`, `MK06_SIMPL2025`, `MM17_SIMPL2025`, `MMBE_SIMPL2025`, `MMBE_OLD_SIMPL2025`, `MMPV_FAL_S32OP`, `MR21_SIMPL2025`, `OMS2_HELP_2025`. The verdicts carried a placeholder claim (MK05), the pointer string 'evidence[2]' (MK06), the string 'evidence[0]' followed by an em dash and the page title (OMS2), identical copies (MM17, MMBE, MMBE_OLD, MMPV) and, for MR21, a shortened copy whose claim is a prefix of the evidence row (checked); the evidence row is used, as for `tx:ME42` in batch 4.
- Source of the text: the drafts and verdicts were read from the workflow journal (`wf_4ce63408-bba` research, `wf_f495b869-e85` repair, audits), matched against the writer prompt, and emitted by a script that asserts every edit matches exactly once. After insertion all 8 records deep-equal the expected objects, and a field-level diff against the audited JSON shows only the changes listed here.
- `tx:MK05`: 'פריט המקביל' corrected to 'הפריט המקביל' in notes; the auditor's gap lead (What's New 2022 topic 'Re-direction to Business Partner (BP) Transaction', loio 220bd05aa56c49318c4fae0173cc10d4) added to notes as item (ה), saying its body was read under `tx:MK06` and what it says about MK05 was not checked.
- `tx:MK06`, `tx:MM17`, `tx:MMBE`, `tx:MMBE_OLD`, `tx:MMPV`, `tx:MR21`, `tx:OMS2`: one sentence appended to notes saying the record supersedes the generated one in `data/verification/transactions-auto.ts`, with Old and New (HOUSE-RULES §3.8; MK05's fixedRecord already had it). For MMBE, MMBE_OLD and MMPV the old value is the derived status 'unchanged' at `repository_verified`, measured with `report-coverage.mjs --ids` before this write. The convention sentence 'הרשומה אינה נושאת שדה reviewer, כמוסכמת הקטלוג' added where it was missing (MM17, MMBE, MMBE_OLD, MMPV, OMS2).
- `tx:MM17`: 'ביקורת אדברסרית חוזרת (2026-09-24) אישרה את הרשומה המתוקנת ללא הורדות' appended to notes (the journal shows the repair step, then a re-audit with empty problems and downgrades).
- `tx:MR21`: 'כשuniversal' corrected to 'כש-universal' in notes.
- `tx:OMS2`: the four listed downgrades applied (the IMG path removed from recommendedAction; `status.source` is the evidence[0] object; the four em dashes in notes replaced by a colon, a colon, a semicolon and a semicolon; evidence[3] now says 'נוסח התהליך המומלץ תואם' and 'אזכור חד-פעמי'). Writer additions: 'המצאות' corrected to 'הימצאות' in evidence[3]; 'בלבד' dropped from status.he (HOUSE-RULES §3.2, the 'only' framing the MMPV auditor struck); 'כטרנזקציה הפעילה' reworded to 'כטרנזקציה להצמדת Field Reference לסוג חומר' in status.he and evidence[2], because the item prints 'go to transaction OMS2' and says nothing about the state of the code (the MMPV and MMBE auditors struck 'פעילה' readings of the same kind); `reviewer: null` dropped; an audit-trail sentence appended to notes.
- Release strings kept as audited: the Simplification List rows that MMBE and MR21 use as status source carry '2025.001' (auditor-approved, equal to their status.release), while other rows of this shard use '2025 FPS01' for the same PDF. Not aligned, so the audited status and source pairs stay as approved.
- Ids not in the universe and therefore not xref'd in this batch: F1076, F1077, F2680, F4006, F6684, F6489 (Fiori); OMSR, OMSA, OMS9, MMCC (tcodes named only in prose); T130F, T130A, T133F, CKMLPP, CKMLCR, MLCD, CKMLMV011, MLDOC, MLDOCCCS, MLDOC_EXTRACT, MLDOCCCS_EXTRACT, MLRUNLIST (tables named only in prose).

## writer deviations (batch 6, 2026-09-25)

- Source of the text: the audited JSON was extracted verbatim from the writer prompt as recorded in the workflow's agent transcript and emitted by a script (adapted from the batch-5 generator) that asserts every edit matches exactly once. After insertion the 7 records deep-equal the expected objects, and a field-level diff against the audited JSON shows only these fields changed: OMSL notes, recommendedAction, status.release; XK01, XK02, XK03, MCXB and MCXC notes; MCVA evidence[0], evidence[2] and evidence[3] claims, status.he, recommendedAction and notes; plus every `status.source` (next line).
- Shared status-source constants, one per record, each the record's own Simplification List item row and also its `status.source`: `OMSL_SIMPL2025` (evidence[0]), `XK01_SIMPL2025` (evidence[1]), `XK02_SIMPL2025` (evidence[0]), `XK03_SIMPL2025` (evidence[0]), `MCVA_SIMPL2025` (evidence[0]), `MCXB_SIMPL2025` (evidence[2]), `MCXC_SIMPL2025` (evidence[1]). The verdicts carried placeholder copies (OMSL, MCXB), identical copies (XK01, XK02), the pointer strings 'evidence[0]' / 'evidence[1]' (XK03, MCXC) and a copy whose claim reads 'ראו evidence[0]: ...' (MCVA); every non-claim field was checked identical to the evidence row before substitution.
- `tx:OMSL`: status.release '2025.001' aligned to '2025 FPS01', the release of its source row (the shard convention for SIMPL_OP2025 rows that the XK03 auditor restated; the same release as versionId 2025.001). 'כמתואר בפריט 5.1.5' in recommendedAction replaced by the item name (HOUSE-RULES §3.5). An audit-trail sentence, the supersede sentence (Old: verification_required; New: unchanged) and the reviewer-convention sentence appended to notes.
- `tx:XK01`: the notes history described the generated record as '(verification_required, סניפטים בלבד)'. It carried repository, help.sap.com and Simplification List context rows and no status, and the derived layer showed 'unchanged' at `repository_verified`; the parenthesis now says that (Old → New kept). Everything else verbatim from the fixedRecord.
- `tx:XK02`: the one listed downgrade applied (16 of the 18 MDG role pages print 'XK02 Change Vendor (Centrally)' in the 'necessary for the CVI implementation' context; two, with their loios, print 'XK02 Change vendor (centrally)' in the 'called by the CVI implementation' context). A second-round audit sentence and the supersede sentence (Old: derived 'changed' at `repository_verified`) inserted before the reviewer-convention sentence.
- `tx:XK03`: taken from the fixedRecord; the measured old value (derived 'unchanged' at `repository_verified`), an audit-trail sentence and the reviewer-convention sentence appended. The approved recommendedAction keeps 'יש לבדוק קוד מותאם, דוחות Z וקיצורי דרך בתפריטים שמניחים XK03 כטרנזקציה עצמאית', while the XK01 auditor struck the analogous XK01 sentence as unsourced. Not aligned: both records stay as audited.
- `tx:MCVA`: re-derived from the draft with all nine downgrades. Writer adjustments inside them: evidence[2] reads 'מהדורות 1909 אינן מופיעות ברשימה' (the verdict text had 'אינן מופיעה'), with the predecessor and successor clause kept as its own sentence; status.he opens with the auditor's suggested sentence, with single instead of double quotes around the repository label, and the release-list fix folded into the second sentence without a double parenthesis; the example list in recommendedAction puts the two inspection-lot apps first and keeps the draft's three apps after them, all with verbatim names ('Last 365 Days'); the notes (1) anchor matched the draft's actual wording (כ'Overview', not כ-Overview). Writer additions: 'רכיב QM-QC-IS' added to the Fiori Apps Library row (the auditor's re-run of `fal-app.mjs MCVA --release S32OP` printed it, and the new status.he names it); the notes (2) conclusion 'אין עמוד תיעוד ייעודי ל-MCVA ...' reworded to say the three searches returned no dedicated page (a documented negative, HOUSE-RULES §3.2 / §3.3); the item named in notes (4) (§3.5); an audit-trail sentence, the measured old level and the reviewer-convention sentence appended. status.he stays at five sentences as approved (HOUSE-RULES §1 asks for one or two; not trimmed, as for `tx:ME42` in batch 4).
- `tx:MCXB`: taken from the fixedRecord, including the three U+200F marks inside the app list of evidence[2].claim (the overlay files use RLM as a bidi aid; shard B already carried 54). An audit-trail sentence, the supersede sentence and the reviewer-convention sentence appended.
- `tx:MCXC`: the supersede sentence completed with Old → New (verification_required to compatibility_scope) and the reviewer-convention sentence appended. xrefs kept as audited, including `tx:MCXA`, refuted in this batch: it still resolves through the route manifest and its generated record.
- `test/evidence-schema.test.ts` not touched: the transactions catalog graduated on 2026-09-02 and `TX_VERIFICATION_B` is already in the graduated repoRef test. `data/verification/index.ts` and the new `data/verification/transactions-c.ts` (another chain's work, uncommitted) were not touched; shard C is empty, so no id collides.
- `report-coverage.mjs --ids` writes `coverage-ids.json` into the working directory unless `IDS_OUT` is set; the first (before) run of this write created it in the repo root, and it was moved to the scratchpad (untracked, generated); the after run used `IDS_OUT`.
- Ids not in the universe and therefore not xref'd in this batch: F3239, F3273, F3383, F3382, F2428, F3584, F3583 (Fiori); FLETS, MCVZ, MCVP, MCVX, MCVB, MCVD (tcodes named only in prose).

## writer deviations (batch 7, 2026-09-25)

- Source of the text: the audited JSON was extracted verbatim from this writer's prompt (the first line of its workflow transcript) and emitted by a script adapted from the batch-6 generator that asserts every edit matches exactly once. After insertion the 9 records deep-equal the expected objects, and a field-level diff against the audited JSON shows only these fields changed: the notes of all nine; MSC3N status.he; QC20 evidence[2].sapNote and evidence[3].sapNote (removed); plus every `status.source` that the verdicts carried as a string (next line).
- Shared status-source constants, one per record, each the record's own evidence row and also its `status.source`: `MCXA_SIMPL2025` (evidence[1], with its nested Fiori Apps Library `conflictingEvidence` row), `MCXX_SIMPL2025` (evidence[0]), `MSC1N_FAL_S32OP` (evidence[3]), `MSC2N_SIMPL2025` (evidence[0]), `MSC3N_SIMPL2025` (evidence[0]), `MSC4N_FAL_S32OP` (evidence[2]), `QA32_F2343_FAL_S32OP` (evidence[4]), `QC20_SIMPL2025` (evidence[2]), `QC21_SIMPL2025` (evidence[2]). The verdicts carried identical full copies (MCXA, QA32, QC21: every field, the claim included, checked equal before substitution), placeholder strings (MCXX, MSC1N, MSC2N, MSC4N, QC20) and the pointer string 'evidence[0]' (MSC3N).
- `DATE25 = "2026-09-25"` added at the top of the file (the access date to stamp). `tx:MCXA` keeps DATE24 on evidence[0], evidence[1] (and its nested row) and evidence[3], as drafted and audited: those sources were read on 2026-09-24 in batch 6. Its re-read 2023 FPS03 row and every row of the other eight records carry DATE25.
- `tx:QC20`: the `sapNote: "2270126"` field dropped from the two Simplification List rows (evidence[2], also the status source, and evidence[3]). The validator's `sap-note-format` rule accepts a sapNote field only with a me.sap.com/notes url or a repoRef, and both rows cite the help.sap.com PDFs; the number stays in the claim prose, as in every earlier batch of this shard (batch-3 header note). In notes, 'לפי פריט 9.6.6' now names the item 'S4TWL - ITS services in QM' (HOUSE-RULES §3.5).
- `tx:MSC3N`: 'רק' ('only') dropped twice where it framed what the item states: status.he 'קובע רק שהקוד הישן MSC3' reads 'קובע שהקוד הישן MSC3', and notes 'MSC3N מוזכר רק כ-functional equivalent' reads 'MSC3N מוזכר בו כ-functional equivalent' (HOUSE-RULES §3.2; the MSC1N auditor struck the same framing of the same item, 'בלבד'). '(לא רק את הכותרת)', which describes what the researcher read, is kept. The three U+200F marks the auditor put in the notes are kept (bidi aid, as in batch 6).
- History (HOUSE-RULES §3.8): each record now names the generated `transactions-auto.ts` record it supersedes with Old → New, Old being the value `report-coverage.mjs --ids` measured before this write. Appended for MSC3N, MSC4N and QA32 (derived 'unchanged' at `repository_verified`); completed inside the existing history sentence for MCXA and MCXX (verification_required), MSC1N and MSC2N (derived 'unchanged' at `repository_verified`), QC20 and QC21 (verification_required). The MSC4N auditor noted that its generated record has no status, so nothing was owed under rule 8; the sentence records the derived value the page showed, as in batches 5 and 6.
- `tx:MCXA`: notes (4) now names the item 'S4TWL - Quality Management Information System (QMIS)' (§3.5, as for MCVA in batch 6), and 'ביקורת אדברסרית חוזרת (2026-09-25) אישרה את הטיוטה המתוקנת ללא בעיות וללא הורדות' is appended. The convention sentence 'הרשומה אינה נושאת שדה reviewer, כמוסכמת הקטלוג' appended to the eight records that lacked it (MCXX's fixedRecord has it). No audit-trail summaries were added this time: the MSC4N auditor struck notes that describe an unpublished draft, and the fixedRecords already carry the history their auditors asked for.
- Release strings kept as audited: the 2025 Simplification List rows of MSC2N and MSC3N (both status sources) carry '2025.001' with an equal status.release, while MCXA, MCXX, MSC1N, MSC4N, QC20 and QC21 carry '2025 FPS01' for the same PDF (the MSC1N and QC21 auditors aligned theirs). MSC4N's status.release is '2025 FPS01' with the Fiori Apps Library row ('2025.001') as source, as approved. Not aligned. The MSC2N source titles keep the en dash (U+2013) of the audited titles; HOUSE-RULES §3.7 bans the em dash.
- `test/evidence-schema.test.ts` not touched: the transactions catalog is graduated and `TX_VERIFICATION_B` is already in the graduated repoRef test. `data/verification/index.ts` drops the nine generated records through RESEARCHED_TX; no id collides with `transactions.ts` or `transactions-c.ts`.
- Gates: `tsc --noEmit` and `tsc --noEmit -p tsconfig.test.json` exit 0; `npm test` 211 tests, 211 pass, 0 fail; coverage as in the batch-7 intro. The `--ids` runs wrote to the scratchpad through `IDS_OUT`.
- Ids not in the universe and therefore not xref'd in this batch: F2343, F2360, F2361 (Fiori, named by QA32; `fal-app.mjs --tcode QA32` at S32OP lists them as apps leading with QA32); QA32WP, WAO_QA32WP, QPQA32, WAO_QPQA32, MSC4, MSC5N, MSC6N, QC40, QC40A, QC42, QEW01, QEW01V, QMW1, QISR, QISRW (tcodes named only in prose). MSC1, MSC2 and MSC3 resolve; `tx:MSC3` is an xref of MSC3N, as audited.

## writer deviations (batch 8, 2026-09-25)

- Source of the text: the audited JSON was extracted verbatim from this writer's prompt (the first line of its workflow transcript) and emitted by a script adapted from the batch-7 generator that asserts every edit matches exactly once. After insertion the 7 records deep-equal the expected objects, and a field-level diff against the audited JSON shows only these fields changed: the notes of all seven; QE51N and QGP1 status.he; QM10 evidence[0].sourceTitle; QM50 reviewer (removed); QI03 evidence[2].sourceTitle, the claims of evidence[2] to evidence[5], status.he, status.recommendedAction and notes (its eight downgrades plus the writer changes below); plus every `status.source` given as a string (next line).
- Shared status-source constants, one per record, each the record's own evidence row and also its `status.source`: `QDV3_SIMPL2025` (evidence[0]), `QE51N_FAL_S32OP` (evidence[1]), `QGA1_SIMPL2025` (evidence[2]), `QGP1_SIMPL2025` (evidence[1]), `QI03_SIMPL2025` (evidence[5]), `QM10_FAL_S32OP` (evidence[0]), `QM50_SIMPL2025` (evidence[2]). The verdicts carried identical full copies (QDV3, QE51N, QM10: every field, the claim included, checked equal before substitution) and pointer strings ('evidence[2]' for QGA1 and QM50, 'evidence[1]' for QGP1, 'evidence[5] (item 9.6.2 ...)' for QI03). Every source row's release equals its record's status.release (checked).
- `tx:QDV3`: the verdict's last downgrade asked the writer to regenerate `data/verification/transactions-auto.ts` so the generated `tx:QDV3` (line 59974) drops out. Not done: that file is outside this writer's files, and both `data/verification/index.ts` and `test/evidence-schema.test.ts` drop a generated record whose id a researched shard carries (RESEARCHED_TX), the path every earlier batch used; `npm test` (211 of 211, duplicate-id included) shows no collision. The conflicts line the verdict asked for is above.
- `tx:QE51N`: status.he opened with 'QE51N פעילה וזמינה ב-SAP S/4HANA On-Premise (מאומת גם למהדורת 2025 FPS01 בספריית האפליקציות של Fiori)' and now opens with 'QE51N מתועדת כזמינה ב-SAP S/4HANA On-Premise: ספריית האפליקציות של Fiori רושמת אותה כאפליקציית SAP GUI בסטטוס Published במהדורת 2025 FPS01 (S32OP).' The QE51N auditor struck the same 'פעילה' inference in evidence[1] (the library prints Published), and the QM10 auditor struck the same wording in its status.he in favour of 'מתועדת כזמינה'; the status stays `unchanged`.
- `tx:QGP1`: status.he and notes (1) cited the two items by bare number ('שני פריטי S4TWL רשמיים (2023 FPS03 §34.3, 2025 FPS01 §9.6.1)'); both now name them 'S4TWL - Removed Navigation to Transactions for Evaluations in QM' (HOUSE-RULES §3.5, the rule the QE51N auditor applied to its recommendedAction).
- `tx:QI03`: no fixedRecord; all eight downgrades applied with the verdict's own replacement text for evidence[2], evidence[3], evidence[4], evidence[5] and recommendedAction. Writer changes inside them: in evidence[4] the kept apps.ts sentence covers both ids and reads 'אינם רשומים בקטלוג ה-Fiori המקומי' (was 'F2256 אינו קיים בקטלוג fiori המקומי'); the gaps[0] sentence the verdict rewrote is carried into notes, with F2256A named as missing from `data/fiori/apps.ts`; the item is named in status.he (§3.5; was 'פריטי הפישוט הרשמיים של QM'); 'מבוססת אך ורק על נוסח' in notes reads 'מבוססת על נוסח' (§3.2; the QA32 auditor struck 'אך ורק' in batch 7); a final period closes the verdict's recommendedAction text. evidence[3] keeps the auditor's '(1909)' for S15OP-S17OP: the QI03 auditor read those releases in the F2256 release list, while the QM13 auditor flagged the same kind of mapping as an inference for the QM13 output (see refuted). Not aligned.
- `tx:QM10`: the em dash (U+2014) between 'SAP Fiori Apps Reference Library' and 'QM10' in the Fiori Apps Library title (evidence[0] and its status.source copy) replaced by ' · ' (HOUSE-RULES §3.7; the QGP1 auditor struck the same title pattern and the verdict's em-dash check missed this one). The en dashes in the two Simplification List titles are kept as audited (the batch-7 MSC2N precedent).
- `tx:QM50`: the fixedRecord's `reviewer: "researcher"` dropped (house convention). The verdict's two remaining downgrades (gaps[6] without QM51, and the summary sentence on F3583) touch the research summary only; neither QM51 nor that sentence appears in the written record (checked).
- History (HOUSE-RULES §3.8): each record names the generated `transactions-auto.ts` record it supersedes with Old → New, Old being the value `report-coverage.mjs --ids` measured before this write. Appended for QDV3, QGA1, QGP1 and QM10 (derived 'unchanged' at `repository_verified`); completed inside the existing history sentence for QE51N and QI03 (derived 'unchanged' at `repository_verified`) and QM50 (verification_required). The convention sentence 'הרשומה אינה נושאת שדה reviewer, כמוסכמת הקטלוג' appended to all seven. No audit-trail summaries added (the batch-7 practice).
- Kept as audited: repository facts inside official-level claims where their auditors approved them (QI03 evidence[4] and QM50 evidence[4]: not in `data/fiori/apps.ts`), although the QDV3 auditor moved the same kind of sentence to notes; release strings '2025.001' on the QDV3, QE51N and QGA1 source rows and '2025 FPS01' on the QGP1, QI03 and QM50 source rows (each equal to its status.release). Not aligned.
- `test/evidence-schema.test.ts` not touched: the transactions catalog is graduated and `TX_VERIFICATION_B` is already in the graduated repoRef test. `data/verification/index.ts` drops the seven generated records through RESEARCHED_TX; no id collides with `transactions.ts` or `transactions-c.ts` (checked before the write).
- Gates: `tsc --noEmit` and `tsc --noEmit -p tsconfig.test.json` exit 0; `npm test` 211 tests, 211 pass, 0 fail; coverage as in the batch-8 intro. The `--ids` runs wrote to the scratchpad through `IDS_OUT`. An untracked `coverage-ids.json` in the repo root (written at 01:25, between this writer's two runs, byte-identical to its before-state) comes from another chain's run without `IDS_OUT`; not touched.
- Ids not in the universe and therefore not xref'd in this batch: F2255, F2689, F3273, F2428, F2256, F2256A, F3583 (Fiori); QE51, QS45, QCYF, QCYT, SO10, QGA4, QE09, QGC2, QGC3, QST05, QM10WP, WAO_QM10WP, QPQM10, WAO_QPQM10 and the other ITS/MiniApp codes of item 9.6.4 / 34.7 (tcodes named only in prose); QALS, QAMR, QAVE, QDPS, QDPV, QINF (tables named only in prose). QGA2, QS28, QGC1, QS42 and SE71 resolve in the route manifest but are not in the audited xrefs, so none was added.
