# בדיקת AI חיה · תסריט ידני (S7-AI-6 · MANUAL_LIVE_TEST_REQUIRED)

**מצב:** לא בוצע בסבב זה. הקריאות ל-`/api/library-stream` ו-`/api/consult-stream` פונות לשירות חיצוני בתשלום (`NEXT_PUBLIC_BOOKS_API_URL`, ברירת מחדל `sap-books-api.vercel.app`), ואין בסביבת העבודה סביבת בדיקה מוגדרת ובטוחה; לפי התדריך (§9) לא בוצעו קריאות בתשלום ללא אישור. מצבי הממשק נבדקו במלואם בתגובות מבוקרות (`scripts/qa/ai-states-check.mjs`, 27/27 · `audit/ux-2026-09/ai-states.round6.json`); מה שנותר לבדיקה חיה הוא **איכות התשובות** מול הספרים.

## תנאים מקדימים
- Preview של הענף (או Export מקומי של אותו HEAD) + חיבור רשת + מפתח שירות פעיל בצד השרת.
- דפדפן במצב פרטי (היסטוריית שיחות ריקה: `localStorage` `neo:ai:*` ריק).
- לרשום לכל שאלה: מסך, היקף, זמן עד טוקן ראשון, זמן כולל, מספר קטעים ("N קטעים נקראו"), מספר מקורות, האם כל ציטוט פותח את הסעיף הנכון (מודגש ב-`<mark>`).

## תסריט · "עזרה מהספרייה" (`/neo/ai/`)
| # | היקף | שאלה | ציפייה (מקור אמת) | תוצאה |
|---|---|---|---|---|
| L1 | כל הספרייה | "מהי הודעת אחזקה (Maintenance Notification) ואילו טבלאות מאחסנות אותה?" | תשובה עם מקורות מספרי PM (ספר 9 / ספר 1); ציטוט של QMEL/QMIH אם מופיע בספר; ללא המצאת טבלאות שאינן בספר | |
| L2 | ספר 1 · פרק 1 | "מהו התהליך המוצע לפרויקט PM?" | תשובה מוגבלת לפרק 1 של ספר 1; ציטוט לסעיף 1.1; "N קטעים נקראו" ≥ 1 | |
| L3 | סעיף (ספר 2 · 3.2) | "הסבר בפשטות" | מסלול מהיר (FOLLOW-UP/פשטות בהיקף סעיף), תשובה קצרה, ציטוט לסעיף 3.2 בלבד | |
| L4 | כל הספרייה | "מה מספר ה-SAP Note לתיקון COGI?" | סירוב מנומק ("לא נמצא מקור מאומת…") או תשובה ללא מספר Note מומצא; אין מספר Note שלא מופיע בספר | |
| L5 | כל הספרייה | "צייר תרשים זרימה של תהליך פקודת אחזקה" | תרשים (flowchart) או הודעה שהתרשים אינו נתמך; אין תוכן מומצא | |
| L6 | כל הספרייה | שאלה בת 900 תווים (הדבקה) | ללא קריסה; תשובה או שגיאה מנומקת; המקורות פתוחים | |
| L7 | — | לחיצה על "עצירת התשובה" באמצע כתיבה | "התשובה נעצרה…" + כפתור ניסיון חוזר; אין טקסט חלקי שנשמר | |

## תסריט · "שיחה כללית על SAP" (`/neo/chat/`)
| # | שאלה | ציפייה | תוצאה |
|---|---|---|---|
| G1 | "מה ההבדל בין IP30 ל-IP30H?" | תשובה כללית מסומנת ברמת ביסוס; אם נמצאו מקורות בספרייה הם מוצגים; אין מספרי Note/KBA מומצאים | |
| G2 | "מה מצב טבלת AFKO ב-S/4HANA?" | תשובה עקבית עם דף הטבלה (ללא שינוי ב-S/4HANA); ציון שאין גישה חיה ל-SAP | |
| G3 | "תן דוגמה מעשית ל-BAPI_ALM_CONF_CREATE" | דוגמה + הסתייגות שדרושה בדיקה במערכת; COMMIT מוזכר רק אם נכון לפי הדף | |
| G4 | שאלה בשפה מעורבת (עברית + מזהים) | מזהים מוצגים LTR מבודדים; אין היפוך סדר | |

## קריטריוני קבלה
- 0 ציטוטים שאינם פותחים סעיף קיים (כל `citationHref` → `<mark>`).
- 0 מספרי SAP Note / KBA שאינם בספרים או במאגר.
- מדדי זמן נרשמים ("תחילת התשובה X שנ׳"), ולא מוערכים.
- כל שגיאה מוצגת בכרטיס הכשל עם ניסיון חוזר (כמו במצבים המבוקרים).

## מה כן נבדק אוטומטית (ללא שירות חי)
`scripts/qa/ai-states-check.mjs` על `/neo/ai/` ו-`/neo/chat/` ב-1363 וב-390 (iPhone UA): unavailable (503), generic (500), incomplete (stream ללא done), empty (done ריק → "לא התקבלה תשובה", תוקן בסבב 6), long (5,186 תווים, 0 גלישה), citations (2 מקורות), usage (שורת המטא), busy + stop, retry — 27/27.
