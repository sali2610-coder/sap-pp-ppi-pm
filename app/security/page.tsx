import { SecurityCenter } from "@/components/security-center";

export const metadata = { title: "SAP Security & Authorizations Center · NEO" };

export default function SecurityPage() {
  return <SecurityCenter />;
}
