import type { Metadata } from "next";
export const metadata: Metadata = { title: "Infrastructure Center · תשתיות SAP" };
export default function InfraLayout({ children }: { children: React.ReactNode }) { return children; }
