import type { Metadata } from "next";
export const metadata: Metadata = { title: "Library · ספריית ידע" };
export default function LibraryLayout({ children }: { children: React.ReactNode }) { return children; }
